# 期货量化交易系统 — 需求文档 (PRD)

> 版本：**v3.0（2026-09-22 精简重构版）** ｜ 初版：2026-09-16
> 本文件是 codebuddy（CB）编码、workbuddy（WB）复审的**唯一权威需求来源**，合并并取代《期货预测平台需求文档_PRD.md v1.3.5》与 WB 的融合/信号/回测补充文档。
> 工程根目录 `E:\Docker\qhyc`；数据模板源 `E:\QH\{PRODUCT}\data`；桌面权威参考 `C:\Users\Seven\Desktop\融合\终版资料\01~04`。
> **当前策略版本：融合策略 V3.4（§6 定稿口径）**｜引擎状态：线上 `app/strategies/fusion_signal.py` 已落地并验收（10 项锚点偏差全 0）；同花顺/文华公式已同步 V34。

> **v3.0 重构说明**：本版按**系统整体架构**重写，删除全部「一次变更 = 一章节」的流水记录（原 §15–§20），
> 将有效结论归并回所属架构层；**已证伪 / 已停用 / 预判勿做 / 数据受阻的条款一律删除，不再保留**（原 §19.3 勿做清单、§17.5 证伪表、§16/§17 停用项均不进入本版）。
> 精简原则：**只写"当前该做什么"**。被删掉的证伪结论其唯一残留价值是"防止重复试错"，已归并为 §6.8 的**一行禁令清单**（无解释、无编号、无数据）。
> 原版全文备份于 `docs/期货量化交易系统需求文档_PRD.md.bak_20260922`，需要历史实证细节时查该备份。

---

## 1. 项目概述与目标

构建一个**本地运行的期货量化交易系统**，在统一数据底座上具备四项能力：

1. **预测系统**（CB 主导）：基于数学/统计/机器学习模型，预测下一交易日涨跌方向与幅度。
2. **交易策略判定**（WB 主导）：按**融合策略 V3.4**，在小时线级别判定每个品种的持仓状态与开/平/反手信号。
3. **信号推送**（WB 主导）：将信号实时推送至微信/企微/钉钉/飞书，含止损位、保本位、持仓简报。
4. **回测与研究**（WB+CB）：手动调参回测、鲁棒性测试、walk-forward 验证，纳入系统 UI。

**非目标（本期）**：自动下单执行、多用户账号体系、云端多租户（架构预留云端演进）。

> 一句话定位：**预测系统给"方向概率"，交易策略给"当下该不该动"，信号推送把结论送到手里，回测研究验证策略稳健性**——四者共用同一数据底座与同一套口径铁律。

---

## 2. 总体架构（分层）

```
[数据源] akshare(主,含新浪60分) ──┐
                                  ├─→ [采集/校准层] 入库 + tqsdk 补缺/比对 + 异常工单 + 小时线逐根增量
[校准源] tqsdk(天勤) ──────────────┘
                                       ↓
[数据层] PostgreSQL 16 + TimescaleDB
   · 运行时表：daily_bar(日线) / hourly_bar(小时线,逐根增量) / main_continuous(主连)
   · 回测底座：fut_kline 超表(freq×kind×symbol×trade_datetime，含 cont_adj 复权)
                                       ↓
[计算层] ┌─ 预测引擎（方向/幅度，模型库集成）            ← CB
         ├─ 交易策略引擎 fusion_signal（开/平/反手状态机） ← WB
         └─ 回测引擎（信号层 walk-forward + 组合层 FIFO 重放） ← WB/CB
                                       ↓
[服务层] FastAPI：行情 / 预测 / 信号 / 回测 / 数据更新 / 任务 / 异常 / 对接
                                       ↓
[展示层] React 看板（预测 / 信号 / 回测调参）+ 微信推送（pushplus/webhook）
```

**分层职责（一句话）**：数据层只负责"准确、单一口径地把数据落下"；计算层三个引擎各自独立、互不改写对方输出；服务层只做暴露；展示层只做呈现。

---

## 3. 技术栈（已确认）

| 层 | 选型 |
|---|---|
| 语言 | Python 3.11+ |
| 后端 | FastAPI（异步，自动文档） |
| 数据库 | PostgreSQL 16 + TimescaleDB 2.17.1（超表） |
| 前端 | React 18 + Vite + TS + AntD + ECharts |
| 计算 | scipy / statsmodels / scikit-learn / PyTorch / prophet |
| 调度 | APScheduler（本地常驻） |
| 行情源 | akshare（主，含新浪 60 分钟）+ tqsdk（校准/兜底/复权） |
| 推送 | pushplus（微信主通道）+ 通用 webhook（企微/钉钉/飞书 降级） |
| 部署 | Docker Compose（本地起步，云端演进预留） |

---

## 4. 数据层 ★ 重点

### 4.1 落盘位置（物理与逻辑）

**所有行情最终落盘 Docker 容器内 TimescaleDB，不是本地 CSV/文件。**

- 容器 `qhyc-timescaledb`；库/用户 `futures`；端口 5432 映射宿主机。
- 物理卷：`E:/Docker/qhyc/pgdata/`（容器 `/var/lib/postgresql/data` 挂载）。
- 启动前提：**必须 Docker Desktop 运行**。
- 本地 `E:/QH/...` 与容器导入源仅作**离线备份/导入**，非落盘点，上线不依赖。

### 4.2 表架构（四表，用途不同，禁止混用）

| 表 | 周期 | 主键 | 写入方 | 用途 |
|---|---|---|---|---|
| `daily_bar` | 日线 | `(symbol, trade_date)` | `orchestrator.upsert_daily_bars` | akshare 主源+校准，日级特征/展示 |
| `hourly_bar` | 小时线 | `(symbol, trade_datetime)` | `HourlyCollector`（逐根增量 + 定时全量兜底） | **信号引擎实时读取**（按 `fusion.hourly_src` 单源） |
| `main_continuous` | 主连 | `(product, trade_date)` | `upsert_main_continuous` | 同时存裸价与复权价 |
| `fut_kline`（超表） | 全频率/全口径 | `(freq, kind, symbol, trade_datetime)` | `fdf/*` + `runtime/_build_daily_*` | **回测/研究底座**，含 `cont_adj` 复权权威 |

`fut_kline.kind ∈ {continuous, contract, cont_adj}`；`freq ∈ {daily, hourly, min15, ...}`；`adj` 列标记已/未复权段（回测只取 `adj=True`）。

### 4.3 获取与更新机制

#### 4.3.1 日线更新（路径 A，已接调度）

- 主源 akshare `futures_main_sina` → `daily_bar`（`src=akshare`，未复权拼接价）。
- 校准：tqsdk 补缺 + 二次比对，异常开 `anomaly_ticket`（不阻塞主源）。
- **频率**：每日三次 cron —— `12:00`(noon) / `16:00`(close) / 次日 `08:00`(night)。
- 增量幂等：`(symbol, trade_date)` upsert，断点可续；范围 `2015-01-01` 起。

#### 4.3.2 小时线更新（★逐根增量 —— 用户硬性要求）

> **需求：小时线更新必须"每完成一根小时线，更新一根"**，而非定时批量重抓整段。

**机制 `HourlyBarIncrementalUpdater`（扩展 `app/ingest/hourly_collector.py`）：**

- **触发**：每个交易时段内的小时 K 线**收盘时刻之后**增量采集。推荐组合：
  - (a) **分钟级 cron**：小时线收盘时点后 1 分钟触发（`:01`/`:31`/`:46`，覆盖白盘 10:15/11:30/13:30/14:15/15:00、夜盘 21:00/22:00/23:00/23:30/01:00/02:00/02:30 等，由配置化收盘时刻表驱动）；
  - (b) **轮询兜底**：每 5 分钟检测"比本品种上次成功更新更晚收盘的 bar"，补采遗漏。
- **行为**：只抓「自上次成功更新以来**新收盘**的小时 K」，**逐根 upsert** 到 `hourly_bar`（主键覆盖）。
- **定稿守卫**：最新一根距现在 < `settle_delay_sec=150s` 时等待定稿后重采一次覆盖（防收盘瞬间过渡值致反手）。
- **主源/兜底**：akshare 新浪 60 分钟主源；失败/限流 → tqsdk `get_kline_serial` 兜底（全品种**复用单 TqApi 连接**）。
- **断点续传**：记录每品种最后更新时间；历史回填用 `--prefer tqsdk` 并调大 `data_length`。
- **与既有机制互补**：保留 ① `16:00` close 档联动的全量快照 `collect_all()`（对齐/校准）、② 融合扫描每 15 分钟尾部 800 根刷新（容错）。逐根增量是"准实时主通道"，批量是"兜底校准通道"。

#### 4.3.3 回测底座 `fut_kline` 更新（非 scheduler 自动）

- `continuous`/`contract`：tqsdk `fetch_fdf.py` → `db_pg.save_bars`。
- `cont_adj`：`build_continuous.py` + `adjust_fdf.py` 重建（**先 `delete_bars` 清旧再插**，防脏累积）。
- `daily`（回测用）：由 hourly 聚合（`_build_daily_*.py`，含夜盘归属修正 ≥20:00 归下一交易日）。
- **触发**：手动 / CI（容器 `RUN_ON_BOOT=0`，避免重启重复全量回补）。数据更新后应重跑路径 B/C + 日线聚合，再跑回测。

#### 4.3.4 周/月维护（scheduler）

| 时间 | 任务 |
|---|---|
| 周六 06:00 | LSTM 周重训 |
| 周六 06:30 | 传导权重周重算（corr/granger/te/var） |
| 周六 07:00 | M4 周度回测 |
| 每月 1 日 06:30 | 模型权重月更（近 60 日准确率） |

#### 4.3.5 一致性红线（口径铁律）

1. **单源消费**：信号引擎按 `fusion.hourly_src`（默认 `akshare`）读 `hourly_bar` 唯一口径；回测按 `(freq,kind,symbol,src)` 单源读 `fut_kline`。**杜绝双源混读**（两源 K 线时刻标记方式不同，会被当成两根喂指标，砍半周期）。
2. **剔除未收盘 K**：`drop_forming_bars` 丢弃 `now < dt` 的正在形成 K。
3. **定稿守卫**：`drop_unsettled` 剔除收盘距现在不足 `settle_delay_sec` 秒的最新一根。
4. **复权唯一权威**：回测复权价只取 `fut_kline.cont_adj`；`daily_bar`/`hourly_bar` 的 akshare 主连**未复权**。
5. **涨跌口径统一 `close`**（非 settle）。
6. **回测价格口径 = `continuous`**（= 同花顺真主连，50 笔成交逐笔比对 Δ=0.0），**非** `cont_adj`（旧回测系统的偏移价）。
7. **标签完整**：任何数据源入库带 `src`；回测/信号指定 `src` 读取。

### 4.4 复权标准（加法平移前复权）

- 主连换月仅一处「新旧合约价差」断层，正确修复是「把断层之前全部历史整体平移 delta」。
- 算法：`pick_dominant`（日持仓量最大为主力，`ROLL_CONFIRM=3` 日领先确认、只向前换）→ `delta=新(t-1)收−旧(t-1)收` → 前复权 `t` 前全部 `+=delta`；剔除未走完 K 与主力持仓不达标（`RELIABLE_OI=100000`、`RELIABLE_HOLD=20`）早期区间；`adj` 列标记。
- 口径归口：`fut_kline.cont_adj` 唯一权威；`main_continuous.adj_*` 另存主连复权裸价供线上特征。

### 4.5 字段统一约定

| 项 | 约定 |
|---|---|
| 品种规格 | `fdf/symbols.json`（`code_digits`/`multiplier`/`tick`/`months`） |
| 交易所代码 | CZCE(大写)/SHFE/DCE/INE/CFFEX/GFEX(小写品种码) |
| 合约码位数 | 郑商所 3 位（FG609），其余 4 位（rb2601） |
| 时间 | `trade_datetime` = 北京时间墙钟的 K 线**收盘时刻** |
| 价格精度 | `NUMERIC(20,4)`；展示按 `_TICKS` 最小变动价位对齐 |

---

## 5. 预测引擎（CB · 摘要 + 锁定裁决）

> 详细模型实现沿用旧 PRD §5/§16/§17/§18。本节保留**对编码有约束力的锁定裁决**，CB 照此实现，WB 照此复审。

### 5.1 特征工程

输入窗口：当日涨跌幅 + 前 5/20 根 K 涨跌幅；波动率、动量、Hurst、样本熵、ATR；Zigzag 拐点、Qi(ATR+MACD) 辅助。

### 5.2 模型库（按族）

变换类（傅里叶/小波）、统计时序（ARIMA/SARIMA/GARCH/卡尔曼）、状态概率（马尔可夫/HMM/贝叶斯/蒙特卡洛）、ML/DL（LSTM/GRU/Transformer/XGBoost/LightGBM/RF/GPR）、复杂系统（Hurst/样本熵/相空间重构）。

### 5.3 集成机制

- **状态门控**：Hurst 判定 trend/mean_revert 选择启用子集。
- **方向投票**：加权投票（权重每月按近 60 日回测准确率更新），起步涨/跌二分类。
- **幅度融合**：中位数点估计 + [P5,P95]。
- **输出契约**（v1 JSON，对接简报复用）：`symbol/target_date/as_of/run_id/direction/direction_prob/ret_point/ret_low/ret_high/confidence/participated_models/state`。

### 5.4 锁定裁决（⚠️ 编码必须遵守）

| 项 | 结论 |
|---|---|
| 涨跌口径 | **`close` 收盘价**（禁止回到 settle） |
| 反转门控 `gate_pct` | **2.0%**（验收只用池化口径，禁止调阈值迎合覆盖率） |
| 覆盖率门槛 | ≥15% 为**结构性未达**已知偏差，维持硬门槛、登记已知偏差 |
| 预测标的 | 仅主力连续合约（FG888 等） |
| 预测频率 | 每次数据更新后预测（一天三次：12:00/16:00/08:00） |
| 简报集成 | `/integrations/*` 与 `briefing_signal` 须显式声明 `caliber="close"` |

### 5.5 跨品种/大类传导

`sector_map` / `sector_index` / `transmission_weights`；先验+数据驱动（Granger/传递熵/VAR）融合；特征一律滞后防前视；权重每周重算。

---

## 6. 交易策略层（融合策略 V3.4）★ 定稿口径

> 源：`桌面/融合/终版资料/02_融合引擎与额外策略最终版` + `app/strategies/fusion_signal.py`。
> 本层与回测引擎**逐位一致（单一真源）**。线上引擎已落地 V3.4 并验收（10 项锚点偏差全 0）。
> **回归开关**：`fib_confl: false` + `add_max_lots: 1` = 逐位回到 V3.1。

### 6.1 五层结构

```
① 方向层（小时 EMA140）      只做顺势一侧（多或空）
② 入场层（小时线）           A 回踩重启(优先，须过斐波汇流) ∨ B 10根突破(补位)；全体须过 ADX(14)≥15 门控
③ 离场层（小时线·额外口径）   2×ATR 初始硬止损 + 2×ATR 吊灯 + 0.5R 保本；仅小时线收盘击穿才平仓
④ 执行层                    当根收盘成交；开仓恒 1 手；阶梯加码（至多 2 手，浮盈门槛 1.0×ATR）；
                            离场后冷却 3 根；单品种至多 1 仓
⑤ 风控层                    单笔风险封顶；保证金上限；月度熔断
```

**三条不可移除硬约束**：① 不设固定止盈（靠吊灯让利润奔跑）；② 不设结构止损；③ 移动止损只向有利方向移动，永不回撤。

### 6.2 方向层

- 小时 EMA140 方向（等价日线 EMA20）：`REF(C,1) > REF(EMA(C,140),1)` → 只做多；反之只做空；临界两侧都不做。
- 用**已收盘**小时线确认；指标取 `DIRPREV` = 上一根方向，避免未收盘值（无前视）。

### 6.3 入场层

- **A 回踩重启（优先）**：方向允许多 + `C>EMA20(H1)` + `L≤EMA20` + 上一根 `C<EMA20`（刚穿越回）。
  **斐波汇流门控**：`L`（空为 `H`）须落在「最近一个**已确认枢轴**低（高）→ 其后极值」这段"腿"的 **0.382 / 0.5 / 0.618** 回撤位 **±0.5×ATR** 内；不满足则本根回踩信号作废。
  斐波腿取枢轴用**本根做右侧确认**（`l[i-1] < l[i-2] and l[i-1] < l[i]`）、回看窗 `W=60`，天然**无前视**。
- **B 10 根突破（补位）**：方向允许多 + `C>前10根最高价(不含当根)` + `C>O`。**不受斐波汇流影响**。
- **ADX(14)≥15 全员门控**：A/B 两支都必须过；用**上一根**已收盘 ADX 判定（无前视），不满足则本根所有入场作废。
- 入场模式 `entry_mode ∈ {pullback, breakout, both, both_nm}`（默认 `both_nm`）。**两条支路必须都保留**（只做回踩净值腰斩、只做突破负期望）。

### 6.4 离场层

- 初始止损 `init_stop = entry ∓ sl_atr×ATR`（`entry` 取**加权均价**；未加码时 == 首次买入价）。
- 吊灯止损 `trail_stop = peak/trough ∓ trail_atr×ATR`。
- 保本 `be_trigger = first_px ± be_r×ATR`（默认 `be_r=0.5`，基准恒为**首次买入价**）；触发后止损上移至首次买入价。
- 当前生效止损 `cur_stop = 三者中最紧者`（多取 max / 空取 min）。
- **仅小时线收盘击穿 `cur_stop` 才平仓**（盘中插针无视）。
- ★ **「收盘判定」是本系统的 alpha 来源，不是缺陷**：任何"打到保本价即出"的机制都会被小时线插针反复踹出（实测基线会崩到 −142 万）。**保本只能由吊灯顺带承担，绝不引入新的盘中成交假设。**

### 6.5 执行层

- **close-only**：开仓价 = 开仓根收盘价；离场后冷却 `cooldown_bars=3`；单品种至多 1 仓。
- **利弗莫尔·阶梯加码**：开仓**恒为 1 手**；仅当同时满足
  ① 收盘**创入场以来新高**、② **浮盈达门槛**（`c[i] − first_px ≥ lots × add_thr_atr × ATR`，默认 `add_thr_atr=1.0`）
  才加 1 手（`add_max_lots=2`，即至多一次；门槛随手数线性抬升，是"金字塔"而非"倒金字塔"）。
  加码后 `avg_px` 更新为**加权均价**，生效止损仍是「初始止损 / 吊灯 / 0.5R 保本」三者中最紧者，**收盘判定**。
  不要求"吊灯已推进到加码后均价之上"（即**不做结构性保本**）。
- **加码的性质（须向下游如实传达）**：加码单约 53% 以亏损结束（亏损中位仅 −0.16R），但在 9 年样本上，**收到加码的交易贡献了全部净值**，未收到加码的交易合计为负。加码条件本身即强过滤器。**若不能接受"约一半加码单亏损"，应设 `add_max_lots: 1` 关闭加码。**

### 6.6 风控层

- 单笔风险封顶（1R=单笔愿亏）；保证金上限；月度熔断（亏损达阈值停新开）。
- 预热：加载后前 `min_bars=160` 根信号不可用（ATR/EMA 未收敛）。

### 6.7 策略参数表（`config/local.yaml` `fusion.*`，均可经回测面板调参）

| 参数 | 默认 | 含义 |
|---|---|---|
| `ema_k` | 140 | 方向层 EMA 周期 |
| `atr_n` | 14 | ATR 周期 |
| `ma_n` | 20 | 入场均线周期 |
| `sl_atr` | 2.0 | 初始止损倍数 |
| `trail_atr` | 2.0 | 吊灯止损倍数 |
| `be_r` | 0.5 | 保本触发倍数 |
| `W` | 60 | 分型窗口 |
| `entry_mode` | both_nm | 入场模式 |
| `cooldown_bars` | 3 | 离场后冷却 |
| `adx_n` | 14 | ADX 周期 |
| `adx_min` | 15.0 | ADX 入场门控阈值，用**上一根**判定；0=关闭。**≥20 起转亏，勿抬** |
| `use_sbull` | false | 回踩支路是否要求「收强/收弱」；true=旧行为 |
| `fib_confl` | true | 斐波那契·汇流门控（仅回踩支路） |
| `fib_ratios` | [0.382, 0.5, 0.618] | 回撤位集合 |
| `fib_tol_atr` | 0.5 | 斐波容差 = 该值 × ATR |
| `add_max_lots` | 2 | 最大手数；1=不加码 |
| `add_thr_atr` | 1.0 | 加码浮盈门槛 = `lots × 该值 × ATR`（金字塔） |
| `min_bars` | 160 | 预热根数 |
| `max_bars` | 400 | 实时读取上限 |

### 6.8 策略红线（评审必须守住）

**必守**：只做顺势一侧；全体入场过 ADX≥15；回踩支路过斐波汇流；两条支路都保留；收盘判定、不引入盘中成交假设；移动止损永不回撤；不设固定止盈。

**禁止（已证实有害或无益，不要再试，也不再展开论证）**：
加码至 3 手 · 保本地板（均价/首价） · 盘中挂单保本 · 三K动能止损 · 结构止损（枢轴） · 方向成熟度过滤 · 时间衰减出场 · 分批/固定止盈 · 突破幅度门槛 · 关键点跟随 · 时间回撤过滤 · 盘中触碰止损 · 收强条件 · 只做单支路 · 实体突破判定 · 外包/内包K线过滤 · 浅/深回调过滤 · 品种级参数优化 · 凯利动态仓位 · 组合级复利手数 · regime 参数切换 · 空仓机制 · 季节性因子 · 跨品种价差 · 跨周期配置 · 换月跳空的**策略层**守卫（改数据/执行面处理）。

> **元结论（决定优化方向）**：在 50 品种 / 9 年 / 3,226 笔样本上，**净值 91% 由 Top5 笔贡献，参数微调的 ±50 万波动全在噪声内**——**靠"加过滤器"已无法取得可信的边际改进**。优化只能走三条路：**修漏洞（数据/执行面）· 扩样本（品种池/min15）· 执行面（滑点/换月/资金规模）**。

### 6.9 方法学铁律（评估任何新条款前必读）

| # | 铁律 |
|---|---|
| 1 | **配对归因 > 重跑链路**：评估"入场过滤器"必须在**同一批已成交交易**上按标志分桶看贡献（重跑会被容量门控的时序重排污染）。 |
| 2 | **判据三件套（缺一不可）**：① 参数曲线单调/成平台（非单点最优）② 跨体系同向（≥3 个体系同号）③ 品种分半同号。 |
| 3 | **框架保真度审计**：证伪任何经典框架（利弗莫尔/斐波那契）前，先审计自己的机械化是否忠实于原始定义。 |
| 4 | **成交假设决定结论**：同一规则换"收盘成交 / 挂单成交 / 盘中挂单"可让结果正负翻转。 |
| 5 | **变手数记账**：`R` 必须是"每手 R"，手数由 `lots` 单独承载；多手变体一律以净¥与 DD 为准（PF 不可比）。 |
| 6 | **参数"不敏感"才是好事**：三档相差 ±5 万 ⇒ 不是调参调出来的；只有单点好、相邻崩 = 噪声。 |
| 7 | **适用边界**：入场过滤 → **只能**配对归因；出场类 → 两法皆可；**仓位/手数类 → 必须真重跑**（改变路径）。 |
| 8 | **多重检验诚实声明**：任何新结论必须报出**累计检验次数与校正后显著性**，回测输出默认带统计加固包（§8.7）。 |

---

## 7. 信号推送层

> 源：`app/scheduler.py`（`_fusion_scan_job` / `_collect_hourly_settled`）+ `app/notify/pushplus.py` + `app/api/routers/fusion.py`。

### 7.1 扫描调度

- 融合策略扫描**每 15 分钟**一次（与每日三次 ingest 并行），由 APScheduler `minute="*/15"` 触发。
- 每轮：① 拉最新小时线（逐根增量 / 尾部 800 根兜底 + 定稿守卫）；② `evaluate_all` 评估全部品种；③ 状态机去重（仅状态变化时推送）；④ 渲染并发送。

### 7.2 状态机去重

- 持久化 `FusionPosition`（symbol, position, entry_price, entry_at, last_stop, last_be_done, signal_at, pushed_at, **lots**）。
  `lots` = 当前手数；手数较上一轮增加即视为「加码事件」，独立推送。
- 冷启动（表空）：以引擎当前状态无条件建基线，再推送上线通知。
- 数据新鲜度：最新 K 距现在 > `stale_minutes=90` 分钟 → 抑制状态变化信号（防过期数据触发）。

### 7.3 推送形态（三段式）

1. 🚨 **新信号**：开/平/反手即时推，附 ⛔止损位 / 🎯保本位（价位按 `_TICKS` 最小变动对齐，如 FG=1、AU=0.02、SC=0.1）。
   持仓行在加码后显示 `×2手`；加码事件单列 `➕ … 加码 至 2手 ｜ 均价 … ｜ ⛔止损 …`。
2. ⚠️ **止损变动**：吊灯止损朝有利方向移动 ≥ `trail_push_atr=0.5×ATR` 补推；首次触发保本补推。
3. 📊 **持仓简报**：盘前（`pre_session_times` 08:45/13:15/20:45）强制推完整清单；交易时段内每 `heartbeat_interval_min=30` 一档（对齐 :00/:30）。
4. ⏳ **待执行复提**：开仓信号在 `signal_repeat_min=120` 分钟内每轮置顶，防漏看。

### 7.4 通道（旁路，失败不阻塞主流程）

- 主通道 **pushplus**（微信）：`POST https://www.pushplus.plus/send`，`template=markdown`，失败自动重试 1 次。
- 备用通道：通用 webhook（企微/钉钉/飞书），按 URL 域名自动选载荷格式；主通道失败降级。
- 留痕：`FusionPushLog`（pushed_at/kind/title/content/n_signals/n_rows/delivered/via），可回查历史信号。

### 7.5 推送窗口 / 日历

- `push_windows`：08:45-12:00 / 13:15-15:30 / 20:45-23:30；窗口外整轮冻结。
- 非交易日（周末 + akshare 交易日历缓存，超 7 天刷新）静默。

---

## 8. 回测与研究模块

> 源：`桌面/融合/终版资料/04_一百二十万回测方法维度步骤` + `runtime/_bt_fusion.py` / `_bt_multi.py` / `_bt_fusion_timeweight.py`。本模块把"手动调参回测 + 鲁棒性测试 + walk-forward"做成系统内功能（CB 编码、WB 复审）。

### 8.1 定位

- 用户可在 UI/API **手动调整策略与组合参数**，即时跑回测，对比不同参数下的净¥/PF/回撤/胜率。
- 内置 **鲁棒性测试**（四维矩阵 × 多 seed）与 **walk-forward 测试**（滚动窗口重估），验证策略稳健性。

### 8.2 两层回测架构（与 `fusion_state_detail` 单一真源）

- **信号层（逐品种前向循环）**：逻辑与 `fusion_signal.fusion_state_detail` **逐位一致**（EMA140 方向 → ADX(14)≥15 门控 → MA20 回踩（含斐波汇流）/10 根突破 → 2×ATR 止损/吊灯/0.5R 保本 → 阶梯加码 → close-only）。输出每笔 `(entry_i, entry_px, entry_atr, dir, exit_i, exit_px, R, lots, first_px)`。
- **组合层（时间轴 FIFO 重放）**：把所有品种成交事件按 `entry_dt/exit_dt` 排序重放；闸门 `max_positions`（默认 5）；各品种手数 `lots ∈ {1, 2}`；净金额 `Y = R×lots×entry_atr×multiplier − 往返成本(bp)`（`R` 为**每手 R**）。
- **铁律**：引擎 `fusion_signal` 与回测 `bt_symbol` 必须同步改，改策略后两处都改并跑回测确认口径未分叉。

### 8.3 四维测试矩阵（鲁棒性）

| 维度 | 取值 |
|---|---|
| ① 数据源 `src` | tqsdk / akshare / csv |
| ② 容量 `max_pos` | 1 / 2 / 3 / 5 仓 |
| ③ 成本 `cost_bp` | 5 / 10 / 20 / 30 / 50 bp |
| ④ 时段权重 | 不加权(p=1) / 加权(tqsdk 分时段期望) |

交叉组合最多 120 组，UI 可勾选批量跑。

### 8.4 可调参数面板

| 类别 | 参数 |
|---|---|
| 策略 | `sl_atr` / `trail_atr` / `be_r` / `entry_mode` / `W` / `ema_k` / `atr_n` / `ma_n` / `cooldown_bars` / `adx_min` / `fib_confl` / `fib_tol_atr` / `add_thr_atr` / `add_max_lots` |
| 组合 | `max_positions` / `lots` / `cost_bp` / `seeds` |
| 数据 | `src` / `freq` / `kind` / `symbols` / `min_year` / `limit` |
| 加权 | `N_MIN` / 概率映射 |

### 8.5 walk-forward 设计

- **信号层**：按时间切滚动窗口（训练/评估不泄漏），定期用滚动窗口重估参数（与 §4.3.4 对齐）。
- **组合层**：滚动 FIFO 重放，输出分年度/分时段净值曲线。
- 机制性消除前视：评估点 `step` 抽样、参数估计只用 `as_of` 之前数据。
- **OOS 判据**：报告 OOS 衰减比（OOS 净利 / IS 净利）；衰减比 ≥50% 维持 V3.4 定稿，深度衰减回退 P0（`fib_confl:true + add_max_lots:1`）。

### 8.6 鲁棒性测试

- 多 `seed`（`seeds=30~60`）取均值±标准差，检验随机性敏感。
- 分年度 / 分品种拆解；成本盈亏平衡点扫描。
- 品种池清单 + 各品种上市时间 + **幸存者偏差审计**结论（已退市品种处理方式）。

### 8.7 指标与输出

- 均 R、净 R、净 ¥、盈利因子 PF、胜率、最大回撤（R 与 ¥ 双口径）、覆盖率-准确率曲线。
- **统计加固包（默认输出）**：品种层 Bootstrap 95%CI 与 p(≤0)、置换检验 p 值、**多重检验声明**（累计检验次数 + Bonferroni/FDR 或 expected-max 校正）、CVaR(5%)、最长回撤时长。
- **基准对照行**：每份报告必含 **买入持有(B&H)** 与 **MA 交叉** 两条基准行（同成本口径、同品种池）。
- **容量梯度**：`max_pos ∈ {3, 5, 7, 10}` 一键扫描。
- **成本韧性**：成本 ×1.5 / ×2 对照 + 波动率依赖滑点模型。
- **VWAP 对照**：收盘成交 vs VWAP 成交双口径。
- 每次回测 JSON 落 `bt_out/`，命名带 `tag`；**必须标注口径**：所用 `(freq,kind,src)`、品种窗口、成本——不同口径结论可差 5~30 倍。

### 8.8 API / 前端

- `POST /backtest/run`：提交参数（8.4 面板），返回 `task_id`。
- `GET /backtest/result/{id}`：结果（汇总 + 分品种 + 分年度 + 曲线）。
- `GET /backtest/matrix`：四维矩阵对比表。
- 前端「回测调参」页：参数表单 + 运行 + 结果对比表/图。

---

## 9. 服务层 API

| 方法 | 路径 | 说明 | 归属 |
|---|---|---|---|
| GET | /symbols | 品种列表 | 共用 |
| GET | /bars/{symbol}?freq= | 行情查询（daily/hourly） | 数据 |
| POST | /ingest | 手动更新（指定品种/全量） | 数据 |
| GET/POST | /integrations/* | 简报引擎对接 | 预测 |
| POST | /predict | /predict/batch | 预测 | 预测 |
| POST | /backtest/run | GET /backtest/result/{id} | /backtest/matrix | 回测 | 回测 |
| GET | /signals/latest | /signals/history | 当前/历史融合信号 | 信号 |
| GET | /anomalies | /anomalies/{id}/resolve | 异常工单 | 数据 |
| GET | /tasks/{id} | 任务状态 | 共用 |

---

## 10. 展示层（看板）

React 18 + Vite + TS + AntD + ECharts，涨跌配色**涨红跌绿**。

- **品种总览**：最新价、N 日涨跌、次日预测方向。
- **单品种预测详情**：K 线 + 方向概率/幅度区间/参与模型/特征/三次预测对比。
- **全市场热力图**：涨跌概率分布 + 大类强弱排名 + 传导来源卡片。
- **信号面板**：实时持仓（多红/空绿）、开/平/反手记录、止损/保本位、推送日志。
- **回测调参面板**：参数表单 + 运行 + 结果对比表/曲线。
- **数据质量面板**：异常工单 + 裁决 + 校准日志 + 数据完整性自检。

---

## 11. 部署与运维（本地 Docker）

- 三容器：`timescaledb` + `python-api(含 scheduler)` + `web(nginx)`。
- 配置：token/更新时刻/校准阈值/模型权重 → `config/{local,cloud}.yaml` + `.env`（密钥不进 git）。
- 资源：日常 4 核/8GB/50GB SSD；深度重训 8 核/16GB。
- 启动前提：Docker Desktop 运行（手动开）。
- **不入库**：`.env` / `config/local.yaml` / `pgdata/` / `runtime/` / 根目录临时文件（`.gitignore`）。

---

## 12. 里程碑

| 阶段 | 内容 | 归属 |
|---|---|---|
| M1 | 数据层 + 日线获取与更新（每日三次） | CB |
| M2 | 特征 + 单模型预测 | CB |
| M3 | 全模型库 + 集成 + 传导 | CB |
| M4 | 回测引擎（walk-forward 机制） | CB |
| M5 | React 看板 + 简报对接 | CB |
| M6 | 小时线扩展 + 因子（库存/龙虎榜/carry） | CB |
| **M6x** | **交易策略融合引擎 + 信号推送 + 小时线逐根增量更新（§4.3.2/§6/§7）** | **WB** |
| **M8** | **回测与研究模块 UI/API（§8）** | **WB+CB** |
| M7 | 云端部署演进 | 共用 |

> 🔴 **两套 M 编号并存，提 M8 前先确认体系（2026-09-22 起）**：
> 本表是 **CB/主 PRD 编号**，其中 **M8 = 回测与研究模块 UI/API（§8）**，状态**未实现**（见 `代码走查与功能验证报告.md` G1）。
> 另有 **WB 框架编号**（`国内期货多智能体框架设计.md` §六）的 **M8 = 决策链路整体迁入 Docker**，其 PRD 为 **`docs/M8_决策链路容器化_PRD_20260922.md`**（2026-09-22 立项，用户已裁定：容器化优先、前端不交付）。
> 三者互不相同， discussion 时请显式指明是哪一套。

---

## 13. 待裁定 / 已知问题

| # | 事项 | 状态 | 归属 |
|---|---|---|---|
| 4.1 | 复权 OI 门槛：低流动性品种（sc/ru/sn/pb）如何复权 | 高 | CB 主+WB |
| 4.2 | 复权是否接入自动调度 | 高 | 共担 |
| 4.5 | 陈旧 cont_adj 全量重跑（57 品种） | 中 | CB |
| K1 | 龙虎榜 SHFE/DCE 接口坏 | Open | CB |
| K4 | INE 无持仓排名/库存接口（硬上限 ~68/73） | Open | CB |
| K6 | 反转覆盖率 ≥15% 结构性未达（已知偏差） | Accepted | CB |
| 6.4 | **补全 `min15` 数据**：库内仅 FG 有 15 分钟线，需 `fetch_fdf.py --freq min15` 补全 | 中 | CB |
| 6.5 | **扩样本**（品种池 / 更长历史）：净值 91% 由 Top5 笔贡献，扩样本是唯一能根本性提高结论可信度的方向 | 中 | 共担 |
| 6.7 | **日线简报引擎（`imports/SX`）V2.0→V3.4 对齐**：简报侧仍为 V2.0 底座 + 旧加码，与 §6 定稿存在三处漂移（ADX 门控缺失 / 斐波汇流缺失 / 加码门槛 0.5→1.0、腿数 3→2）；详见 §16 | 高 | CB |
| 6.8 | **walk-forward IS 基线问题**：滚动 OOS/IS 衰减比 103.7% 表面达标，但单次无重叠切分显示 IS(2018-06~2023-01) 笔均仅 ¥18（≈打平）、OOS +67.0万 ⇒ 收益高度依赖 2022 后市场状态，**判据前提（IS 为可信正基线）不成立**，待用户裁定 | 待裁定 | 用户 |
| 6.9 | **换月跳空**：策略层守卫已证伪，改为**数据/执行面修复**（真实合约拼接 vs 移仓按新旧价差结算），先出方案经用户裁定 | 高 | CB |

---

## 14. WorkBuddy 复审要点（CB 交付后复审，任一不符即退回）

1. **单一真源**：`fusion_signal.fusion_state_detail` 与回测 `bt_symbol` 逻辑逐位一致；改其一必须同步另一并跑回测。
2. **口径铁律**：信号只读 `hourly_bar` 单一 `src`；回测只读 `fut_kline.continuous`；无双源混读；涨跌 `close` 口径。
3. **小时线逐根增量**：§4.3.2 的"完成一根更新一根"已实现且带定稿守卫；既有批量兜底不冲突。
4. **定稿守卫**：最新 K < `settle_delay_sec=150s` 时等待重采覆盖。
5. **推送窗口/日历**：非交易日/非窗口整轮冻结；价位按 `_TICKS` 对齐。
6. **回测可复现**：参数面板改动可一键复跑；输出带 `(freq,kind,src)` 口径标注；四维矩阵与多 seed 可用；统计加固包/基准对照/容量梯度齐备。
7. **策略红线**：§6.8「必守」全满足、「禁止」清单无误用。
8. **不入库**：§11 所列路径均被 `.gitignore` 排除。
9. **git 基线**：双方改动已提交 baseline，分支开发，无互相覆盖。

---

## 15. CB 交付清单（当前未完成项）

> 已完成并验收的项（V3.4 引擎落地 + 等价性验证、同花顺/文华 V34 公式、walk-forward 脚本、统计加固包、**#1 日线简报引擎 V3.4 对齐**）不再列此；下表保留已关闭项的结论备查。

| # | 事项 | 要求 / 验收标准 | 优先级 |
|---|---|---|---|
| 1 | **日线简报引擎 V2.0→V3.4 对齐** | 见 §16 三处漂移逐项落地；含无前视审计 + 门槛参数与线上引擎逐项对账 | ★★★ → **✅ 已交付并验收（2026-09-22）**：`_fut_daily_brief.py`（ADX_GATE=15/FIB_CONFL/W=60/0.382·0.5·0.618/±0.5×ATR）+ `_fut_positions.py`（ADDON_MIN_PROFIT_ATR 0.5→1.0、ADDON_MAX_LEGS 3→2、条件④改「收盘创入场以来新高/新低」，删「回踩 EMA10 收回」）。**验收脚本 `桌面/融合/_brief_v34_accept.py` → ALL PASS**：门槛参数 8 项与 `FusionConfig` 逐项对账全一致；ADX 门控（adx_prev<15 全观望、adx_prev≥15 逐根回归一致、信号只减不增 208→154）；斐波（单元腿 100→200 通过/170 作废、**无前视审计 3 品种 ×359 根 0 不一致**、B 支路逐一相同、拦截率 60–64%）；加仓 9 情形全中。两副本（技能目录 + `imports/SX`）已同步一致 | **已关闭** |
| 2 | **换月跳空数据/执行面修复方案** | 出方案文档（真实合约拼接 vs 移仓按价差结算对比），经用户裁定后实施 | ★★ → **✅ 已交付并落地（2026-09-22）**：方案见桌面 `融合策略_换月跳空数据执行面方案_20260922.md`。**用户裁定 = 路线 A（维持 continuous）+ 仅 A1/A3**；A1 已强化进 `融合策略V2.0_实盘操作手册.md` R5（换月持仓跨越用新合约 ATR 重算手数）；A3 固化为 `_fusion_roll_gap_stress.py`（季度压测，基线净利漂移 **−1.67% = PASS**）；路线 B（复权）/A2（入场回避）**实测均更差，已否决** | 
| 3 | **补 min15 数据** | `fetch_fdf.py --freq min15` 全品种补全 | ★★ |
| 4 | **扩样本** | 品种池扩容 + 更长历史；报告静态池 vs 动态池对照 | ★★ |
| 5 | **动态品种池健康度评分** | 月度评分 = 流动性 + 波动率（ATR 百分位）+ 趋势强度（ADX/斜率），取前 30–40；信号逻辑不变做对照 | ★★ → **❌ 已实测证伪并独立复核（2026-09-22）**：Top30 = +30.7万/DD 27.0万/MAR **1.14**；Top40 = +24.3万/DD 25.7万/MAR **0.95**（基线 +69.6万/MAR 4.14）——**净利腰斩 + DD 反升 + MAR 崩至 ~1**，轮换剔除的恰是利润来源（与「Top5 笔 91%」元发现同构）。与 §17 #27 区分：#27「筛选放**品种池构成**」仍成立，被证伪的是**动态轮换机制**本身 | **已关闭（证伪）** |
| 6 | **相关性 / 板块暴露约束（组合层）** | 60 日滚动相关矩阵；新仓与现有持仓相关 >0.7 降仓或跳过；单板块敞口上限；**必须真重跑**（铁律 #7），报容量 5 门控重排影响 | ★★ → **❌ 已实测证伪并独立复核（2026-09-22）**：见桌面 `融合策略_V3.8复核报告_20260922.md`。**相关性约束**：阈值 0.5→0.8 扫描净利呈 +29.5/+38.2/+66.4/**−6.4**/+1.6/+36.9万，MAR 剧烈跳变（1.05→3.60→**−0.19**→1.63），**无参数平台 = 噪声**（判据三件套第 1 条不过）。**板块约束**：⚠️ 新增警告——`sec_cap=3` 表面 MAR **5.03 > 基线 4.14**，但分半检验**两半反向**（前半 Δ+9.39万 / 后半 Δ−1.29万）、参数尖峰（0.97/3.71/5.03/4.17/4.14），**禁止引用**。基准逐位复现 ✅（+69.6万/MAR 4.14） | **已关闭（证伪）** |
| 7 | **资金管理研究包** | ① 波动率平价 vs 等权 vs 夏普加权三档；② 回撤阶梯降仓 + 恢复阶梯；变手数按铁律 #5 记账、报成交假设（铁律 #6）；输出"DD 改善 vs 收益侵蚀"对照与分年表 | ★★ → **❌ 已实测证伪（V3.8 #45/#46，2026-09-22 复核维持）**：波动率平价三档**全口径转负**（−43.0/−53.1/−54.8万）——等风险把手数配给低波品种，而利润集中在高波大趋势（SC/AU/AG 族）；回撤阶梯 A 净利 −79%/DD −62%/**MAR 反降**（2.29 < 4.14），"用回撤换收益"不成立 | **已关闭（证伪）** |
| 8 | **边界单元测试** | pytest 覆盖：加码×止损/吊灯/保本位联动边界、入场当日即触发离场、ATR 极值、跨换月跳空开盘 | ★ |
| 9 | **复权遗留**（§13 #4.1/#4.2/#4.5） | 低流动性品种复权口径 + 接入调度 + 陈旧 cont_adj 全量重跑 | 高 → **⚠️ 与 §15 #2 结论需协调**：换月跳空审计已证实 `cont_adj`（复权口径）回测**更差**（net −18%、DD +52%）且违背 V3.0 数据源裁定；本项应降级为**数据完整性维护**（保证 `cont_adj` 表可用、供交叉验证），**不得**作为回测基准替换 `continuous` |
| 10 | **成本韧性并入统计加固包输出**（采纳#8，V3.8 #14） | 每份回测报告附加输出「成本韧性」段：real 口径 ×1.0/×1.5/×2.0 跳 + flat 口径对照，**必须注明成本口径** | ★ → **✅ 已采纳（2026-09-22 复核逐位复现）**：real ×2 = **75%**（+51.9万）、×1.5 = 87%（+60.7万）→ alpha 对真实成本稳健；flat 口径 ×1.5 即归零（**口径不同结论不同**，铁律 #6 又一例） |

---

## 16. 日线简报引擎 V3.4 对齐（技术债清偿）

> **性质**：不是新策略研究，是让「每日简报跑出来的信号」与 §6 定稿的 V3.4 口径**逐条一致**。
> **漂移成因**：策略实现有两套独立代码 —— ① 线上引擎 `app/strategies/fusion_signal.py`（小时线，已验收 V3.4）；② 每日简报流水线 `imports/SX/_fut_daily_brief.py::fusion_signal()`（日线，**未跟随 V3.1/V3.2/V3.4 升级**）。
> 两者的「小时 EMA140/EMA20 → 日线 EMA20/EMA10」周期折叠是既定设计，**不在改动范围**；本次只补与周期无关的门槛口径。

### 16.1 三处漂移

| # | 漂移项 | 定稿口径（§6） | 简报现状 | 优先级 |
|---|---|---|---|---|
| ① | **ADX(14)≥15 门控** | `adx_min=15`，用**上一根**判定，A/B 两支都拦 | **完全缺失** | P0 |
| ② | **斐波那契·汇流门控** | `fib_confl=true`，0.382/0.5/0.618 ±0.5×ATR，`W=60`，右侧确认，**仅约束 A 支路** | **完全缺失** | P0 |
| ③ | **阶梯加码口径** | 浮盈 ≥ `add_thr_atr=1.0`×ATR 且**收盘创入场以来新高** → +1 手，至多 2 手 | 旧规则：门槛 **0.5×ATR** + 回踩 EMA10 收回；腿数 **3** | P1 |

### 16.2 工作项 ①：补 ADX(14)≥15 门控

**文件**：`imports/SX/_fut_daily_brief.py` ｜ **锚点**：`fusion_signal(met)`（函数起始行 279），入场判定 `do_long / do_short`（316–317 行）之前插闸。

1. 新增常量（勿复用 `ADX_THR=22`——那项只服务遗留 `_engine_signal` 路径与「趋势」列标注）：
   ```python
   ADX_GATE = 15.0        # V3.1：融合入场硬门槛（对齐 config fusion.adx_min）
   ```
2. 语义（与线上实现逐位一致）：用**上一根**已收盘 ADX 判定、无前视；`adx_v < ADX_GATE` → **A、B 两支入口全部作废**；拦截时 `reason` 追加 `ADX={adx_v:.0f}<15 震荡市，扣扳机作废`、`action="观望"`。**不得**因此改动方向层、止损倍数或持仓台账既有逻辑。
3. `met` 里 `adx` 已由 `_analyze_one`（447–448 行）算好（489 行写入），**无需新增取数**。

**验收**：构造 `adx<15` 样本根 → 入场必须为「观望」；同品种同根在 `adx≥15` 时结果与改动前**完全一致**；全量 49 品种信号总数**只减不增**。

### 16.3 工作项 ②：补斐波那契·汇流门控（最大工作量）

**为什么不能就地加**：`fusion_signal(met)` 只拿到**末根**标量与极值，**没有 OHLC 序列**，算不出「枢轴 → 其后极值」的斐波腿。必须在持有 `df` 的调用方先算好、塞进 `met`。

1. **新增函数** `fib_confluence(df, direction, atr_v, W=60, ratios=(0.382,0.5,0.618), tol_atr=0.5)`，位置建议紧邻 `fusion_signal` 之前（278 行前）。返回 `dict(ok, pivot, levels, hit, leg_hi, leg_lo)`。
2. **算法（严格对齐 §6.3，无前视）**：只对**A 回踩支路**生效（B 突破不受影响）；多头腿在 `W=60` 窗内取「最近一个**已确认枢轴低** → 该枢轴后最高价」，枢轴须用**本根右侧确认**（`l[i-1] < l[i-2] and l[i-1] < l[i]`；空头镜像 `h[i-1] > h[i-2] and h[i-1] > h[i]`）；回撤位 = `pivot + r × (leg_hi − pivot)`；**本根最低价 `l`**（空为 `h`）落在任一档 `± tol_atr × ATR` 内 → `ok=True`，否则本根 A 信号作废。**严禁使用未来数据**。
3. **调用方**：`_analyze_one`（442 行拿到 `df`）构造 `met`（485–492 行）时加入 `fib_ok / fib_levels / fib_pivot`。
4. **消费**：仅修改 `a_long / a_short`（311–312 行）追加 `and met.get("fib_ok", True)`；`b_long / b_short`（314–315 行）**保持不动**。
5. **可回退**：加常量 `FIB_CONFL = True`（对齐 `fusion.fib_confl`）；置 `False` 时跳过斐波判定。
6. 参数名与默认值必须与 §6.7 一致：`W=60` / `[0.382,0.5,0.618]` / `fib_tol_atr=0.5`。

**验收**：
- **单元级**：合成已知腿（枢轴低 100 → 高 200，0.5 档 = 150），回踩至 150 附近应通过、回踩至 170 应作废。
- **无前视审计**：序列截断到 `i-1` 重算，`fib_ok` 与完整序列在 `i-1` 处**必须一致**（铁律 #3 强制项）。
- **真实数据**：`FG`/`RB`/`SA` 跑近 400 根，输出「斐波拦截根数 / 总 A 信号根数」；**若拦截率 > 70% 或把某品种 A 支路全部拦死，须报告并用 `tol_atr ∈ {0.5, 0.75}` 与 `W ∈ {60, 80}` 做敏感性对照**，以拦截面与信号数的平衡为准，勿凭空放宽。
  **（2026-09-22 已扩到全 49 品种执行**：拦截率中位 63.6%、**16/49 品种 > 70% → 已触发并完成敏感性对照**；结论见 §16.7 与桌面《融合策略_斐波门控核查与优化评估_20260922.md》）
- **B 支路不受影响**：改动前后 B 信号根**逐一相同**。

### 16.4 工作项 ③：P3 加仓口径 → 阶梯加码

**文件**：`imports/SX/_fut_positions.py` ｜ **锚点**：常量 283–286 行；判定体 `addon_suggestions()` 755–782 行。

1. 常量对齐 §6.7：
   ```python
   ADDON_MIN_PROFIT_ATR = 1.0    # 原 0.5 → 1.0（= fusion.add_thr_atr）
   ADDON_MAX_LEGS       = 2      # 原 3 → 2（= fusion.add_max_lots）
   ```
   `ADDON_MARGIN_CAP=0.40`、`ADDON_LOTS=1` 不变。
2. **替换条件 ④**：删除「回踩 EMA10 后收回」（763–769 行），改为 **① 浮盈 ≥ `ADDON_MIN_PROFIT_ATR × ATR`（保留）② 本根收盘创入场以来新高/新低（新增）**——两条**同时**满足（对应 §6.5）。数据来源：由台账「开仓日期」起，从当日 `df`/`tech` 序列取该区间最高收盘比对；若 `tech` 未带序列，允许 `addon_suggestions` 增加入参（如 `df_map`/`seq_map`）承载。**取不到时不得猜测放行**，保守返回不触发并在说明里标注。条件 ③（方向层顺势）保留。
3. **不得**引入「吊灯已推进到加码后均价之上」（结构性保本已停用）。
4. 文案同步：`cond` 组装（778–782 行）与 `addon_md_table()`（796–804 行）中 `浮盈 ≥ 0.5×ATR` → `≥ 1.0×ATR`、`腿数 {legs}/3` → `/2`；`_fut_daily_brief.py` 内相关硬编码文案一并同步。

**验收**：合成台账（沿用 `runtime/_t_render_all.py` 合成持仓法）——**浮盈 0.8×ATR 且收盘创新高 → 不触发**（旧规则会触发，此差异即判定点）；**浮盈 1.2×ATR 且创新高 → 触发 +1 手**；**浮盈 1.2×ATR 但未创新高 → 不触发**；亏损腿/逆势腿一律不触发；已有 2 手时不再触发。

### 16.5 工作项 ④：对账 + 回归（收口）

1. **对账（不可省）**：改造后取**同一批日线样本**，将简报 `fusion_signal()` 的入场/止损/加码输出与线上引擎逐项比对（允许周期折叠带来的频率差异，但**门槛类参数必须一致**：`adx_min`/`fib_ratios`/`fib_tol_atr`/`add_thr_atr`/`add_max_lots`/`sl_atr`），产出差异清单（日期、品种、分歧点、判定归属）。
2. **回归**：跑 `C:/Users/Seven/.workbuddy/skills/futures-daily-brief-pipeline/scripts/run_pipeline.py --all`，确认 49/49 抓取成功、简报七章齐全、无报错。
3. **文档解锁**（**归 WB**）：验收后移除四处「日线实现待对齐 V3.4」标注 —— `skills/Qi Analisy/SKILL.md`、`skills/futures-daily-brief-pipeline/SKILL.md`、`国内期货多智能体框架设计.md`、项目记忆 `MEMORY.md`。

### 16.6 边界

- 本 §16 只覆盖「日线简报实现向 V3.4 靠拢」。**不改**：方向层周期折叠设计、离场倍数（2.0/2.0/0.5）、锁仓机制、时间止损、护栏、账户参数。
- **不得顺手"优化"**：§6.8 禁止清单 + 元结论适用 —— 本次是**追平既有定稿**，不是加新过滤器。若需放宽 `tol_atr`/`W`，必须按 §6.9 铁律先立敏感性对照、报数据，不得直接改默认值。
- **单一真源提醒**：`app/strategies/fusion_signal.py` 仍是策略逻辑唯一真源；本 §16 是让日线侧与其一致，不是新立一套口径。

### 16.7 交付记录（2026-09-22，WB）

| 工作项 | 落地内容 | 文件锚点 |
|---|---|---|
| ① ADX 门控 | 新增 `ADX_GATE = 15.0`；`analyze()` 补 `adx_prev = adxv.iloc[-2]`（= 线上 `adx_arr[i-1]` 同义）；`fusion_signal()` 在 `do_long/do_short` 前插闸，`adx_prev < 15` 时 A/B 全废、`reason` 追加 `ADX=..<15 震荡市，扣扳机作废`、`action=观望` | `_fut_daily_brief.py` L61 / L565 / L421-427 / L471 |
| ② 斐波汇流 | 新增 `FIB_CONFL/FIB_W/FIB_RATIOS/FIB_TOL_ATR` + `_fib_eval()`（枢轴右侧确认、窗口 W、腿=pivot→其后极值、档位=leg_hi−r×leg）+ `fib_confluence()`；`analyze()` 末根判定写入 `met[fib_ok/fib_pivot/fib_levels/fib_hit/...]`；**仅 `a_long/a_short` 追加 `fib_ok`**，`b_*` 不动 | `_fut_daily_brief.py` L62-65 / L292 / L342 / L618-627 |
| ③ 阶梯加码 | `ADDON_MIN_PROFIT_ATR 0.5→1.0`、`ADDON_MAX_LEGS 3→2`；新增 `_addon_new_extreme()`，条件④由「回踩 EMA10 收回」改为 **`c[i] > max(h[买入口期..i-1])`（空头镜像 `min(l)`）**；序列缺失/窗口未覆盖 → 保守不触发；`cond` 与 `addon_md_table` 文案同步 | `_fut_positions.py` L283-284 / L704 / L791-793 / L803-808 / L830 |
| 序列承载 | `analyze()` 内嵌 `met[seq_dates/seq_high/seq_low]`（近 120 根调整后序列）→ 加仓判定可在 **skip-fetch 复用路径**下同样生效（JSON 可序列化，实证复现一致） | `_fut_daily_brief.py` L628-633 |
| 版本标注 | 简报文案「融合策略 V2.0」→「V3.4」（5 处） | `_fut_daily_brief.py` |

**验收（`桌面/融合/_brief_v34_accept.py`，真实日线源 `回测验证/_bt_cache/{FG,RB,SA}.csv`）→ ALL PASS ✅**

| 项 | 结果 |
|---|---|
| 门槛参数对账（§16.5.1） | 8 项与 `FusionConfig` **逐项一致**（adx_min/fib_confl/fib_tol_atr/add_thr_atr/add_max_lots/sl_atr/W/fib_ratios） |
| ① ADX | `adx_prev<15` **全部观望** ✅；`adx_prev≥15` 逐根与「未合 ADX」口径**完全一致** ✅；三品种可操作 **208 → 154（只减不增）** ✅ |
| ② 单元级 | 合成腿（枢轴 100 → 极值 200）：末根 150 **通过**、170 **作废**（多空双侧均符） ✅ |
| ② 无前视审计 | FG/RB/SA × 359 根，截断重算 vs 完整序列 **0 不一致** ✅ |
| ② B 支路 | 改动前后 B 信号根 **逐一相同**（58/55/55） ✅ |
| ② 真实拦截率（原 3 品种） | FG 62.5% / RB 64.3% / SA 60.0% ✅ |
| ② **拦截率全品种补测**（2026-09-22 补，49 品种 × 400 根日线） | 合计 **65.9%**（A 481→拦 317）、**中位 63.6%**、P75 75.0%、**P90 81.1%、最大 92.3%（A）**；**16/49 品种 > 70% → 触发并完成 §16.3 敏感性对照**：tol 0.5→0.75→1.0→1.5 拦截率 **65.9→58.6→55.1→51.1%**（单调放宽）、**W=80/120 完全无变化**、仅 0.5 档 74.2%、五档 58.0%；**A 支路 0/49 被拦死**；B 支路不受影响 ✅ ｜ 口径保真：后处理 vs 真实 analyze **A 支路逐根 0 差异**（B 差异 1 根＝"A 被拦后 stage 回落 B"标注效应） |
| ③ 加仓 | 9 情形全中：0.8×ATR+创新高**不触发**（判定点）· 1.2×ATR+创新高**触发**· 1.2×ATR 未创新高**不触发**· 亏损腿/逆势腿/已有 2 手**不触发**· 空头镜像触发· 序列缺失/窗口未覆盖**保守不触发** ✅ |

> **口径说明（须向下游传达）**：条件④「创入场以来新高」按 §6.5 + 引擎实现取 **最高价**（`max(h[entry_i:i])`），
> 而非 §16.4 原文措辞「最高收盘」——以引擎为准（bt33 与线上 `fusion_signal` 完全同式）。加仓保证金护栏仍按简报台账口径。
> **尚存的语言性差异**（非漂移）：简报加仓 ① 不按 `lots` 线性抬升门槛（PRD §16.4 第 2 条明确「保留」）；
> 周期折叠（小时 EMA140/20 → 日线 EMA20/10）为既定设计，不在本次范围。

**文档解锁（§16.5.3）已完成**：`Qi Analisy/SKILL.md`、`futures-daily-brief-pipeline/SKILL.md` 的「待对齐 V3.4」标注已移除；
`国内期货多智能体框架设计.md` 已不存在；项目记忆 `MEMORY.md` 已同步。
**双副本**：技能目录 `scripts/pipeline/` 与 `E:/Docker/qhyc/imports/SX/` 已 `cp` 同步并 `diff` 校验一致。

### 16.8 斐波门控「作用与优化」评估（2026-09-22 补测，真重跑）

> **触发**：全品种拦截率补测显示 **16/49 品种超 70%**，§16.3 条款要求做敏感性对照并出结论；用户亦提出"斐波拦截率偏高、某日信号归零"。
> 脚本：`_fib_blockrate_audit.py`（49 品种拦截率/归因/日横截面）· `_fib_optimize_backtest.py`（关开对照）· `_fib_tolgrid_backtest.py`（参数密集网格）。

**① 斐波的作用（同口径关/开真重跑，容量5 + 真实 1 跳成本）**

| 口径 | 事件 | 成交 | 净¥ | **DD** | **MAR** |
|---|---|---|---|---|---|
| 关斐波（加码2手·1.0R） | 18,009 | 3,340 | **+79.8万** | **28.6万** | **2.79** |
| 开斐波（= V3.4 锚点） | 16,400 | 3,226 | +69.6万 | **16.8万** | **4.14** |

→ **斐波 = 用 12.9% 的净利，换 41.2% 的回撤削减（MAR +48%）。它是风险调节器，不是收益增强器**：拦掉无结构的"随机/浅回踩"入场，压住连续小亏单。

**② 信号损失归因（日线 49 品种横截面）**
底座 2,977（A 537 / B 2,440）→ +ADX 2,583（A 481 / B 2,102）→ +斐波 2,266（A 164 / B 2,102）。
损失 711 中 **ADX 占 55.4%、斐波占 44.6%**；且斐波**只作用于 A 支路**（占信号 18%），**B 突破支路（82%）完全不受影响**。
→ ⚠️ **"信号变少"的第一主因是 ADX 门控（作用于 A+B），不是斐波**（须纠正直觉）。

**③ 能否优化：不能（三重证据，判据三件套全不过）**
- **tol_atr（0.3–2.0，共 12 点，真重跑）**：净利在 **58.8–82.7 万**间剧烈跳动、MAR 3.06–5.34，**无参数平台**；唯一"最优" tol=1.0（+82.7万/MAR5.34）**分半不同号**（前半 **−2.2万** / 后半 +84.9万）且被 0.90（+77.7）与 1.20（+66.7）双向夹击；
- **W（40/60/80/100/120/180）**：**六档完全恒等**（16400 / +69.6万 / 4.14）→ W 非有效抓手（最近枢轴总在 40 根内）；
- **ratios**：**仅 0.5 档 → +24.2万 / MAR 0.86（灾难）**；两档 ≈ 三档（仅差 1 事件）；五档（含 0.236/0.786）→ 净利 +81.9万 但 DD 由 16.8万 升至 19.0万（噪声级）。

→ **判定：维持 `fib_confl=true` / `fib_tol_atr=0.5` / `W=60` / `fib_ratios=[0.382,0.5,0.618]` 现状（局部最优，不得放宽）。**
→ 元发现再确认：**在"利润集中于少数大波动趋势"的结构下，过滤层参数微调无可信边际。**

**④ "某日信号归零"机制**：斐波清零日实测 **6/358 交易日（1.7%）**；最新交易日横截面 底座 49 → +ADX 40 → +斐波 36（**A 支路 8→1**）。属**设计内行为**（该日无合格回踩结构），B 支路信号可正常使用。

> **§16.8 补充（2026-09-22，WB）**：`W` 六档恒等的**根因已查明** —— 见 §16.9。
> `W` 管的是「窗口内找不到枢轴」（实测 0 例），真瓶颈在**枢轴的年龄阈值 `(i-piv) >= 2`**（实测占「无有效腿」100%）。
> 即「**找到了但不让用**」，故调 `W` 必然无效。§16.8 的「不得放宽」结论因此获得**新的、更强的支持**（§16.9 实测同向）。

---

### 16.9 斐波腿定义放宽（`(i-piv)` 阈值 + 幅度定义腿）评估（2026-09-22，WB）

> **触发**：用户提出「按**枢轴数或幅度**重新定义腿」，要求评估放宽 `(i-piv) < 2` 的影响。
> **脚本**：`_piv_gap_probe.py`（成因分解）· `_fib_relax_backtest.py`（base/relax 双跑）· `_fib_relax_r.py`（R 口径复核，均在 `E:/QH/期货简报/过程思考/`）。
> **底稿**：`过程思考/PRD16_斐波腿定义放宽评估_20260922.md`。
> 🔴 **处置（2026-09-22 晚，用户决定）**：**选方案 B —— 已回退，维持现状**（技能侧 `FIB_MIN_GAP=2` / `FIB_K_ATR=0`）。
> 回退后回归：`_recon_engine.py` 676 组合 → 与线上实现**逐位一致**（不一致 0）。**qhyc 侧自始至终一行未改。**

> ⚠️ **口径对账（用户问「盈亏比 4:1 / 胜率 22%」为何与此处 2.12 / 31.6% 不同）**：
> 两者**都真，但不是同一件事**。**生产口径**（50 品种 / 9 年 / **日线**，16,400 事件 / 3,226 成交）是
> **胜率 23.5% / 盈亏比 ≈3.96（R 口径）/ MAR 4.14**（`Qi Analisy/SKILL.md` L11、§16.8①）——
> 这正是用户记忆中的「4:1 / 22%」，**正确**。本 § 表里的 **31.55% / 2.120** 来自
> **55 品种小时线 + `both_nm`** 研究平台（25,813 笔）：持有期更短、分母更大 → 高换手 →
> **胜率更高、赔率更低**（正合 `futures-backtest-merge` §13.3「高换手趋势系统必然低胜率+高赔率」）。
> 另注意「4:1」在本项目另有含义：`PRD16_斐波敏感性对照` 里的 **4:1** 指
> 「无有效腿 : 找到腿但不落档 = 50.7% : 12.7%」，**与盈亏无关**。
> 👉 **本 § 数据只作 base↔relax 相对比较，不得替换生产指标。** 详见底稿 §八。

**① 成因分解（`_piv_gap_probe.py`，60 品种 / 1,889 例 A 触发）**

| `_fib_eval` 分支 | 例数 | 占比 |
|---|---|---|
| `piv is None`（窗口内无枢轴） | **0** | 0% |
| `lg <= 0`（腿退化） | **0** | 0% |
| **`(i - piv) < 2`（枢轴太近）** | **704** | **37.3%** |

枢轴距离 `i-piv` 分布：`1→704`、`2→476`、`3→268`、`4→178`、`5→102`、`6→76`、`7→30`、`8→24`、`9+→28`；**中位数恰为 2**。
→ 🔴 **「无有效腿」100% 由 `(i-piv) < 2` 造成**。机理：A 回踩当根的最低价常即全窗最低 → 枢轴必落 `i-1`
（搜索自 `j=i-1` 起且须 `l[j-1]` 存在，**只能**命中 `i-1`）→ 被「太近」规则**系统性自锁**。
→ 反事实：704 例中 **619 例（87.9%）**在 `gap>=1` 下会落到档位内 → **A 支路信号量 +63%**。

**② 放宽方案（方案B，用户已同意）**：`(i-piv) >= 1`；腿的有效性**改由幅度定义**（`lg >= k × ATR`，**k=1.0**），
腿长不足则**继续前寻更早枢轴**而非作废。回退：`min_gap=2` + `k=0`。

**③ 双跑结果（55 品种小时线主连，V3.4 全参，成本 1.3bp；信号层逐字抽取线上 `walk_fusion_states`，
自检确认 `relax=False` 与原版逐位一致 → 放宽是唯一变量）**

| 指标 | base（原版） | relax（方案B） | Δ |
|---|---|---|---|
| **信号数** | 34,773 | 37,144 | **+2,371（+6.82%）** |
| **成交笔数**（逐手） | 25,813 | 27,510 | +1,697（+6.57%） |
| 其中加码手数 | 8,440 | 8,952 | +512 |
| **胜率** | 31.55% | 31.46% | **−0.09pp** |
| **盈亏比** | 2.120 | 2.100 | **−0.020** |
| 期望（点/笔） | −3.55 | **−5.64** | **恶化 59%** |
| 净点值 | −91,709 | −155,261 | −63,552 |
| 净 R（1R=2ATR） | −113.1R | −203.7R | −90.6R |

- **成分半同号**（§6.9 判据三件套之③）：两口径前半/后半**同为负** → 同号 ✅（方向可信）。
- **品种级配对**：55 品种中 **25 改善 / 30 恶化**；受益集中于 `lc +14,739`（几乎独占全部账面改善），
  受损广布有色主力（`sn −37,786`、`ni −18,114`、`ru −7,527`、`cu −5,327`、`al −5,249`）→ **噪声结构，非边际改进**。
- **判据三件套：① 参数平台 ❌（k 只测了单点 1.0，未扫）② 跨体系同向 ❌（未做）③ 分半同号 ✅** → **两件不过**。

**④ 判定：🔴 不采纳（用户已决定回退，维持 §16.8「不得放宽」）。**
放宽确实解开了自锁（信号 +6.8%），但放进来的是**负期望交易**（期望恶化 59%、胜率↓、盈亏比↓），
与 §16.8「斐波 = 风险调节器（拦掉无结构回踩）」的定位**完全一致**。
→ **与 §16.8 的关系**：§16.8 扫的是 `tol`/`W`/`ratios`，**从未**扫 `(i-piv)` 阈值 → **本次是新旋钮，非重复试错**；
结论**同向**，等于给「不得放宽」补上了一个未测分支的证据，并解释了 `W` 哑参数的根因。

**⑤ ⚠️ 口径保真警示（引用本 § 数据者必读）**
1. **`-99.98%` 收益是记账产物，非策略亏损**：引擎 `_metrics_from_trades` 用 `eq=cumprod(1+pnl_pct/100)`，
   等价「每笔 100% 满仓」，2.5 万笔连乘必归零（实测 Σpnl_pct=+7.9% → 0.000239）。**收益/回撤两项弃用**，
   只采用信号数/笔数/胜率/盈亏比/净点值。
2. **小时线平台 ≠ 生产口径**：线上跑**日线**（EMA20/10 折叠）+ A/B 入场；本回测为**小时线**（EMA140）+ `both_nm`。
   故此处胜率/盈亏比**不可**当作线上指标，只能作 base↔relax **相对**比较。
3. **两个分母不可混用**：拦截率对「可评估 A」vs「全体 A」相差 **2.4×**（§16.2 坑），本次统一为「对可评估 A」。

**⑥ 回归验收（`_recon_engine.py`，676 个 bar×方向组合）**

```
旧口径回退一致性: 逐位一致 ✅（判定不一致 0｜仅线上有腿 0｜仅日线有腿 0）
§16.9 放宽量: 旧无腿→新放行 170｜枢轴换人 28｜合计 198（占组合 29.29%）
```
→ ⚠️ 实现中曾自引入一个 bug（把「枢轴太近」写成 `continue`，导致 `min_gap=2` 时会越过 `i-1`
前寻更早枢轴，**回退路径不再等价**），已修正为 `min_gap>=2` 时 `break`（整根作废、不回溯）；
修正后回退路径逐位一致。**教训：放宽类改造必须单独对回退路径跑逐位对账。**

**⑦ 技能侧改动（WB 已完成，qhyc 侧一行未改，遵守分工铁律）**
- `skills/futures-daily-brief-pipeline/scripts/pipeline/_fut_daily_brief.py`
  —— `_fib_eval()` 重写为「幅度定义腿 + `min_gap` 参数化」；新增常量组
  `FIB_MIN_GAP = 1` / `FIB_K_ATR = 1.0`（L66–73）；`fib_confluence()` 透传 `k_atr`/`atr_v`。
- `.../pipeline/_recon_engine.py` —— `fib_daily()` 同步复刻；对照段改为**两口径并列**
  （旧口径回退一致性 + 放宽量），防 D1/D2 回归断言误报。

**⑧ 若策略方决定采纳（= 方案A），cb 侧改动点（精确锚点）**
| 文件 | 锚点 | 改动 |
|---|---|---|
| `app/strategies/fusion_signal.py` | **L233** 与 **L250** | `if i - _sli >= 2:` → `>= 1`（多空两处，**必须同改**） |
| 同上 | 斐波段（L227–262） | 腿有效性由「右端 ≥2 根」改为「**腿长 ≥ k×ATR**（k=1.0，可配）；不足则**继续前寻更早枢轴**」 |
| `app/core/config.py` | `fusion.*` | 新增 `fib_min_gap=1` / `fib_k_atr=1.0`（**默认建议仍为 2 / 0 = 不改行为**） |
| ⚠️ 前置 | —— | **必须先补 k 的敏感性扫描**（k ∈ {0.5,0.75,1.0,1.5,2.0}）并成平台，否则§6.9 判据①不过 |

---

## 16.10 运营排查：「推送有信号、同花顺图上无标记」（2026-09-22 夜，WB）

> **症状**：用户收到 `OI 菜油 卖(开空) @10,168（止损 10,277.3 / 保本 10,140.7）`、
> `EG 乙二醇 卖(开空) @5,195（止损 5,469.7 / 保本 5,126.3）` 两条推送，但**同花顺小时图未见「开空」标记**。
> **初判结论（⚠️ 后被实图部分推翻，见 §16.10.6）**：推送无误；根因在「看图侧」。

### 16.10.0 ⚠️ 更正记录（2026-09-22 深夜，依据用户实图）

用户提供 **菜油2701 · 60分钟 · RHH 主图** 实图，直接推翻初判的第一嫌疑「**周期挂错（挂日线）**」：

| 项 | 初判 | 实图证据 | 结论 |
|---|---|---|---|
| 周期 | 「最可能挂日线」 | 顶栏 **`60分`** 高亮 | **初判错误** |
| 图种 | 「可能副图」 | `RHH` + `AVGLINE/STOPLONG/STOPSHRT` 主图均线系 | **是主图** |
| 数据层级 | 未确认 | 图上 **ATR(14)=53.71** vs 推送反推 **54.6**（差 1.6%） | **同层级同源** |

**方法论教训（升格为排查铁律）**：在缺少当日数据、且未向用户确认图表配置的情况下，
**不得**把「统计上最可能的原因」当结论输出。**第 0 步必须是：请用户报「周期 + 图种 + 公式名」
（一句话），或先补齐当日数据，再下结论。「最可能」≠「已证实」。**

### 16.10.1 推送自洽性验证（先排除引擎错）

用点位反解 ATR、两法互验（`ATR=(止损−入场)/sl_atr=2.0`；`ATR=(入场−保本)/be_r=0.5`）：

| 品种 | 入场 | 止损 | 保本 | 法①→ATR | 法②→ATR | 判定 |
|---|---|---|---|---|---|---|
| OI 菜油 | 10,168.0 | 10,277.3 | 10,140.7 | 54.65 | 54.60 | **吻合** |
| EG 乙二醇 | 5,195.0 | 5,469.7 | 5,126.3 | 137.35 | 137.40 | **吻合** |

→ 两法独立吻合且倍数符合 V3.4（`SLK=2.0`/`BER=0.5`）→ **推送由引擎同一轮扫描产生，口径无误**。

### 16.10.2 ATR 量级定位数据层级（关键推理）

| 品种 | 推送反解 ATR | 小时线 ATR14 | 日线 ATR14 | 判定 |
|---|---|---|---|---|
| OI | 54.6 | **59.2** | 157.6 | **小时线** |
| EG | 137.4 | **115.6** | 209.4 | **小时线** |

日线 ATR ≈ 小时线 **2.5–4 倍** → **推送 100% 出自小时线引擎（`fusion.timeframe=1H`）**，
不可能出自日线。→ **用户必须看 60 分钟图**。

### 16.10.3 看图侧 5 坑（按概率排序）

| # | 坑 | 机制 | 修法 |
|---|---|---|---|
| 1 | **周期挂错**（最高频） | 挂日线看小时信号：日线当前 K **未收盘**，`REF(CLOSE,1)`/`BARSLAST`/枢轴全基于昨日 → **今日入场条件在日线上未成立**；且小时方向翻空 ≠ 日线翻空 | 改挂 **60 分钟**（核对指南 §一） |
| 2 | **挂成副图** | `DRAWICON` 按**价格坐标**画，副图坐标轴里 `LOW*0.997` 越界 | 指标属性→类型→**主图** |
| 3 | **公式 `NOPOS` 有意不画** | 公式含 `NOPOS := REF(INL,1)=0 AND REF(INS,1)=0`（要求**当前无持仓**）；推送为事件驱动、不论是否有仓 | **设计差异，非 bug**；两者互补 |
| 4 | **冷却口径差** | 引擎有 `cooldown_bars=3`（平仓后 3 根不进场），**公式无冷却** | 边界根不一致属预期 |
| 5 | **多品种只开一张图** | OI（郑商所）与 EG（大商所）是两个品种两张图 | **逐个品种各挂一次** |

补充：`DRAWICON` 第 3 参数为**图标序号**，各版本图标表不同 → 不显示就换数字（1/2/3/4）。

### 16.10.4 方向层一致性已实证（排除「公式写反」）

逐根比对公式 `DIRP := IF(REF(CLOSE,1) > REF(EMA(CLOSE,140),1), 1, -1)` 与引擎 `htf_dir`：
**OI/EG 近 200 根一致率 100.0%** → 方向层同源，不存在方向写反。

### 16.10.5 交付

- 报告：`融合/同花顺无标记归因_OI_EG_20260922.md`（初版，**结论已被 §16.10.0 更正**）、
  `融合/同花顺无标记归因_OI_更正版_20260922.md`（第二版）
- 脚本：`融合/_sig_trace_oi_eg.py`、`融合/_fetch_oi_hourly.py`（tqsdk 补数）
- 技能沉淀：`futures-signal-push-execution/SKILL.md` 新增 **§⑤「推送有信号、图上没标记」三步定位法**
  （本次为该技能首次覆盖此最高频症状；**更正教训已同步写入**）
- 附：DB `fut_kline` 现状（排查时须知）——OI hourly 仅至 2026-09-11（10,000 根）、**EG hourly 为 0 根**；
  `_bt_cache` 日线至 2026-09-01 → **当日推送不走 DB/缓存，为引擎侧实时取数**。

### 16.10.6 实图复判（2026-09-22 深夜，OI701 · 60分 · RHH）

**图上硬事实**（放大裁剪逐区确认）：

| # | 事实 | 读数 |
|---|---|---|
| 1 | ATR(14) 三方互验 | 图上 **53.71** ≈ 推送反推 **54.6**（差 1.6%）；缓存 9/11 为 59.18（平滑衰减轨迹连续）→ **同层级同源** ✅ |
| 2 | 开空标记序列 | **两次开空均带 `B`**（B 支路突破型）：① ~9/11 开空B → S平空@10035；② ~9/16 开空B → S平空@10062 |
| 3 | 最右标记（~9/22） | **`平多 297` + ↓**（平多离场，**不是开空**） |
| 4 | 持仓线 | `STOPLONG:--` / `STOPSHRT:--` → 公式视角**当前空仓** |
| 5 | 光标 09-22 23:00 | O=10168 H=10192 L=10157 C=10174（**推送 entry=10168 恰为此根开盘价**） |
| 6 | 副图配置 | 只有 RHD/MACD/ATR，**无 ADX 副图** → 公式硬门槛 `AXOK`（REF(AXAD,1)≥15）图上不可目视 |

**9/22 最右根两支路验算**（公式口径）：

| 支路 | 条件 | 本根数据 | 判定 |
|---|---|---|---|
| B | `CLOSE<LLN` 且 `CLOSE<OPEN` | C=10174 > O=10168（阳线） | ❌ |
| A | `CLOSE<MAL` 且下穿 + 斐波 | 价格在 EMA20 上方横盘 | ❌ |

→ **9/22 这根（光标所在根）公式不画开空是正确行为**；且 9/21–9/22 横盘无突破，与 9/17 后无新开空一致。

**剩余两个待定解释（需用户确认推送时刻，或等补数后逐根复算定论）**：

| 解释 | 机制 | 验证途径 |
|---|---|---|
| 甲：**跨根错位** | 引擎 close-only 按**已收盘 K** 判定；用户看图时已是**下一根** → 标记画在**左边一根**、用户盯的是右边一根 | 光标左移一格（09-22 22:00 那根）查有无「开空」 |
| 乙：**推送为 9/16 前后所发** | entry=10168 落在 9/16 开空 B 的价位区间（约 10150–10200）→ 图上**有**标记，只是时点对不上 | 用户确认推送接收时间 |

**待办**：① 补 OI 小时线 9/12–9/22（tqsdk 进行中）后**逐根复算定论**；② EG 小时线 DB 为 0 根，优先补；
③ 「跨根错位（标记在左、盯在右）」写进核对指南；④ 建议用户**加挂 ADX 副图**（否则 `AXOK` 门槛永远不可目视）。

### 16.10.7 ★ 最终定论（2026-09-22 深夜②：主连+合约双序列 RHH 全公式复算完毕）

> 取数兜底成功：新浪 `OI0` 主连 1023 根 + `OI2701` 合约 990 根（tqsdk 免费源挂死，HTTP API 秒回）。
> 复算：RHH 全公式逐根重放（EMA140/ATR Wilder/ADX EMA/枢轴/斐波腿/状态机）。

**结论一句话**：**推送没有错，用户图也没有错——引擎（主连）与用户图（OI2701 合约）是两条不同的
历史价格路径，EMA140 差 15 点，恰好把 9/22 22:00 根的方向判定 `DIRP` 推向相反两侧：
主连开空 ✓，合约图不画 ✓。这是数据源结构性差异，不是 bug。**

**决定性证据**（公式第一道门 `DIRP:=IF(REF(C,1)>REF(MA140,1),1,-1)`，判定根=9/22 15:00）：

| 数据源 | 前收 C | 同根 MA140 | DIRP | 结果 |
|---|---|---|---|---|
| 引擎·主连 OI0（复算） | 10218 | **10225.51** | **-1 空** | A 支路五条件全过 → **开空 ✓**（推送发出） |
| 用户图·OI2701（同花顺顶栏） | 10218 | **10210.26** | **+1 多** | `OKS:=0` → **不画 ✓**（实图无标记） |
| OI2701 独立复算（新浪 990 根） | 10218 | 10175.54 | **+1 多** | **不开空 ✓（独立复现用户图行为）** |

- **10218 恰好卡在 10210.26 与 10225.51 之间** → 15 点 EMA 差 = DIRP 翻转 = 标记有无翻转；
- EMA140 无限路径依赖：主连=主力拼接路径（1–4 月跟随 OI2605）vs OI2701=远月自身路径，
  8 个月价格差异累积 → 当前值天然差十几点（三个口径 10225/10210/10175 互不相同，
  但**全部 <10218 → 「用户图不开空」在所有口径下稳健**）；
- 三个待定解释裁定：**甲「跨根错位」价格定位层面对**（entry=10168=09-22 22:00 根收盘，
  引擎 close-only 判上一根）但「左移一格可见标记」错（用户图该根同样不画）；**乙「推送是 9/16 所发」错**
  （推送即当日，ATR 两法互验 54.65/54.60 自洽）；§16.10.6「9/22 根两支路不成立=公式正确」对但理由不完整
  （完整理由是 DIRP 层分歧）；**「平多 297」确认为同一根源表现**（用户图 EMA140 路径更低 →
  8 月底以来 DIRP/状态机与主连不同步 → 用户图 9/21 夜盘平多 vs 主连复算 9/21 14:15 平空）。

**用户自验证两步**：① 光标放 9/22 15:00 根看顶栏 MA140≈10210（<10218）→ 多头方向拦截开空；
② 切**菜油主连** RHH 60 分钟 → 9/22 22:00 根应见「开空」标记。

**系统层动作（采纳）**：
1. **推送文案加「（主连）」字样**——推送=主连 continuous 口径（回测/实盘同口径，§15 #2 已裁定），
   用户用具体月份合约图核对时，EMA140/ADX/斐波腿都可能因路径不同而与推送分歧；
2. **核对指南 §一 增补**：「核对推送请用主连图（或主力连续），不要用具体交割月份合约图」；
3. **EG 推送同理**（EG 5195 亦主连口径，用 EG2601 等合约图核对可能出现同类分歧）；
4. 「60 分钟周期」「标记画在已收盘根」两条既有结论保留。

**新增工件**：`_fetch_oi_sina.py`/`_oi_sina_hourly.pkl`、`_fetch_oi2701.py`/`_oi2701_hourly.pkl`
（含 9/12–9/18 换月无大缺口扫描、OI2701 vs OI0 近 16 根 ΔC=0 对比——证明分歧不在近期数据而在历史路径）、
`_recalc_rhh_oi.py`（主连定论）、`_recalc_rhh_oi2701.py`（合约定论）。
报告定稿：`融合/同花顺无标记归因_OI_更正版_20260922.md` §九。

### 16.10.8 ★★ 信号时间戳错位事故（2026-09-23 上午：7 条推送价格盘面不存在）

**事故**：用户收到 7 条开仓推送（SN 414,170 / SA 1,009 / AP 7,332.5 / HC 3,306.5 / A 5,010 / Y 8,954 / JM 1,487），
**全部无法在盘面找到对应价格**；推送留痕显示 10:05 一轮 **信号18·止损6**、10:35 又信号1。

**根因链**（逐环实锤）：
1. **新浪分钟线接口 datetime 标签整体 +8h**：真实 09:00-10:00 收盘棒被标 "18:00"（AP「18:00」棒收盘 7079
   与新浪 AP0 真实 10:00 棒逐位一致）；SN 夜盘 22:00/23:00/00:00/01:00 被标 6:00/7:00/8:00/9:00
   （沪锡夜盘至凌晨 1 点，4 棒完美对齐）——**不是采集器转换 bug，是新浪接口侧口径错误**；
2. 旧代码（无防护）把这些棒写入 `hourly_bar` → **47 个品种各 1 根「未来棒」**（今天 18:00 > 当前 10:05）；
3. **新鲜度门控负数盲区**：`fresh = age_min <= stale_minutes`，`age_min = 10:05 − 18:00 = 负数` 恒判"新鲜"
   → 门控从未生效；
4. 状态机在「错位棒 + 正确棒」乱序序列上行走：SN 推送入场 414,170 = 未来棒收盘（真实 411,300–412,020）；
   AP 两腿均价 7,332.5 = 9/16 真实棒价位（tqsdk 9/16 10:00 C=7334）——**上周价格当今日信号推送**；
5. 10:05 误触发叠加当日早间「数据质量整改」清库后的状态重算（§16.10.7 早间会话），形成单轮 18 信号。

**修复（2026-09-23 上午，全部落地）**：
1. **采集器**：`HourlyCollector` 默认 `prefer="tqsdk"`（起点标签 +1h 重标为收盘口径，时间戳已实证正确）；
   **akshare 主源禁用**（`_fetch_akshare` 返回空，sina 标签口径未修复前不得入库），留 TODO；
2. **评估门控补丁**（scheduler.py 两处）：`fresh` 增加 `age_min >= -10` 下界——未来棒与陈旧棒一律不新鲜；
   `_heartbeat_rows` 持仓行同样剔除负 age；
3. **数据清理**（备份至 `融合/_backup_hourly_20260923/`）：删除 47 根未来棒 + 全部 src='akshare' 棒
   39.4 万行（**注意：tqsdk 路径的 src 也写 "akshare"，见 hourly_collector L285——实际是 tqsdk 全量历史**）；
4. **fusion_position 基线清空**（50 行幽灵持仓入备份）：引擎冷启动重建基线，只发一条启动通知，
   避免清理后状态重算再触发一轮修正信号轰炸；
5. **tqsdk 全量回填**（`scripts/backfill_hourly.py`，容器内 `PYTHONPATH=/app`，~7 分钟/50 品种）。

**遗留与教训**：
- 新浪分钟线标签 +8h 是**接口侧问题**，若要恢复 akshare 主源须先做口径矫正 + 未来棒剔除（guard 白名单
  `{0,1,2,9,10,11,13,14,15,21,22,23}` 挡不住落在白名单内的错位棒，如真实 01:00→标签 9:00）；
- tqsdk 棒 `src` 错标为 "akshare"（统一收盘口径的历史决定）——**按 src 统计来源时不可信**，建议后续
  改为 src='tqsdk' 并同步清洗/评估逻辑（本次未动，避免扩大改动面）；
- 教训：**清理前必须先验证兜底数据源真实覆盖**（本次误判 "10:18 backfill=tqsdk 已覆盖"，
  实为 akshare 棒，删后表空，靠 backfill 重建）。

---

*本 PRD 更新（2026-09-22 晚）：§15 #1「日线简报引擎 V2.0→V3.4 对齐」已交付并验收（ALL PASS），新增 §16.7 交付记录；§16.5.3 文档解锁完成。*
*追加（2026-09-22 晚②）：新增 **§16.9 斐波腿定义放宽评估**（新旋钮 `(i-piv)` + 幅度定义腿；判定**不建议放宽**，与 §16.8 同向）；§16.8 补记 `W` 哑参数根因。*
*追加（2026-09-22 夜）：新增 **§16.10 运营排查「推送有信号、同花顺图上无标记」**（推送自洽性验证 → ATR 量级定位数据层级 → 看图侧 5 坑）；**深夜依用户实图追加 §16.10.0 更正**（初判「周期挂错」被 60 分钟 RHH 实图推翻，升格排查铁律「第 0 步先确认图表配置」）与 **§16.10.6 实图复判**（两次开空均 B 支路、9/22 根两支路均不成立属公式正确行为；剩余「跨根错位/推送时点」两解释待定论）。*
*追加（2026-09-22 深夜②）：**§16.10.7 最终定论**——主连+合约双序列 RHH 全公式复算：根因为 **EMA140 路径依赖差异（主连 10225.51 vs 合约图 10210.26）在临界前收 10218 处翻转 DIRP 方向判定**；推送与图都没错，属主连/合约数据源结构性差异；采纳推送文案加「（主连）」+ 核对指南增补「用主连图核对」。*
*此前（2026-09-22）v3.0：**按整体架构精简重构**。删除原 §15–§20 变更流水章节，有效结论归并回所属层（§4 数据层 / §5 预测 / §6 策略 / §7 推送 / §8 回测）；**全部已证伪、已停用、预判勿做、数据受阻条款删除**，仅保留 §6.8 一行禁令清单承载"防重复试错"价值；新增 §13 #6.7~#6.9 与 §15/§16（日线简报对齐技术债）。原版全文备份 `docs/期货量化交易系统需求文档_PRD.md.bak_20260922`。*
