# 期货量化交易系统 — 需求文档 (PRD) · 统一版

> 版本：v2.5（2026-09-21，融合策略 **V3.4** 定稿口径 + **CR-6 方法学加固** + **CR-7 优化方向评审**）｜ 初版：2026-09-16
> 本文件**合并并取代**《期货预测平台需求文档_PRD.md v1.3.5》与 WorkBuddy 的融合/信号/回测补充文档，是 codebuddy（CB）编码、workbuddy（WB）复审的**唯一权威需求来源**。
> 工程根目录 `E:\Docker\qhyc`；数据模板源 `E:\QH\{PRODUCT}\data`；桌面权威参考 `C:\Users\Seven\Desktop\融合\终版资料\01~04`。

---

## 1. 项目概述与目标

构建一个**本地运行的期货量化交易系统**，在统一的数据底座上，同时具备：

1. **预测系统**：基于数学/统计/机器学习模型，预测下一交易日涨跌方向与幅度（CB 主导）。
2. **交易策略判定**：基于**融合策略 V3.4**（额外执行口径 + ADX 门控 + 斐波汇流 + 阶梯加码），在小时线级别判定每个品种的**持仓状态**（空仓/持多/持空）与**开/平/反手**信号（WB 主导）。
3. **信号推送**：将交易信号实时推送至微信（pushplus）/ 企业微信 / 钉钉 / 飞书，含止损位、保本位、持仓简报（WB 主导）。
4. **回测与研究**：可手动调整参数进行回测，支持**鲁棒性测试**（多源/多容量/多成本/多 seed）、**walk-forward 测试**，并纳入系统 UI（WB 主导，方法学源自 04 资料）。

**非目标（本期）**：自动下单执行、多用户账号体系、云端多租户（架构预留云端演进）。

> 一句话定位：**预测系统给"方向概率"，交易策略给"当下该不该动"，信号推送把结论送到手里，回测研究验证策略 robustness**——四者共用同一数据底座、同一套口径铁律。

---

## 2. 总体架构（分层）

```
[数据源] akshare(主,含新浪60分) ──┐
                                  ├─→ [采集/校准层] 入库 + tqsdk 补缺/比对 + 异常工单 + 小时线逐根增量
[校准源] tqsdk(天勤) ──────────────┘
                                       ↓
[数据层] PostgreSQL + TimescaleDB
   · 运行时表：daily_bar(日线) / hourly_bar(小时线,逐根增量) / main_continuous(主连)
   · 回测底座：fut_kline 超表(freq×kind×symbol×trade_datetime，含 cont_adj 复权)
                                       ↓
[计算层] ┌─ 预测引擎（方向/幅度，模型库集成）        ← CB
         ├─ 交易策略引擎 fusion_signal（开/平/反手状态机） ← WB
         └─ 回测引擎（信号层 walk-forward + 组合层 FIFO 重放） ← WB/CB
                                       ↓
[服务层] FastAPI：行情 / 预测 / 信号 / 回测 / 数据更新 / 任务 / 异常 / 对接
                                       ↓
[展示层] React 看板（预测 / 信号 / 回测调参）+ 微信推送（pushplus/webhook）
```

---

## 3. 技术栈（已确认）

| 层 | 选型 |
|---|---|
| 语言 | Python 3.11+ |
| 后端 | FastAPI（异步，自动文档） |
| 数据库 | PostgreSQL 16 + TimescaleDB 2.17.1（超表） |
| 前端 | React 18 + Vite + TS + AntD + ECharts |
| 计算 | scipy / statsmodels / scikit-learn / PyTorch / prophet |
| 调度 | APScheduler（本地常驻） |
| 行情源 | akshare（主，含新浪 60 分钟）+ tqsdk（校准/兜底/复权） |
| 推送 | pushplus（微信主通道）+ 通用 webhook（企微/钉钉/飞书 降级） |
| 部署 | Docker Compose（本地起步，云端演进预留） |

---

## 4. 数据层（统一标准 + 日线/小时线获取与更新机制）★ 重点

### 4.1 落盘位置（物理与逻辑）
**所有行情最终落盘 Docker 容器内 TimescaleDB，不是本地 CSV/文件。**
- 容器 `qhyc-timescaledb`；库/用户 `futures`；端口 5432 映射宿主机。
- 物理卷：`E:/Docker/qhyc/pgdata/`（容器 `/var/lib/postgresql/data` 挂载）。
- 启动前提：**必须 Docker Desktop 运行**（沙箱内 `wsl.exe` 被安全策略拦截，需手动开 Docker）。
- 本地 `E:/QH/...` 与容器导入源仅作**离线备份/导入**，非落盘点；上线不依赖。

### 4.2 双表架构（两套表，用途不同，禁止混用）
| 表 | 周期 | 主键 | 写入方 | 用途 |
|---|---|---|---|---|
| `daily_bar` | 日线 | `(symbol, trade_date)` | `orchestrator.upsert_daily_bars` | akshare 主源+校准，日级特征/展示 |
| `hourly_bar` | 小时线 | `(symbol, trade_datetime)` | `HourlyCollector`（逐根增量 + 定时全量兜底） | **信号引擎实时读取**（按 `fusion.hourly_src` 单源） |
| `main_continuous` | 主连 | `(product, trade_date)` | `upsert_main_continuous` | 同时存裸价与复权价 |
| `fut_kline`（超表） | 全频率/全口径 | `(freq, kind, symbol, trade_datetime)` | `fdf/*` + `runtime/_build_daily_*` | **回测/研究底座**，含 `cont_adj` 复权权威 |

- `fut_kline.kind ∈ {continuous, contract, cont_adj}`；`freq ∈ {daily, hourly, min15, ...}`；`adj` 列标记已/未复权段（回测只取 `adj=True`）。

### 4.3 获取与更新机制

#### 4.3.1 日线更新（路径 A，已接调度）
- 主源 akshare `futures_main_sina` → `daily_bar`（`src=akshare`，未复权拼接价）。
- 校准：tqsdk 补缺 + 二次比对，异常开 `anomaly_ticket`（不阻塞主源）。
- **频率**：每日三次 cron —— `12:00`(noon) / `16:00`(close) / 次日 `08:00`(night)。
- 增量幂等：`(symbol, trade_date)` upsert，断点可续；范围 `2015-01-01` 起。

#### 4.3.2 小时线更新（★逐根增量更新 —— 用户硬性要求）
> **需求：小时线更新必须"每完成一根小时线，更新一根"**，而非定时批量重抓整段。

**新增机制 `HourlyBarIncrementalUpdater`（建议扩展 `app/ingest/hourly_collector.py`）：**
- **触发**：在每个交易时段内的小时 K 线**收盘时刻之后**增量采集。实现二选一（推荐组合）：
  - (a) **分钟级 cron**：在交易所小时线收盘时点后 1 分钟触发（如 `:01`/`:31`/`:46`，覆盖白盘 10:15/11:30/13:30/14:15/15:00、夜盘 21:00/22:00/23:00/23:30/01:00/02:00/02:30 等，由配置化的收盘时刻表驱动）；
  - (b) **轮询兜底**：每 5 分钟检测"比本品种上次成功更新更晚收盘的 bar"，补采遗漏。
- **行为**：只抓取「自上次成功更新以来**新收盘**的小时 K」，**逐根 upsert** 到 `hourly_bar`（主键 `(symbol, trade_datetime)` 覆盖）—— 即"完成一根、更新一根"，满足实时性。
- **定稿守卫**：对最新一根（距现在 < `settle_delay_sec=150s`）等待定稿后重采一次覆盖（复用 `_collect_hourly_settled` 逻辑，防新浪收盘瞬间过渡值致 JD 案例式反手）。
- **主源/兜底**：akshare 新浪 60 分钟主源；失败/限流 → tqsdk `get_kline_serial` 兜底（全品种**复用单 TqApi 连接**，避免 50× 登录超时）。
- **断点续传**：记录每个品种最后更新时间；akshare 仅近期窗口、历史回填用 `--prefer tqsdk` 并调大 `data_length`。
- **与既有机制互补（不冲突）**：保留 ① `16:00` close 档联动的**全量快照** `collect_all()`（对齐/校准用）、② 融合扫描每 15 分钟尾部 800 根**刷新**（容错/补齐）。逐根增量是"准实时主通道"，批量是"兜底校准通道"。

#### 4.3.3 回测底座 `fut_kline` 更新（路径 B/C + 聚合，非 scheduler 自动）
- `continuous`/`contract`：tqsdk `fetch_fdf.py` → `db_pg.save_bars`。
- `cont_adj`：`build_continuous.py` + `adjust_fdf.py` 重建（**先 `delete_bars` 清旧再插**，防脏累积）。
- `daily`（回测用）：由 hourly 聚合（`_build_daily_*.py`，含夜盘归属修正 ≥20:00 归下一交易日）。
- **触发**：手动 / CI（容器 `RUN_ON_BOOT=0`，避免重启重复全量回补）。数据更新后应重跑路径 B/C + 日线聚合，再跑回测。

#### 4.3.4 周/月维护（scheduler）
| 时间 | 任务 |
|---|---|
| 周六 06:00 | LSTM 周重训 |
| 周六 06:30 | 传导权重周重算（corr/granger/te/var） |
| 周六 07:00 | M4 周度回测 |
| 每月 1 日 06:30 | 模型权重月更（近 60 日准确率） |

#### 4.3.5 一致性红线（口径铁律，两边都必须遵守）
1. **单源消费**：信号引擎按 `fusion.hourly_src`（默认 `akshare`）读 `hourly_bar` 唯一口径；回测按 `(freq,kind,symbol,src)` 单源读 `fut_kline`。**杜绝双源混读**（akshare 结束时刻标 vs tqsdk 起点标会被当成两根喂指标，砍半周期）。
2. **剔除未收盘 K**：`drop_forming_bars` 丢弃 `now < dt` 的正在形成 K。
3. **定稿守卫**：`drop_unsettled` 剔除收盘距现在不足 `settle_delay_sec` 秒的最新一根。
4. **复权唯一权威**：回测复权价只取 `fut_kline.cont_adj`；`daily_bar`/`hourly_bar` 的 akshare 主连**未复权**。
5. **涨跌口径统一 `close`**（非 settle），见 §5.4 / §17。
6. **标签完整**：任何数据源入库带 `src`；回测/信号指定 `src` 读取。

### 4.4 复权标准（加法平移前复权）
- 主连换月仅一处「新旧合约价差」断层，正确修复是「把断层之前全部历史整体平移 delta」。
- 算法：`pick_dominant`（日持仓量最大为主力，需 `ROLL_CONFIRM=3` 日领先确认、只向前换）→ `delta=新(t-1)收−旧(t-1)收` → 前复权 `t` 前全部 `+=delta`；剔除未走完 K 与主力持仓不达标（`RELIABLE_OI=100000`、`RELIABLE_HOLD=20`）早期区间；`adj` 列标记。
- 口径归口：`fut_kline.cont_adj` 唯一权威；`main_continuous.adj_*` 另存主连复权裸价供线上特征。

### 4.5 字段统一约定速查
| 项 | 约定 |
|---|---|
| 品种规格 | `fdf/symbols.json`（`code_digits`/`multiplier`/`tick`/`months`） |
| 交易所代码 | CZCE(大写)/SHFE/DCE/INE/CFFEX/GFEX(小写品种码) |
| 合约码位数 | 郑商所 3 位（FG609），其余 4 位（rb2601） |
| 时间 | `trade_datetime` = 北京时间墙钟的 K 线**收盘时刻** |
| 价格精度 | `NUMERIC(20,4)`；展示按 `_TICKS` 最小变动价位对齐 |

---

## 5. 预测引擎（CB 预测系统 · 摘要 + 锁定结论）

> 详细模型实现见旧 PRD《期货预测平台需求文档_PRD.md》§5/§16/§17/§18。本节保留**对编码有约束力的锁定裁决**，CB 照此实现，WB 照此复审。

### 5.1 特征工程
输入窗口：当日涨跌幅 + 前 5/20 根 K 涨跌幅（以"根数"表达，见 §4.3 频率无关）；波动率、动量、Hurst、样本熵、ATR；Zigzag 拐点、Qi(ATR+MACD) 辅助。

### 5.2 模型库（按族）
变换类（傅里叶/小波）、统计时序（ARIMA/SARIMA/GARCH/卡尔曼）、状态概率（马尔可夫/HMM/贝叶斯/蒙特卡洛）、ML/DL（LSTM/GRU/Transformer/XGBoost/LightGBM/RF/GPR）、复杂系统（Hurst/样本熵/相空间重构）。

### 5.3 集成机制
- **状态门控**：Hurst 判定 trend/mean_revert 选择启用子集。
- **方向投票**：加权投票（权重每月按近 60 日回测准确率更新，⑳）；起步涨/跌二分类。
- **幅度融合**：中位数点估计 + [P5,P95]。
- **输出契约**（v1 JSON，对接简报复用）：`symbol/target_date/as_of/run_id/direction/direction_prob/ret_point/ret_low/ret_high/confidence/participated_models/state`。

### 5.4 锁定裁决（⚠️ 编码必须遵守）
| 项 | 结论 |
|---|---|
| 涨跌口径 | **`close` 收盘价**（§17 已裁决，禁止回到 settle） |
| 反转门控 `gate_pct` | **2.0%**（§18.11 阈值对齐结案；验收只用池化口径，禁止调阈值迎合覆盖率） |
| 覆盖率门槛 | ≥15% 为**结构性未达**已知偏差（K6），维持硬门槛、登记已知偏差 |
| 预测标的 | 仅主力连续合约（FG888 等） |
| 预测频率 | 每次数据更新后预测（一天三次：12:00/16:00/08:00） |
| 简报集成 | `/integrations/*` 与 `briefing_signal` 须显式声明 `caliber="close"` |

### 5.5 跨品种/大类传导（§16，已确认需求）
`sector_map` / `sector_index` / `transmission_weights`；先验+数据驱动（Granger/传递熵/VAR）融合；特征一律滞后防前视；权重每周重算。

---

## 6. 交易策略层（融合策略 **V3.4** · 额外执行口径 + ADX 门槛 + 斐波汇流 + 阶梯加码·放开保本）★ 2026-09-21 定稿

> 源：`桌面/融合/终版资料/02_融合引擎与额外策略最终版` + `app/strategies/fusion_signal.py`。本层与回测引擎**逐位一致（单一真源）**。
> **版本沿革**：V2.0（2026-09-16 统一版）→ **V3.1**（2026-09-20，去收强 + ADX≥15）→ **V3.2**（2026-09-20，
> P0 斐波汇流 + P1 阶梯加码·**结构保本**）→ **V3.4**（2026-09-21，**放开保本约束**、加码改由浮盈门槛门控）。
> 变更明细与实证见 **§15**（CR-1/CR-2 → V3.1）、**§16**（CR-3/CR-4 → V3.2）、**§17**（CR-5 → V3.4，含 32 项结论总账）。
>
> ⚠️ **代码落地状态（截至 2026-09-21）**：**线上引擎与同花顺公式当前仍为 V3.2**；本节描述的是 **V3.4 目标口径**。
> V3.4 与 V3.2 只差执行层的加码约束（见 §17.1），CB 按 §17.8 交付后即为 V3.4。
> **回归开关**：`fib_confl: false` + `add_max_lots: 1` = 逐位回到 V3.1。

### 6.1 五层结构
```
① 方向层（日线/小时 EMA140）  只做顺势一侧（多或空）
② 入场层（小时线）           A 回踩重启(优先，★须过斐波汇流) ∨ B 10根突破(补位)；全体 ★须过 ADX(14)≥15 门控
③ 离场层（小时线·额外口径）   2×ATR 初始硬止损 + 2×ATR 吊灯 + 0.5R 保本；仅小时线收盘击穿才平仓
④ 执行层                    当根收盘成交；开仓恒 1 手；★P1 台阶加码（至多 2 手，浮盈门槛 1.0×ATR）；
                            ★无额外保本地板约束；离场后冷却 3 根；单品种至多 1 仓
⑤ 风控层                    单笔风险封顶；保证金上限；月度熔断
```
**三条不可移除硬约束**：① 不设固定止盈（靠吊灯让利润奔跑）；② 不设结构止损；③ 移动止损只向有利方向移动，永不回撤。

> ★ **V3.4 起取消第 4 条隐性约束**：V3.2 曾要求"吊灯止损已推进到加码后均价之上才可加码"（结构性保本）。
> 实测该约束让加码**损失了 19.6% 的收益**（+58.2万 → +69.6万），而回撤只多 1.4万、MAR 反而更高，
> 故 V3.4 **放开**该约束，改由浮盈门槛（`add_thr_atr = 1.0`）代替。**代价**：加码后不再保证"最差=保本"，
> 加码单约 53% 会以亏损结束（但亏损中位仅 −0.16R，均值 +1.00R）——详见 §17.2、§17.3。

### 6.2 方向层
- 小时 EMA140 方向（等价日线 EMA20）：`REF(C,1) > REF(EMA(C,140),1)` → 只做多；反之只做空；临界两侧都不做。
- 用**已收盘**小时线确认；指标取 `DIRPREV` = 上一根方向，避免未收盘值（无前视）。

### 6.3 入场层
- **A 回踩重启（优先）**：方向允许多 + `C>EMA20(H1)` + `L≤EMA20` + 上一根 `C<EMA20`（刚穿越回）。
  **★V3.2 引入、V3.4 沿用的斐波汇流门控**：`L`（空为 `H`）须落在「最近一个**已确认枢轴**低（高）→ 其后极值」这段
  "腿"的 **0.382 / 0.5 / 0.618** 回撤位 **±0.5×ATR** 内；不满足则本根回踩信号作废。
  斐波腿取枢轴用**本根做右侧确认**（`l[i-1] < l[i-2] and l[i-1] < l[i]`）、回看窗 `W=60`，天然**无前视**。
- **B 10 根突破（补位）**：方向允许多 + `C>前10根最高价(不含当根)` + `C>O`。**不受斐波汇流影响**。
- 入场模式参数 `entry_mode ∈ {pullback, breakout, both, both_nm}`（默认 `both_nm`，去 MACD）。

### 6.4 离场层（额外执行口径）
- 初始止损 `init_stop = entry ∓ sl_atr×ATR`（`entry` 取**加权均价**；未加码时 == 首次买入价）。
- 吊灯止损 `trail_stop = peak/trough ∓ trail_atr×ATR`。
- 保本 `be_trigger = first_px ± be_r×ATR`（默认 `be_r=0.5`，基准恒为**首次买入价**）；触发后止损上移至首次买入价。
- 当前生效止损 `cur_stop = 三者中最紧者`（多取 max / 空取 min）。
- **仅小时线收盘击穿 `cur_stop` 才平仓**（盘中插针无视）。

### 6.5 执行层
- **close-only**：开仓价=开仓根收盘价，与回测"只看收盘价"完全一致；离场后冷却 `cooldown_bars=3`；单品种至多 1 仓。
- **★P1 利弗莫尔·阶梯加码（V3.4：放开保本约束）**：开仓**恒为 1 手**；仅当同时满足
  ① 收盘**创入场以来新高**、② **浮盈达到门槛**（`c[i] − first_px ≥ lots × add_thr_atr × ATR`，默认 `add_thr_atr=1.0`）
  才加 1 手（默认 `add_max_lots=2`，即至多一次；门槛随手数线性抬升，是"金字塔"而非"倒金字塔"）。
  **V3.4 不再要求**"吊灯已推进到加码后均价之上"（V3.2 的 `add_guard_atr` 结构性保本前提，V3.4 停用）。
  加码后 `avg_px` 更新为**加权均价**，生效止损仍是「初始止损 / 吊灯 / 0.5R 保本」三者中最紧者，**收盘判定**。

### 6.6 风控层
- 单笔风险封顶（1R=单笔愿亏）；保证金上限；月度熔断（亏损达阈值停新开）。
- 预热：加载后前 `min_bars=160` 根信号不可用（ATR/EMA 未收敛）。

### 6.7 策略参数表（来自 `config/local.yaml` `fusion.*`，均可经回测面板调参）
| 参数 | 默认 | 含义 |
|---|---|---|
| `ema_k` | 140 | 方向层 EMA 周期 |
| `atr_n` | 14 | ATR 周期 |
| `ma_n` | 20 | 入场均线周期 |
| `sl_atr` | 2.0 | 初始止损倍数 |
| `trail_atr` | 2.0 | 吊灯止损倍数 |
| `be_r` | 0.5 | 保本触发倍数 |
| `W` | 60 | 分型窗口 |
| `entry_mode` | both_nm | 入场模式 |
| `cooldown_bars` | 3 | 离场后冷却 |
| `adx_n` | 14 | ADX 周期（V3.1） |
| `adx_min` | 15.0（V3.1 启用） | ADX 入场门控阈值，用**上一根**判定；0=关闭。**≥20 起转亏，勿抬** |
| `use_sbull` | false（V3.1 启用） | 回踩支路是否要求「收强/收弱」；true=旧行为 |
| `fib_confl` | true（V3.2 引入，V3.4 沿用） | 斐波那契·汇流门控（仅回踩支路） |
| `fib_ratios` | [0.382, 0.5, 0.618] | 回撤位集合 |
| `fib_tol_atr` | 0.5 | 斐波容差 = 该值 × ATR |
| `add_max_lots` | 2（V3.2 引入，V3.4 沿用） | 最大手数；1=不加码（回到固定 1 手） |
| `add_thr_atr` | **1.0（V3.4 新增）** | 加码浮盈门槛 = `lots × 该值 × ATR`（金字塔）。0.5/1.0/1.5 三档等效（±5万，噪声带内） |
| `add_guard_atr` | —（V3.2 用；**V3.4 停用**） | V3.2 的"结构保本"放宽量(×ATR)。V3.4 改由 `add_thr_atr` 门控，本项保留字段但不再启用 |
| `min_bars` | 160 | 预热根数 |
| `max_bars` | 400 | 实时读取上限 |

---

## 7. 信号推送层 ★ 新增

> 源：`app/scheduler.py`（`_fusion_scan_job` / `_collect_hourly_settled`）+ `app/notify/pushplus.py` + `app/api/routers/fusion.py`。

### 7.1 扫描调度
- 融合策略扫描**每 15 分钟**一次（与每日三次 ingest 并行），由 APScheduler `minute="*/15"` 触发。
- 每轮：① 拉最新小时线（逐根增量 / 尾部 800 根兜底 + 定稿守卫）；② `evaluate_all` 评估全部品种；③ 状态机去重（仅状态变化时推送）；④ 渲染并发送。

### 7.2 状态机去重
- 持久化 `FusionPosition`（symbol, position, entry_price, entry_at, last_stop, last_be_done, signal_at, pushed_at, **lots**）。
  `lots` = 当前手数（P1 加码用，V3.2 引入）；手数较上一轮增加即视为「加码事件」，独立推送。
- 冷启动（表空）：以引擎当前状态无条件建基线，再推送上线通知。
- 数据新鲜度：最新 K 距现在 > `stale_minutes=90` 分钟 → 抑制状态变化信号（防过期数据触发）。

### 7.3 推送形态（三段式）
1. 🚨 **新信号**：开/平/反手即时推，附 ⛔止损位 / 🎯保本位（价位按 `_TICKS` 最小变动对齐，如 FG=1、AU=0.02、SC=0.1）。
   持仓行在加码后显示 `×2手`；加码事件单列 `➕ … 加码 至 2手 ｜ 均价 … ｜ ⛔止损 …`（V3.2 引入，V3.4 沿用）。
2. ⚠️ **止损变动**：吊灯止损朝有利方向移动 ≥ `trail_push_atr=0.5×ATR` 补推；首次触发保本补推。
3. 📊 **持仓简报**：盘前（`pre_session_times` 08:45/13:15/20:45）强制推完整清单；交易时段内每 `heartbeat_interval_min=30` 一档（对齐 :00/:30）。
4. ⏳ **待执行复提**：开仓信号在 `signal_repeat_min=120` 分钟内每轮置顶，防漏看。

### 7.4 通道（旁路，失败不阻塞主流程）
- 主通道 **pushplus**（微信）：`POST https://www.pushplus.plus/send`，`template=markdown`，失败自动重试 1 次。
- 备用通道：通用 webhook（企微/钉钉/飞书），按 URL 域名自动选载荷格式；主通道失败降级。
- 留痕：`FusionPushLog`（pushed_at/kind/title/content/n_signals/n_rows/delivered/via），可回查历史信号。

### 7.5 推送窗口 / 日历
- `push_windows`：08:45-12:00 / 13:15-15:30 / 20:45-23:30；窗口外整轮冻结。
- 非交易日（周末 + akshare 交易日历缓存，超 7 天刷新）静默。

---

## 8. 回测与研究模块 ★ 新增（纳入系统）

> 源：`桌面/融合/终版资料/04_一百二十万回测方法维度步骤` + `runtime/_bt_fusion.py` / `_bt_multi.py` / `_bt_fusion_timeweight.py`。本模块把"手动调参回测 + 鲁棒性测试 + walk-forward"做成系统内功能（CB 编码、WB 复审）。

### 8.1 定位
- 用户可在 UI/API **手动调整策略与组合参数**，即时跑回测，对比不同参数下的净¥/PF/回撤/胜率。
- 内置 **鲁棒性测试**（四维矩阵 × 多 seed）与 **walk-forward 测试**（滚动窗口重估），验证策略稳健性。

### 8.2 两层回测架构（与 `fusion_state_detail` 单一真源）
- **信号层（逐品种前向循环）**：逻辑与 `fusion_signal.fusion_state_detail` **逐位一致**（EMA140 方向 → ADX(14)≥15 门控 → MA20 回踩（含**斐波汇流**）/10 根突破 → 2×ATR 止损/吊灯/0.5R 保本 → **阶梯加码** → close-only）。输出每笔 `(entry_i, entry_px, entry_atr, dir, exit_i, exit_px, R, lots, first_px)`。
- **组合层（时间轴 FIFO 重放）**：把所有品种成交事件按 `entry_dt/exit_dt` 排序重放；闸门 `max_positions`（默认 5）；**各品种手数 `lots ∈ {1, 2}`（V3.2 起支持变手数）**；净金额 `Y = R×lots×entry_atr×multiplier − 往返成本(bp)`（`R` 为**每手 R**）。
- **铁律**：引擎 `fusion_signal` 与回测 `bt_symbol` 必须同步改，改策略后两处都改并跑回测确认口径未分叉。**变手数变体一律以净¥与 DD 为准，不用 PF(R)**（PF 按每手 R 统计时对多手不可比，见 §17.7 铁律 5）。

### 8.3 四维测试矩阵（鲁棒性）
| 维度 | 取值 |
|---|---|
| ① 数据源 `src` | tqsdk / akshare / csv |
| ② 容量 `max_pos` | 1 / 2 / 3 / 5 仓 |
| ③ 成本 `cost_bp` | 5 / 10 / 20 / 30 / 50 bp |
| ④ 时段权重 | 不加权(p=1) / 加权(tqsdk 分时段期望) |
交叉组合最多 120 组，UI 可勾选批量跑。

### 8.4 可调参数面板（手动调参）
| 类别 | 参数 |
|---|---|
| 策略 | `sl_atr` / `trail_atr` / `be_r` / `entry_mode` / `W` / `ema_k` / `atr_n` / `ma_n` / `cooldown_bars` |
| 组合 | `max_positions` / `lots` / `cost_bp` / `seeds` |
| 数据 | `src` / `freq` / `kind` / `symbols` / `min_year` / `limit` |
| 加权 | `N_MIN` / 概率映射 |

### 8.5 walk-forward 设计
- **信号层**：按时间切滚动窗口（训练/评估不泄漏），定期用滚动窗口重估参数（与 §4.3.4 LSTM 周训/权重月更对齐）。
- **组合层**：滚动 FIFO 重放，输出分年度/分时段净值曲线。
- 机制性消除前视：评估点 `step` 抽样、参数估计只用 `as_of` 之前数据。

### 8.6 鲁棒性测试
- 多 `seed`（`seeds=30~60`）取均值±标准差，检验随机性敏感。
- 分年度 / 分品种拆解；成本盈亏平衡点扫描（已知 ~20–30bp）。
- 头部品种（j/au/ag/lh/jm）专项（原 +120 万结论中贡献毛利 44.5%）。

### 8.7 指标与输出
- 均 R、净 R、净 ¥、盈利因子 PF、胜率、最大回撤（R 与 ¥ 双口径）、覆盖率-准确率曲线。
- 每次回测 JSON 落 `bt_out/`，命名带 `tag`（日期/数据版本），横向可对比。
- **必须标注口径**：所用 `(freq,kind,src)`、品种窗口、成本 —— 不同口径结论可差 5~30 倍。

### 8.8 API / 前端（纳入系统）
- `POST /backtest/run`：提交参数（8.4 面板），返回 `task_id`。
- `GET /backtest/result/{id}`：结果（汇总 + 分品种 + 分年度 + 曲线）。
- `GET /backtest/matrix`：四维矩阵对比表。
- 前端「回测调参」页：参数表单 + 运行 + 结果对比表/图（PF/净¥/回撤随参数热力图）。

---

## 9. 服务层 API（合并）

| 方法 | 路径 | 说明 | 归属 |
|---|---|---|---|
| GET | /symbols | 品种列表 | 共用 |
| GET | /bars/{symbol}?freq= | 行情查询（daily/hourly） | 数据 |
| POST | /ingest | 手动更新（指定品种/全量） | 数据 |
| GET/POST | /integrations/* | 简报引擎对接 | 预测 |
| POST | /predict | /predict/batch | 预测 | 预测 |
| POST | /backtest/run | GET /backtest/result/{id} | /backtest/matrix | 回测 | 回测 |
| GET | /signals/latest | /signals/history | 当前/历史融合信号 | 信号 |
| GET | /anomalies | /anomalies/{id}/resolve | 异常工单 | 数据 |
| GET | /tasks/{id} | 任务状态 | 共用 |

---

## 10. 展示层（看板）

React 18 + Vite + TS + AntD + ECharts，涨跌配色**涨红跌绿**。
- **品种总览**：最新价、N 日涨跌、次日预测方向。
- **单品种预测详情**：K 线 + 方向概率/幅度区间/参与模型/特征/三次预测对比。
- **全市场热力图**：涨跌概率分布 + 大类强弱排名 + 传导来源卡片。
- **信号面板**：实时持仓（多红/空绿）、开/平/反手记录、止损/保本位、推送日志。
- **回测调参面板**：参数表单 + 运行 + 结果对比表/曲线（PF/净¥/回撤热力图）。
- **数据质量面板**：异常工单 + 裁决 + 校准日志 + 数据完整性自检。

---

## 11. 部署与运维（本地 Docker）

- 三容器：`timescaledb` + `python-api(含 scheduler)` + `web(nginx)`。
- 配置：token/更新时刻/校准阈值/模型权重 → `config/{local,cloud}.yaml` + `.env`（密钥不进 git）。
- 资源：日常 4 核/8GB/50GB SSD；深度重训 8 核/16GB。
- 启动前提：Docker Desktop 运行（手动开）。

---

## 12. 里程碑（合并编排）

| 阶段 | 内容 | 归属 |
|---|---|---|
| M1 | 数据层 + 日线获取与更新（每日三次） | CB |
| M2 | 特征 + 单模型预测 | CB |
| M3 | 全模型库 + 集成 + 传导 | CB |
| M4 | 回测引擎（walk-forward 机制） | CB |
| M5 | React 看板 + 简报对接 | CB |
| M6 | 小时线扩展 + 因子（库存/龙虎榜/carry） | CB |
| **M6x** | **交易策略融合引擎 + 信号推送 + 小时线逐根增量更新（§4.3.2/§6/§7）** | **WB** |
| **M8** | **回测与研究模块 UI/API（手动调参/鲁棒性/walk-forward，§8）** | **WB+CB** |
| M7 | 云端部署演进 | 共用 |

> M6x 与 M8 为本次统一 PRD 新增阶段，优先级高（用户当前重点）。

---

## 13. 待裁定 / 已知问题

| # | 事项 | 优先级 | 归属 |
|---|---|---|---|
| 4.1 | 复权 OI 门槛：低流动性品种（sc/ru/sn/pb）如何复权 | 高 | CB 主+WB |
| 4.2 | 复权是否接入自动调度 | 高 | 共担 |
| 4.3 | 权威数据口径（akshare 未复权 vs 天勤复权） | 高 | 共担 |
| 4.5 | 陈旧 cont_adj 全量重跑（57 品种） | 中 | CB |
| K1 | 龙虎榜 SHFE/DCE 接口坏（覆盖 25/73） | Open | CB |
| K4 | INE 无持仓排名/库存接口（硬上限 ~68/73） | Open | CB |
| K6 | 反转覆盖率 ≥15% 结构性未达（已知偏差） | Accepted | CB |
| 4.6 | **回测价格口径修订**：§14.2 原写"回测只读 `cont_adj`"已过时——WB 用 50 笔成交样本逐笔比对证实 **`continuous` = 同花顺真主连（50/50，Δ=0.0）**，`cont_adj` = 旧回测系统的偏移价（低约 2%、收益更差）；回测与实盘应统一以 `continuous` 为准 | 高 | CB |
| 6.1 | **融合策略 V3.4 定稿**：PRD 已按 V3.4 更新（§6 目标口径 + §17/§18 变更记录与 42 项结论总账）；**线上引擎仍停在 V3.2**，需 CB 按 §17.8 交付（放开加码的保本约束 + 新增 `add_thr_atr`），并重启 `python-api` 容器 | 高 | CB |
| 6.2 | ~~同花顺 `SLK := 1.0` vs 引擎 `sl_atr := 2.0`~~ **✅ 已裁定并执行（2026-09-21）**：用户裁定止损 = **2×ATR**，桌面两份 V32 公式已由 WB 改为 `SLK := 2.0`（`公式2_RHH_小时主图_V32.txt` / `公式3_RHT_交易系统_V32.txt`）。遗留：CB 需同步产出 V3.4 加码语义的公式版本（见 §18.1 #9） | ~~高~~ 已关闭 | WB 已改 → CB 同步 |
| 6.3 | ~~换月跳空守卫~~ **✅ 已裁定（2026-09-21，V3.7 实测）**：**策略层守卫不做**（#42 证伪）——持仓跨跳空根的 139 笔毛 ¥ 合计 **+64.5 万为正**（顺跳空 +98.7 万 > 逆跳空 −34.2 万），守卫会连利润一起砍；G2(3×) 强平 1,020 笔 → −28.9 万毁灭性；最差单笔（AP888 −63.32R）的出场根就是跳空根本身，收盘判定体系已在跳空根收盘自动平仓。**改为数据/执行面修复**（真实合约拼接 / 移仓按新旧价差结算），见 §18.1 #8 | 高 | CB（先方案后实施） |
| 6.4 | **补全 `min15` 数据**：库内 50 品种中**只有 FG** 有 15 分钟线，无法检验"小周期入场"主张；需 `fetch_fdf.py --freq min15` 补全 | 中 | CB |
| 6.5 | **扩样本**（品种池 / 更长历史）：净值 91% 由 Top5 笔贡献，扩样本是唯一能根本性提高结论可信度的方向 | 中 | 共担 |
| 6.6 | **方法学加固落地（CR-6）**：walk-forward 样本外验证、统计加固包（Bootstrap/置换检验/多重检验声明/CVaR）、基准对照行、容量梯度、品种池透明度、边界单元测试、VWAP 对比 → **CB 交付清单见 §18.1** | 高 | CB |

---

## 14. WorkBuddy 复审要点（供 CB 交付后复审）

WB 在 CB 完成编码后将按以下清单复审，**任一不符即退回**：

1. **单一真源**：`fusion_signal.fusion_state_detail` 与回测 `bt_symbol` 逻辑逐位一致；改其一必须同步另一并跑回测。
2. **口径铁律**：信号只读 `hourly_bar` 单一 `src`；回测只读 `fut_kline.continuous`（★2026-09-20 修订：原文为 `cont_adj`，经 50 笔成交样本逐笔比对证实 `continuous` 才是同花顺真主连，见 §13 #4.6）；无双源混读；涨跌 `close` 口径。
3. **小时线逐根增量**：§4.3.2 的"完成一根更新一根"已实现且带定稿守卫；既有批量兜底不冲突。
4. **定稿守卫**：最新 K < `settle_delay_sec=150s` 时等待重采覆盖（JD 案例防反手）。
5. **推送窗口/日历**：非交易日/非窗口整轮冻结；价位按 `_TICKS` 对齐。
6. **回测可复现**：参数面板改动可一键复跑；输出带 `(freq,kind,src)` 口径标注；四维矩阵与多 seed 可用。
7. **不入库**：`.env`/`config/local.yaml`/`pgdata/`/`runtime/`/根目录临时文件均被 `.gitignore` 排除。
8. **git 基线**：双方改动已提交 baseline，分支开发，无互相覆盖。

---

## 15. 变更记录 & 变更请求（CR-2026-09-20：融合策略 V2.0 → V3.1）

> 提出：WB（2026-09-20） · **执行：CB** · 依据：`桌面/融合/融合策略_V3.1_总策略_20260920.md`、`融合策略_ADX趋势强度过滤_20260920.md`、`融合策略_去掉收强条件对照_20260918.md`、`融合策略_滚动窗口对照_20260918.html`
> ★ **需求方明确：这两处改动不保留「回退旧口径」的兼容语义。**

### 15.1 变更内容（仅 2 处，都在入场层）

| # | 变更 | 类型 | 参数 |
|---|---|---|---|
| **CR-1** | **取消入场「收强」条件**（回踩支路） | 删除条件 | `use_sbull: true → false` |
| **CR-2** | **新增 ADX(14)≥15 趋势强度入场门控** | 新增门控 | `adx_n: 14`（新）、`adx_min: 0.0 → 15.0` |

方向层、离场层（2×ATR 初始 / 2×ATR 吊灯 / 0.5×ATR 保本 / close-only）、
执行层（冷却 3 根 / 单品种至多 1 仓 / 平仓后再开）**均不变**。

### 15.2 精确实现（单一真源；Python 与回测逐位一致）

**CR-1** —— `walk_fusion_states` 回踩支路：

```python
# 旧（V2.0）
s_tl = use_pb and ok_l[i] and up and tl and sbull and cross_up
s_ts = use_pb and ok_s[i] and dn and ts and sbear and cross_down
# 新（V3.1）
s_tl = use_pb and ok_l[i] and up and tl and (sbull or not use_sbull) and cross_up
s_ts = use_pb and ok_s[i] and dn and ts and (sbear or not use_sbull) and cross_down
```
> 突破支路的 `c[i] > o[i]` **保留**——它不是「收强」（无"收在上半区"要求），是突破支路自带条件。

**CR-2** —— 新增 `adx14()` 指标；入场门控置于「四个入场信号算完之后、`if s_tl or s_bkl:` 之前」：

```python
# 门控：用「上一根」ADX 判定，无前视（与回测 bt_engine 完全一致）
if adx_min > 0 and adx_arr[i - 1] < adx_min:
    s_tl = s_ts = s_bkl = s_bks = False
```

ADX 公式（Wilder 平滑；`ewm(span=n, adjust=False)` 的 α = 2/(n+1)，与同花顺 `EMA(X, n)` 等价）：

```
TR   = max(H-L, |H-Cprev|, |L-Cprev|)
+DM  = (H-Hprev)  若 (H-Hprev) > (Lprev-L) 且 (H-Hprev) > 0，否则 0
-DM  = (Lprev-L)  若 (Lprev-L) > (H-Hprev) 且 (Lprev-L) > 0，否则 0
ATR  = RMA(TR, 14);  +DI = 100*RMA(+DM,14)/ATR;  -DI = 100*RMA(-DM,14)/ATR
DX   = 100*|+DI - -DI| / (+DI + -DI);   ADX = RMA(DX, 14);   前 2×14 根置 0
```

### 15.3 实证（真主连 `continuous` · 容量 5 = 「固定 1 手 / 最大 5 手 / 平仓再开」）

| 口径 | 净¥(5bp) | 净¥(13bp) | PF | 盈/亏品种 |
|---|---|---|---|---|
| live（V2.0 线上原状） | +17.8万 | −7.3万 | 1.045 | 23/27 |
| live + ADX（只做 CR-2） | +33.6万 | +8.3万 | 1.064 | 21/29 |
| V3.0（只做 CR-1） | +33.1万 | +8.3万 | 1.068 | 19/31 |
| **V3.1（CR-1 + CR-2）** | **+42.9万** | **+17.9万** | **1.082** | 20/30 |

- 两处改动各约 **+15.5 万**，叠加后 **+25.1 万**（超可加：ADX 恰好砍掉「去收强」新增的那批低质回踩信号）。
- 滚动窗口：V3.1 在 W2(2021–24) +27.5万 vs live +12.3万、W3(2024–26) +30.5万 vs live +16.4万，全面领先；
  W1(2018–21) 所有口径皆亏（时代效应，非参数问题，不可过拟合）。
- 原油集中度：占组合净利由 live 的 **213% 降至 92%**（未根治，属"1 手地板"约束下的结构性问题）。

### 15.4 CB 交付清单

| # | 事项 | 状态 |
|---|---|---|
| 1 | `app/strategies/fusion_signal.py`：新增 `adx14()`；回踩支路支持 `use_sbull`；入场前 ADX 门控 | WB 已落补丁 → **待 CB 复核** |
| 2 | `app/core/config.py`：`FusionConfig` 增加 `adx_n=14` / `adx_min` / `use_sbull` | WB 已落补丁；**CB 将默认值改为 V3.1 口径**（`adx_min=15.0`、`use_sbull=false`，不再保留旧口径回退语义） |
| 3 | `config/local.yaml` → `fusion.*`：`adx_n: 14` / `adx_min: 15.0` / `use_sbull: false` | WB 已配置 |
| 4 | **重启 `python-api` 容器**（`app/` 为挂载目录，Python 进程不热重载） | ⬜ 待 CB |
| 5 | 等价性验证：把补丁后的线上函数抽出来在真实小时线上推演——旧口径 **17,840 笔**（= 回测 live）、V3.1 口径 **17,982 笔**（= 回测 v31），**双向逐位一致** | ✔ 通过（`桌面/融合/_fusion_v31_verify.py`） |
| 6 | `py_compile` 两个被改文件 + `local.yaml` 解析 | ✔ 通过 |
| 7 | **同花顺 / 文华 指标公式副本同步**（见 §15.5） | ⬜ 待 CB（RHX 与核对指南已由 WB 同步） |
| 8 | 桌面交付可直接粘贴的同花顺 V3.1 公式（RHH / RHT） | ✔ WB 已交付 |

### 15.5 必须同步的指标公式副本（否则「图 vs 引擎」再次分叉）

| 文件 | 处理 |
|---|---|
| `docs/RHX_融合策略指标公式.txt` | **已同步为 V3.1**（去 `FSBL/FSBR`、加 ADX 门控） |
| `docs/融合策略信号_同花顺核对指南.md` §六 | **已同步为 V3.1** |
| `桌面/融合/融合_同花顺_公式2_RHH_小时主图_V31.txt`、`融合_同花顺_公式3_RHT_交易系统_V31.txt` | ✔ 新增，用户可直接"打开→全选→复制→粘贴" |
| `docs/02_融合引擎与额外策略最终版/*`（RHH / RHT / 同花顺V2.0指标代码 / 文华6 / 额外执行口径完整版.md） | **归档保持原样**（V2.0 历史版本）；**使用中的副本一律以 V3.1 为准** |

> ⚠️ **两处必须知道的既有差异（非本次改动引入）**：
> 1. **`SLK` 与 `sl_atr` 必须一致**：同花顺 V2.0 公式为 `SLK := 1.0`，而引擎 `sl_atr := 2.0` ——
>    **初始止损宽窄差一倍**，必然出现"图上已平、引擎仍持仓"。本次交付文件按需求只做「去收强 + ADX」两处改动，
>    **未动 `SLK`**；若要图与引擎完全对齐，请把 `SLK` 改为 `2.0`（一行）。
> 2. **结构性近似**：同花顺公式用 `BARSLAST(BK0)` 反推"入场参照"、且只有收盘价，
>    持仓中途再现原始信号时止损线会漂移；THS 也无法表达组合层约束（最多 5 仓 / 4 万本金）。
>    **信号一律以引擎推送为准，图只作参考。**

---

## 16. 变更记录 CR-2026-09-20（v2.2）：融合策略 V3.1 → **V3.2**

> 触发：用户指令「做 P0 和 P1：**一手固定开仓，后续可以使用利弗莫尔加码收益，但要注意回撤风险，
> 盈利后最差是保本而不是亏损**」。P0 = 斐波那契·汇流；P1 = 利弗莫尔·阶梯加码。
> 全部实证脚本见 `桌面/融合/_fusion_v32.py` / `_fusion_v32_verify.py` / `_ths_v32_check.py`。

### 16.1 两处改动（都在入场层与执行层，方向层/离场层口径不变）

| # | 改动 | 位置 | 说明 |
|---|---|---|---|
| CR-3 | **P0 斐波那契·汇流门控** | 入场层（**仅回踩支路**） | 回踩极值须落在 0.382/0.5/0.618 位 ±0.5×ATR 内 |
| CR-4 | **P1 利弗莫尔·阶梯加码（结构保本）** | 执行层 | 开仓恒 1 手；**吊灯已推进到加码后均价之上**才可加至 2 手 |

**CR-3 精确实现**（`walk_fusion_states`）：
```python
# 斐波"腿"：最近一个已确认枢轴低(pl[-1]) → 其后最高（回看窗 W=60，无前视）
seg  = h[sl_i:i+1];  sh_i = sl_i + argmax(seg);  leg = seg.max() - l[sl_i]
# 汇流：本根最低价须落在 0.382/0.5/0.618 位 ± tol×ATR 内，否则回踩信号作废
hit = any(abs(l[i] - (h[sh_i] - r*leg)) <= 0.5*atr14[i] for r in (0.382, 0.5, 0.618))
# 空头镜像：h[i] vs (l[sl_j] + r*leg2)
```

**CR-4 精确实现**：
```python
# 开仓恒 1 手；加码条件（多；空镜像）
hh = max(h[entry_i:i])                       # 入场以来最高（不含当根）
avg_new = (avg_px * lots + c[i]) / (lots + 1.0)
trail   = peak - trail_atr * e_atr           # 当前吊灯止损
if c[i] > hh and trail >= avg_new - add_guard_atr * e_atr:
    avg_px = avg_new; lots += 1              # 至多加至 add_max_lots（默认 2）
```
> **为何这样设计**：唯一能"保证"保本的做法是把保本止损当**盘中挂单**（任意影线触及即成交）。
> 但实测该口径会让系统崩塌（基线 真实1跳 **−142 万**）——**本系统的边际利润正来自"收盘判定、不被插针打掉"**。
> 因此保本必须由**已有机制**承担：只要加码时吊灯已在均价之上，加码后整仓止损就是吊灯，
> 最差成交 ≈ 均价 = 保本，且**完全不需要新增成交假设**。

### 16.2 实证（真主连 `continuous` · 容量 5 · 真实成本 = 手续费 + 1 跳/边 ≈ 4.8bp）

| 口径 | 净¥(5bp) | 净¥(13bp) | 净¥(真实1跳) | MDD | MAR | W1 2018–21 | W2 2021–24 | W3 2024–26 |
|---|---|---|---|---|---|---|---|---|
| V3.1 基线（1手） | +42.9万 | +17.9万 | +43.4万 | 16.8万 | 2.58 | −12.6万 | +27.5万 | +30.5万 |
| **P0 斐波汇流（1手）** | +48.4万 | +23.4万 | **+49.2万** | **10.0万** | **4.94** | −5.4万 | +27.6万 | +38.9万 |
| **P0+P1 = V3.2（至多2手）** | +57.2万 | +27.0万 | **+58.2万** | 15.4万 | 3.77 | −7.3万 | +20.0万 | +59.6万 |

- **P0 单独即全面优于 V3.1**：净利 +13%、**回撤 −40%**、MAR +91%，三个滚动窗全部不劣。
- **P0+P1（V3.2，严格保本）**：净利再 +18%（相对 P0）、相对 V3.1 **+34%**、MDD 反而比 V3.1 低 8%；
  但相对"仅 P0"回撤回升、W2 回落到 +20.0 万 —— 这是"加码换取总收益"的代价，属已知取舍。
- 加码单质量：633 笔加码中 118 笔（18.6%）以微亏结束（**主因是主连换月跳空穿越吊灯**，非逻辑破口）；
  最差加码单毛¥ −2.85 万，**整仓保本在结构上成立**。
- **回归可逆**：`fib_confl:false` + `add_max_lots:1` 复现 V3.1 的 18,009 事件 / 17,982 已平仓 / +42.9万，逐位一致。

### 16.3 等价性验证（WB 已完成）

| 检查 | 方法 | 结果 |
|---|---|---|
| 线上引擎 ↔ 回测（V3.1 回归） | 抽出补丁后函数在 50 品种真实小时线上推演 + 同一 replay | ✔ 18,009 事件 / 17,982 已平 / +42.9/+17.9/+43.4万 / MDD 16.8万 全对 |
| 线上引擎 ↔ 回测（P0） | 同上 | ✔ 16,400 / 16,374 / +48.4/+23.4/+49.2万 / MDD 10.0万 全对 |
| 线上引擎 ↔ 回测（V3.2） | 同上（含加码单数 613、手数均值 1.19） | ✔ 全部一致（`_fusion_v32_verify.py`） |
| **同花顺公式 ↔ 引擎（斐波段）** | numpy 复刻 THS 的 `BARSLAST/HHV/LLV/REF` 语义，**469,391 根逐根对照** | ✔ 命中一致率 **100.0000%**（`_ths_v32_check.py`） |
| `py_compile` | `fusion_signal.py` / `config.py` / `scheduler.py` | ✔ 通过 |

### 16.4 CB 交付清单（v2.2）

| # | 事项 | 状态 |
|---|---|---|
| 1 | `app/strategies/fusion_signal.py`：新增斐波汇流门控 + 阶梯加码；`yield` 新增 `first_px` / `lots` | WB 已落补丁 → **待 CB 复核** |
| 2 | `app/core/config.py`：`FusionConfig` 增加 `fib_confl` / `fib_ratios` / `fib_tol_atr` / `add_max_lots` / `add_guard_atr` | WB 已落补丁 |
| 3 | `config/local.yaml` → `fusion.*`：`fib_confl: true` / `add_max_lots: 2` / `add_guard_atr: 0.0` | WB 已配置 |
| 4 | `app/scheduler.py`：**加码事件推送**（手数增加即推，附均价/止损）；`_row_line` 显示 `×N手` | WB 已落补丁 |
| 5 | 数据库迁移：`fusion_position` 新增 `lots INTEGER DEFAULT 1`（已加入 `_MIGRATIONS`） | WB 已落；**随容器启动自动执行** |
| 6 | **重启 `python-api` 容器**（`app/` 为挂载目录，Python 进程不热重载） | ⬜ 待 CB |
| 7 | 同花顺 / 文华 指标公式副本同步（见 §16.5） | WB 已同步（RHX、核对指南、RHH/RHT V32） |
| 8 | 桌面交付：`融合_同花顺_公式2_RHH_小时主图_V32.txt`、`融合_同花顺_公式3_RHT_交易系统_V32.txt` | ✔ WB 已交付 |

### 16.5 指标公式副本同步（v2.2）

| 文件 | 处理 |
|---|---|
| `docs/RHX_融合策略指标公式.txt` | **已同步为 V3.2**（加 `FSPL…FSFOKL/S` 斐波汇流块） |
| `docs/融合策略信号_同花顺核对指南.md` §六 | **已同步为 V3.2** |
| `桌面/融合/融合_同花顺_公式2_RHH_小时主图_V32.txt` | ✔ 新增（含斐波门控 + 加码标记 + 均价线 `AVGLINE`） |
| `桌面/融合/融合_同花顺_公式3_RHT_交易系统_V32.txt` | ✔ 新增（含斐波门控 + `ADDLONG`/`ADDSHORT` 输出） |
| `*_V31.txt` | 保留（V3.1 历史版本） |
| `docs/02_融合引擎与额外策略最终版/*` | 归档保持原样（V2.0 历史版本）；**使用中的副本一律以 V3.2 为准** |

> ✅ **已裁定（2026-09-21 CR-6）**：§15.5 的 `SLK := 1.0` vs 引擎 `sl_atr := 2.0` —— 用户裁定止损 = **2×ATR**，桌面 V32 公式已改为 `SLK := 2.0`（见 §13 #6.2 / §17.9 / §18.2）。
> 此前"图上已平/引擎仍持仓"的不同步风险就此消除；剩余差异仅在加码约束（V3.2 vs V3.4，见 §17.9）。

---

## 17. 变更记录 CR-2026-09-21（v2.3）：融合策略 V3.2 → **V3.4** ★ 定稿口径

> 提出与实证：WB（2026-09-20 ~ 09-21） · **执行：CB（待办）** · 用户指令链：
> 「当前阶段只是讨论更优的交易策略」→「**我决定放弃保本的要求，在应该加码时加码**，这样看看亏损和盈利，是否能接受」
> →「请把当前所有结论记录进 PRD（有益于系统的改善），更新融合策略版本到 V3.4」。
>
> 实证脚本：`桌面/融合/_fusion_v33.py` / `_fusion_v33b.py` / `_fusion_v33c.py` / `_fusion_v33d.py`
> / `_fusion_v34.py` / `_fusion_v34b.py` / `_fusion_v35.py`（结果见同名 `*_result.json` 与 `*_report.txt`）。
> 依据文档：`融合策略_V3.3_加码放开与新增素材_20260920.md`（§一~§四）、`融合策略_V3.4_抖音前两段未测条款与15分钟可行性_20260920.md`（§二~§八）。

### 17.0 版本号说明（★ 避免与讨论期代号混淆）

讨论期曾用两个编号承担不同含义，为免歧义，此处一次性对齐：

| 讨论期代号 | 当时的含义 | 与正式版本号的关系 |
|---|---|---|
| V3.1 | 去收强 + ADX≥15（已上线） | **= 正式 V3.1**（一致） |
| V3.2 | 斐波汇流 + 加码·**结构保本** | **= 正式 V3.2**（一致，线上现状） |
| V3.3（讨论代号） | 斐波汇流 + 加码·**放开保本** ← 讨论中的最优 | **合并入正式 V3.4** |
| V3.4（讨论代号） | 抖音文稿「前两段」未测条款审计批次 | 属**研究批次编号**，不是策略版本；其结论并入 §17.5 总账 |

> **结论：正式策略版本 = V3.4**，口径 = **V3.1 底座 + P0 斐波汇流 + P1 阶梯加码（放开保本）**。
> 本文档后续提到的"V3.4"一律指这个正式口径。

### 17.1 本次改动（仅 1 处，都在执行层的加码条件）

| # | 改动 | 位置 | 参数 |
|---|---|---|---|
| **CR-5** | **放开加码的"结构性保本"约束**，改由**浮盈门槛**门控加码 | 执行层 | `add_guard_atr` 停用；**新增 `add_thr_atr = 1.0`** |

**CR-5 精确实现**（`walk_fusion_states`，多；空镜像）：

```python
# ---- V3.2（结构保本）：加码前必须已"无风险" ----
hh = max(h[entry_i:i]);  avg_new = (avg_px * lots + c[i]) / (lots + 1.0)
trail = peak - trail_atr * e_atr               # 当前吊灯止损
if c[i] > hh and trail >= avg_new - add_guard_atr * e_atr:
    avg_px = avg_new; lots += 1

# ---- V3.4（放开保本）：只看浮盈门槛 ----
if c[i] > hh and (c[i] - first_px) >= lots * add_thr_atr * e_atr:
    avg_px = (avg_px * lots + c[i]) / (lots + 1.0); lots += 1
```

方向层、入场层（含斐波汇流、ADX 门控）、离场层（2×ATR 初始 / 2×ATR 吊灯 / 0.5R 保本 / close-only）、
执行层的其余部分（冷却 3 根 / 单品种至多 1 仓 / 平仓后再开 / 当根收盘成交）**全部不变**。

### 17.2 为什么"必须"放开保本 —— 「保本」四种实现的实测

这是本次最重要的工程结论：**「保本」怎么落，比"要不要保本"更决定成败。**

| 实现 | 机制 | 实测（真实 1 跳） | 判定 |
|---|---|---|---|
| **A** | 均价地板 + **收盘价成交** | +44.0万 / DD 24.4万 / MAR 1.81；**加码单 1069/1819 亏损、最差 R=−23.98** | ❌ **不满足"最差=保本"** |
| **B** | 均价地板 + **挂单价成交** | +103.3万 / DD 12.9万 / MAR 8.02 | ❌ **逻辑自相矛盾、不可执行**（只在收盘破位时按挂单价成交） |
| **C** | 保本止损做成**真正的盘中挂单**（high/low 触及即成交） | **基线自身 −142.1万 / DD 142.9万 / MAR −0.99** | ❌ **系统崩塌** |
| **D** | **结构保本**（V3.2）：吊灯须已在加码后均价之上 | +58.2万 / DD 15.4万 / MAR 3.77 | ⚪ 可行，但**让出 19.6% 收益** |
| **E ★** | **放开保本**（V3.4）：只用浮盈门槛门控 | **+69.6万 / DD 16.8万 / MAR 4.14** | ✅ **采纳** |

> **★★ 强论断（写入口径铁律）**：**「收盘判定」不是这套系统的缺陷，而是它的 alpha 来源。**
> 任何"打到保本价即出"的机制都会被小时线插针反复踹出，只交成本（实现 C 让基线崩到 −142 万即为铁证）。
> 因此**保本只能由已有机制（吊灯）顺带承担，绝不能引入新的盘中成交假设**。
> 用户放弃该要求后（实现 E），多赚 **+11.4 万（+19.6%）**，回撤只多 **1.4 万**，**MAR 反而从 3.77 升到 4.14** —— 这一刀划算。

### 17.3 V3.4 实证全景（真主连 `continuous` · 容量 5 · 2018-01 ~ 2026-09）

**（1）主结果**

| 口径 | 净¥(5bp) | 净¥(13bp) | **净¥(真实1跳)** | MDD | MAR | W1 2018–21 | W2 2021–24 | W3 2024–26 |
|---|---|---|---|---|---|---|---|---|
| V3.1 基线（1手） | +42.9万 | +17.9万 | +43.4万 | 16.8万 | 2.58 | −12.6万 | +27.5万 | +30.5万 |
| P0 斐波汇流（1手） | +48.4万 | +23.4万 | +49.2万 | **10.0万** | **4.94** | −5.4万 | +27.6万 | +38.9万 |
| V3.2（结构保本加码） | +57.2万 | +27.0万 | +58.2万 | 15.4万 | 3.77 | −7.3万 | +20.0万 | +59.6万 |
| **★V3.4（放开加码）** | **+68.1万** | **+30.2万** | **+69.6万** | 16.8万 | **4.14** | −6.6万 | **+38.5万** | +55.7万 |

- 逐笔胜率 23.5%，成交 3,226 笔，加码单 1,583，**手数均值 1.49**，最大 2.0。
- **相对 V3.2**：多赚 **+11.4万（+19.6%）**，DD 只多 1.4万，MAR 反升 ⇒ CR-5 成立。
- **相对 P0（不加码）**：多赚 **+20.4万（+41%）**，代价是 DD 从 10.0万 → 16.8万 ⇒ 这是**"用回撤换收益"的明确取舍**，不是免费改进。

**（2）门槛不敏感 ⇒ 不是调参产物**

| `add_thr_atr` | 真实1跳净¥ | MDD | MAR |
|---|---|---|---|
| 0.5 | +76.2万 | 16.4万 | 4.64 |
| **1.0（采用）** | **+69.6万** | **16.8万** | **4.14** |
| 1.5 | +74.6万 | 16.3万 | 4.58 |
| 加码至 **3 手** | +95.0万 | **24.7万** | 3.85（胜率 19.2%） |

> 三档仅差 ±5 万（在噪声带内）；**3 手明确否决**（DD +47%、MAR 反降、胜率跌破 20%）。

**（3）加码单质量 —— "亏损能否接受"的量化回答**

| 指标 | 值 |
|---|---|
| 加码单笔数 | 1,583 |
| 其中亏损 | **847 笔（53.5%）** |
| **亏损单 R 中位数** | **−0.16**（几乎平手） |
| 盈利单 R 75 / 95 分位 | +2.27 / +7.15 |
| **加码单 R 均值** | **+1.00** |
| **加码单合计** | **+249.8万** |
| **未加码的 1,643 笔合计** | **−180.2万** |

> ⇒ **"一半加码单亏小钱、靠少数大单赚钱"**——这是利弗莫尔加码的结构性特征（不是参数问题）。
> **加码条件本身就是一个极强的过滤器**：收到加码的 1,583 笔贡献了全部净值 +249.8万，而没收到加码的 1,643 笔合计是**亏的**。
> **若不能接受"约一半加码单亏损"，就不应启用加码**（退回 `add_max_lots: 1`）。

**（4）分年（真实1跳，万元，按平仓年份）**

| 2018 | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 | 2025 | 2026(至9月) |
|---|---|---|---|---|---|---|---|---|
| −1.5 | −5.7 | −0.6 | +2.6 | +7.2 | +12.9 | +16.2 | +27.3 | +11.3 |

**（5）尾部风险**

| 指标 | 值 |
|---|---|
| 单笔 1% 分位 | −1.04万 |
| **最差单笔** | **−7.16万**（AP888 换月跳空，R = −23.98，**唯一大额亏损源**，见 §13 #6.3） |
| 最差单月 | −6.60万 |
| 99 个月中为负的月数 | 54（平均月 +0.70万） |
| 真实成本 | ≈ **4.69bp**（手续费 + 1 跳/边） |

### 17.4 ★★ 结构发现：两条支路**都必须保留**（附 4 次实锤的方法学结论）

> 抖音文稿主张「回调是多头换手、是健康换手」——检验时顺带把入场结构拆开，得到一条**与直觉相反**的结论。

**（1）配对归因（同一批已成交交易上按支路分桶，真实 1 跳）**

| 支路 | V3.1 基线 | P0 斐波汇流 | **V3.4** |
|---|---|---|---|
| **回踩 PB** | +35.8万（**82%**） | +41.7万（**85%**） | +61.4万（**88%**） |
| 突破 BK | +5.5万（13%） | +6.2万（13%） | +5.8万（**8%**） |
| 同日双支 PBBK | +2.2万（5%） | +1.3万（3%） | +2.4万（3%） |

**（2）真重跑（唯一变量 = 只留一条支路）**

| 口径 | V3.1 | P0 | V3.4 |
|---|---|---|---|
| **两者都做（= 现状 `both_nm`）** | +43.4万 | +49.2万 | **+69.6万** |
| 只做回踩 | +19.6万 | +20.6万 | +56.8万（**DD 16.8→35.8万**） |
| 只做突破 | **−13.6万** | **−13.6万** | **−8.6万** |

**（3）品种分半（判据③：两半须同号）**

- 回踩 PB：**三体系两半全同向 ✅**（分半差 +4.4 / +18.9 / +30.8 万）
- 突破 BK：**三体系全部反向 ❌**

**（4）★ 反直觉的核心：账面贡献 ≠ 边际贡献**

> 突破支路**账面**只 +5.5万（13%），但**把它删掉，净值掉 23.8 万**。
> 差额来自**容量 5 门控的时序重排**：删掉一个信号，等于把"最多 5 仓"的**名额重新分配**给另一笔排队的交易
> ——而顶上来那批（合计 −18.3万）比它差。即：

```
BK 自身 +5.5万；删掉后名额被更差的交易顶上（−18.3万）
⇒ 净值变化 = +5.5 − (−18.3) = −23.8 万（不升反降）
```

**（5）判定**

> ✅ **现状 `entry_mode = both_nm` 正确，不得改成单支路。**
> · 「只做回踩」❌：净值腰斩，且 V3.4 的 DD 从 16.8万 翻到 35.8万。
> · 「只做突破」❌：**负期望**（MAR −0.64），它单独不成立。
> · **新认识**：`10 根突破` 的真实作用是**在趋势启动期"抢先占位"**、阻挡更差的信号占用容量，
>   而不是"自己赚钱"。**突破支路是"占座的"，不是"赚钱的"。**
> · 唯一例外：**弱市 W1 里"只做回踩"是唯一为正的口径**（+5.6/+3.8/+4.5万），但 W2/W3 都是"两者都做"更优 ⇒ 整体仍应采用 `both_nm`。

### 17.5 ★★ 端到端结论总账（32 项 · 供后续优化决策的唯一索引）

> **32 项编号条目：✅ 已采纳 7 项（含 2 项口径修正）· ❌ 已证伪 21 项 · ⚪ 观察 4 项。**
> **另有 ⏳ 待办 3 项（换月跳空守卫 / 补 min15 / 扩样本，见 §13 #6.3~#6.5，不计入上表编号）。**
> **评估任何新条款前必须先查本表，避免重复劳动。**

**A. ✅ 已采纳（构成 V3.4 的每一项）**

| # | 改进项 | 实测（真实 1 跳） | 引入版本 |
|---|---|---|---|
| 1 | 数据口径锁定 **`continuous`**（= 同花顺真主连） | 50/50 铁证 Δ=0.0；`cont_adj` 为偏移价，弃用 | 口径修正 |
| 2 | **去「收强」**（`use_sbull=false`） | +18.5 → +43.4万 | V3.1 |
| 3 | **ADX(14)≥15 入场门控**（取上一根，无前视） | 显著提升；**≥20 起 13bp 转亏、≥25 双成本全亏** | V3.1 |
| 4 | 方向层用 **EMA140**（不是 MA140 / SMA） | EMA > SMA；K=130 略好但在噪声内 | V3.1 |
| 5 | **P0 斐波那契·汇流**（回踩极值落 0.382/0.5/0.618 ±0.5ATR） | +43.4 → +49.2万；**DD 16.8→10.0万（−40%）；MAR 2.58→4.94** | V3.2 |
| 6 | **P1 利弗莫尔加码至 2 手 · 放开保本**（CR-5） | +49.2 → **+69.6万**；DD 10.0→16.8万；MAR 4.14 | **V3.4** |
| 7 | **真实成本回填**（≈4.8bp，非 13bp） | 推翻此前"策略脆弱"的警报 | 口径修正 |

**B. ❌ 已证伪（有害或无用 —— 勿再试）**

| # | 已证伪项 | 实测 / 原因 |
|---|---|---|
| 8 | 加码至 3 手 | +95.0万但 DD 24.7万（+47%）、MAR 反降 3.85、胜率 19.2% |
| 9 | 加码后"保本地板"（均价 / 首价） | `floor=首价` **与什么都不做逐位等价**；均价地板按收盘成交会让 1069/1819 加码单亏钱 |
| 10 | 真正的"盘中挂单保本" | **基线自身崩到 −142.1万**（见 §17.2 实现 C） |
| 11 | **三K动能止损**（抖音 §5） | **−4.7万**，成交 3,340→4,524（**+35%**），2025 −9.8万 |
| 12 | 结构止损（枢轴点） | −20.3万（与 #11 同源：**结构性止损一律劣于 ATR 止损**） |
| 13 | 突破幅度门槛 ≥X×ATR（抖音 §3） | +41.5（0.25ATR）/ +25.3（0.5ATR）→ 中性偏害 |
| 14 | **方向成熟度过滤 ≥K 根**（抖音 §3） | 表面 +114.6万，实为**噪声**：1.42σ、分半差 95万、无斐波体系反向、配对归因符号相反 |
| 15 | 关键点跟随（4 根 / 0.5R） | +19.7万，但成交翻倍，成本吞掉收益 |
| 16 | 时间回撤过滤 | +16.8万（≈噪声） |
| 17 | 1.272 扩展位"分批"止盈 | 全平版 −18.7万；分批版 +29.0万（仍不如纯吊灯） |
| 18 | 盘中触碰止损（乙方案） | 3 跳滑点即退化 |
| 19 | 「收强」保留 | 去掉更好（已移除） |
| 20 | 抖音其它条款（ATR 止损 / 固定止盈 / 时段过滤等） | 大多有害（详见 `融合策略_抖音策略叠加分析_20260920.md`） |
| 21 | SC 原油集中度治理 | 1 手约束下无法治理（结构性，需扩品种池） |
| 22 | **只做回踩**（抖音 §4） | 净值腰斩（69.6→56.8万），DD 16.8→**35.8万** |
| 23 | **只做突破** | **负期望**（−13.6 / −13.6 / −8.6万，MAR −0.64） |
| 24 | 实体突破前一根高点（抖音 §3） | "实体未突破"的笔也赚钱（39%/13%/11%）→ 过滤只会伤 |
| 25 | **突破幅度中间态 (0,0.35)×ATR 排除**（抖音 §3） | 跨体系**反向**（base +6.2 vs V3.4 −24.6）；曲线锯齿；且亏损实为**加码放大**（桶内 BK −27.2万 / PB 仅 −2.6万） |
| 26 | 外包K线当根不入场（抖音 §3） | 贡献≈0（0–7%），胜率三体系最低，手数加权 R 和为负；分半不过 |
| 27 | 领导股 / 相对强弱做成**时序入场过滤器** | 仅 +6.7万（**正确落点是品种池构建**，不是入场过滤） |
| 28 | 浅回调 / 深度回调过滤 | 无用 |

**C. ⚪ 观察项（不足以稳定改善，留待扩样本）**

| # | 观察项 | 现象 |
|---|---|---|
| 29 | 回调分档（趋势腿内第几次入场） | **第 2 笔入场最赚**（+38.0/+25.9/+21.2万，三体系方向一致）、**第 1 笔（反转后首笔）最差** |
| 30 | 内包K线当根入场 | 斐波/V3.4 为负贡献（−2.4 / −5.6万），但仅 90 笔（2.8%） |
| 31 | 「进场要快速脱离成本区」 | **不可机械化**（需要未来信息） |
| 32 | 领导股 / 相对强弱作为**品种池筛选** | 方向正确但未落地（见 #27） |

**D. ⏳ 待办（见 §13 #6.3~#6.6）**：换月跳空**数据/执行面**修复 ★★（策略层守卫已证伪 #42）/ 补 min15 数据 ★★ / 扩样本 ★★ / 方法学加固（§18.1）★★

**E. V3.6/V3.7 补充证伪（#33–#42，总账 32 → 42 项 · 完整证据见研究侧《总索引》§4/§12/§14 与 `融合策略_V3.6/V3.7` 明细文档）**

| # | 已证伪项 | 实测 / 原因 |
|---|---|---|
| 33–40 | **V3.6 抖音交易系统3 八个"入场质量"过滤**（三均线排列 / FVG 三K缺口 / 关键位触碰次数 / 楔形三推递减 / MACD 零轴同向 / KDJ 超卖金叉 / 结构未破坏 / 放宽斐波容差） | 后验归因多数"看似有用"，做成引擎内硬过滤（allow 掩码真重跑、含容量 5 门控重排）**全部变差或崩塌**（如 MACD 零轴：后验 +60 万 → 真重跑 v34 +26.7/base +15.0）—— 第 5~6 次实锤"重跑≠归因" |
| 41 | **E1 组合级复利手数**（每赚 10 万 +1 手·上限 3；S7 账面 +118.1 万 / MAR 5.85） | 压力测试全挂：参数平台断裂（20 万档崩至 +32.9 万，比固定 1 手还差）、品种分半反向、增量 76% 集中 2026 一年、3 品种（SC/AU/AG）占增量 >100%、Bootstrap 95%CI 含 0（p=17.9%）—— 是对 2024–2026 原油/贵金属大趋势的**暴露放大器**，不是新 alpha |
| 42 | **换月跳空守卫（策略层）** | 跨跳空根 139 笔毛 ¥ **+64.5 万为正**（顺跳空 +98.7 万 > 逆跳空 −34.2 万）→ "消除跳空暴露"目标本身不成立；G2(3×) 强平 1,020 笔 → −28.9 万毁灭性、阈值 3→5→8 响应非单调；最差单笔（AP888 −63.32R）的出场根就是跳空根本身，收盘判定体系已在跳空根收盘自动平仓 → **改数据/执行面修复（§18.1 #8）** |

> ★ **总账的用法**：这张表是"投入产出比"最高的一页。任何新想法先在此表里查是否已被证伪；
> 任何新测试必须按 §17.6 的方法学（配对归因 + 三件套判据）执行，否则结果不可信。



### 17.6 ★★ 元发现：靠「加过滤器」优化已经走到头

净值被**极少数交易**主导（真实 1 跳）：

| 口径 | Top5 笔贡献 | Top20 笔贡献 |
|---|---|---|
| V3.1 基线 | 61% | 164% |
| P0 斐波汇流 | 69% | 165% |
| **★V3.4** | **91%** | **205%** |

> （>100% 意味着：其余约 3,200 笔交易**合计是亏的**。）

结合 §17.4（4）的容量门控机制 —— **每删掉一个信号，都会把"最多 5 仓"的名额重新分配给另一笔交易** ——
于是**参数微调的 ±50 万波动全在噪声里**，无法用"净利更高"证明某个过滤器有效。
趋势跟随的肥尾特征是天生的，但它意味着：

> **★ 结论：在 50 品种 / 9 年 / 3,226 笔的样本上，已经无法再用"加过滤器"取得可信的边际改进。**
> 优化方向必须从「加过滤器」转向：① **修漏洞**（换月跳空守卫）② **扩样本**（品种池 / min15）③ **执行面**（滑点 / 换月 / 资金规模）。

### 17.7 方法学铁律（本轮第 4 次实锤 —— 评估任何新条款前必读）

| # | 铁律 | 依据（本轮实证） |
|---|---|---|
| 1 | **配对归因 > 重跑链路**：评估任何"过滤器"必须在**同一批已成交交易**上按标志分桶看贡献 | 突破支路账面 +5.5万，**删掉却让净值掉 23.8万** → 重跑会被容量 5 门控重排污染 |
| 2 | **判据三件套（缺一不可）**：① 参数曲线是否单调/成平台（算 μ、σ；"最优"仅 ≈1.4σ 而 n 次抽样最大值期望本就 ≈1.5σ ⇒ **纯噪声**）② **跨体系同向**（base/fib/V3.4 至少 3 个体系同号）③ **品种分半同号** | 方向成熟度过滤：1.42σ + 分半差 95万 + 无斐波体系反向 → 一票否决 |
| 3 | **框架保真度审计**：证伪任何经典框架（利弗莫尔/斐波那契）前，先审计自己的机械化是否忠实于原始定义 | 否则只证伪了自己造的稻草人（本轮曾因此两次自我纠错） |
| 4 | **成交假设决定结论**：同一规则换"收盘成交 / 挂单成交 / 盘中挂单"可让结果正负翻转 | §17.2：同一「保本」规则 +44万 → +103万 → **−142万** |
| 5 | **变手数记账**：`R` 必须是"每手 R"，手数由 `lots` 单独承载；**PF 若按每手 R 统计，对多手变体不可比** | 多手变体一律以净¥与 DD 为准 |
| 6 | **参数"不敏感"才是好事**：三档相差 ±5 万 ⇒ 不是调参调出来的；只有单点好、相邻崩 = 噪声 | §17.3（2）加码门槛三档 |
| 7 | **配对归因的适用边界**：入场过滤 → **只能**配对归因（重跑必被容量门控污染）；出场类 → 两法皆可；**仓位/手数类 → 必须真重跑**（改变路径） | V3.6 八个入场过滤真重跑全崩；V3.7 E1 复利手数必须真重跑 |
| 8 | **多重检验诚实声明**：累计 40+ 次独立检验未做 Bonferroni/FDR 校正；判据三件套的"1.4σ vs 期望最大 1.5σ"本身就是 expected-max 启发式 → **任何新结论必须报出累计检验次数与校正后显著性**，回测输出默认带统计加固包（§18.1 #2） | 《方法论优化.txt》评审 #2 采纳 |

### 17.8 CB 交付清单（v2.3 · V3.2 → V3.4）

| # | 事项 | 状态 |
|---|---|---|
| 1 | `app/strategies/fusion_signal.py`：加码条件改为 `c[i] > hh` **且** `(c[i] - first_px) >= lots * add_thr_atr * e_atr`（去掉 `trail >= avg_new - add_guard_atr*e_atr`） | ⬜ 待 CB |
| 2 | `app/core/config.py`：`FusionConfig` 新增 `add_thr_atr: float = 1.0`；`add_guard_atr` 保留字段但 V3.4 不再参与判定 | ⬜ 待 CB |
| 3 | `config/local.yaml` → `fusion.*`：新增 `add_thr_atr: 1.0`（`add_max_lots: 2` / `fib_confl: true` 不变） | ⬜ 待 CB |
| 4 | **重启 `python-api` 容器**（`app/` 为挂载目录，Python 进程不热重载） | ⬜ 待 CB |
| 5 | **等价性验证**：线上引擎 ↔ 回测（V3.4 口径应为 **16,400 事件 / 3,226 成交 / +69.6万 / DD 16.8万 / 加码单 1,583 / 手数均值 1.49**） | ⬜ 待 CB（WB 提供对照脚本） |
| 6 | `py_compile`（`fusion_signal.py` / `config.py` / `scheduler.py`）+ `local.yaml` 解析 | ⬜ 待 CB |
| 7 | 同花顺 / 文华 指标公式副本同步 | ⬜ 待 CB + 用户（见 §17.9） |
| 8 | 推送层：**加码事件推送**（手数增加即推，附均价/止损）与持仓行 `×N手` 已在 V3.2 交付，**V3.4 无需再改** | ✔ 沿用 |

> **回退路径（两个开关，一行为单位）**：
> · `add_max_lots: 1` → 逐位回到 **P0**（+49.2万 / DD 10.0万 / MAR 4.94）；
> · 再 `fib_confl: false` → 回到 **V3.1**（+43.4万 / DD 16.8万 / MAR 2.58）。

### 17.9 指标公式副本同步状态（v2.4 更新）

| 文件 | 状态 |
|---|---|
| `桌面/融合/融合_同花顺_公式2_RHH_小时主图_V32.txt`、`…公式3_RHT_交易系统_V32.txt` | ✅ **`SLK := 2.0` 已由 WB 改定（2026-09-21，对齐引擎 `sl_atr=2.0`，grep 复核）**；加码语义仍为 V3.2 结构保本 → 待 CB 产出 **V34 版**（见 §18.1 #9） |
| `docs/RHX_融合策略指标公式.txt` | 方向/入场层公式（无止损与加码行）→ 不受 SLK 影响，无需改 |
| `docs/融合策略信号_同花顺核对指南.md` §六 | ⚠️ 仍为 V3.2（SLK=1.0 旧说明）→ 待 CB 随 V34 公式一并同步 |
| `*_V31.txt` / `*_V32.txt` | 保留（历史版本；V32 现含 SLK=2.0 修订） |

> ⚠️ 线上**引擎**仍停在 V3.2（与 V3.4 的差异**只在加码约束**：V3.2 要求"吊灯已推进到加码后均价之上"，V3.4 只看浮盈门槛）。
> 在同步完成前，引擎推送的加码时点会比 V3.4 口径偏晚、加码单偏少（回测口径 V3.2 = 613 笔 vs V3.4 = 1,583 笔）。⚠️ 交付前必须知晓。

#### 17.9.1 公式 ↔ 引擎 三方一致性审计结论（2026-09-21 WB 全量核对）

**已核对一致**（逐参数逐语义）：方向层 EMA140 取上一根（`DIRP`/`FSHTF` ↔ `htf_dir`）；回踩重启（`XUP+L≤EMA20`）；10 根突破不含当根（`REF(HHV(HIGH,10),1)` ↔ `max(h[i-10:i])`）；斐波汇流（0.382/0.5/0.618 ±0.5×ATR、W=60、本根右侧确认，仅回踩支路，V3.2 曾 469,391 根逐位核对）；ADX(14)≥15 用上一根；离场三件套（初始=加权均价∓2×ATR、吊灯=收盘极值∓2×ATR 含当根、0.5R 保本基准=**首次买入价**、三取最紧、close-only）；V3.2 加码语义（`add_guard_atr=0` 下 engine `trail≥avg_new` ↔ THS `(HIB−TRK×EAB)≥AVNL` 逐位等价）；config `local.yaml fusion.*` 全部参数与 PRD §6.7 / 公式常量一致。

**本轮修复（WB 已改公式并复核）**：
| # | 差异 | 修复 |
|---|---|---|
| 1 | `SLK := 1.0` vs 引擎 `sl_atr=2.0`（挂起自 V3.1） | ✅ 两份 V32 公式改 `SLK := 2.0` |
| 2 | **公式3 内建「连亏 3 次冷却 120 根」（N4/LSCNT/B3/COOLOK）**——引擎、回测 bt、PRD §6 均无此规则；系 V2.0 时代把**组合层纪律 R8（连亏暂停）**误编进单品种公式 → 图上会比引擎**少给信号** | ✅ 已删除，`ENTERLONG: BK0` / `ENTERSHORT: SK0`；R8 保留为**组合层人工纪律**（见实盘操作手册），不进单品种公式 |
| 3 | 公式无预热门：引擎入场要求 `i≥30` 且 ADX 前 2n 根强制 0，图上**历史最前段**可能多出引擎没有的信号 | ✅ 两份公式 `BK0/SK0` 增加 `AND BARSCOUNT(CLOSE) > 30` |

**已知残余差异（公式语言限制，无法内建；以引擎推送为准）**：
| # | 差异 | 影响 |
|---|---|---|
| A | 公式**不含 `cooldown_bars=3`**：引擎平仓后第 3 根才接受新信号；公式状态机锚定 `BARSLAST(BK0)`，若把冷却门加在 `BK0` 上会与 `SP0→NB→BK0` 形成**环形依赖**，同花顺编译不过 | 平仓后 3 根内若再出 BK0/SK0，图上开仓/持仓线会**早于引擎 1~2 根**；实盘以引擎推送为准（RHX 入场箭头同此，核对指南已注明） |
| B | 公式无手数概念：加码后成本线 `AVGL` 按 2 手加权均价绘制，但图上无法表达"手数变 2"与 V3.4 的 `lots×1.0×ATR` 门槛（门槛随手数线性抬升） | 加码展示为事件标记；门槛语义以引擎为准（V34 公式按 lots=1 时门槛 `1.0×EAB` 实现，即 `§18.1 #9`） |
| C | 引擎实时评估另有 `min_bars=160` 数据量门槛 / `settle_delay_sec=150s` 定稿守卫 / 单一 `hourly_src` 口径 | 图表数据口径与引擎采集口径若有差异（数据源标签），信号时点可能错根；以引擎为准 |

### 17.10 裁定状态（v2.4 · 原「仍未裁定」三项全部落定）

1. ~~同花顺 `SLK := 1.0` vs 引擎 `sl_atr := 2.0`~~ → **✅ 已裁定 = 2×ATR，公式已改**（2026-09-21，见 §13 #6.2 / §17.9）。
2. ~~换月跳空守卫的具体口径~~ → **✅ 策略层不做（#42 证伪）**，改数据/执行面修复（§18.1 #8，先出方案经用户裁定）。
3. ~~加码强度是否长期停在 2 手~~ → **✅ 手数维度关闭**（#8 单笔 3 手 ❌ / #40 保本地板 ❌ / #41 组合级复利 ❌），长期维持 **V3.4 每笔加码 2 手**；未来若要降风险，唯一旋钮是 `add_thr_atr`（须接受净利下调）。
4. **新待办**（全部转入 §18.1 CB 交付清单）：walk-forward OOS ★★ / 统计加固包 ★★ / 基准对照 ★ / 容量梯度 ★ / 品种池透明度 ★ / 边界单元测试 ★ / VWAP 对照 ★ / 换月数据面方案 ★★ / min15 补数 / 扩样本。

---

## 18. 变更记录 CR-6（2026-09-21）：方法学加固落地 + SLK 裁定 + V3.6/V3.7 结论同步

> 提出：WB（2026-09-21）· **执行：CB** · 用户指令：「将建议采纳的项落地到 PRD，由我要求 CB 进行调整；同花顺止损按 2×ATR」
> 依据：《方法论优化.txt》评审（13 项：9 采纳 / 4 部分采纳）、V3.6（抖音交易系统3 条款验证）、V3.7（E1 复利手数压力测试 + 换月跳空守卫验证）、研究侧《总索引》§13/§14。

### 18.1 CB 交付清单（CR-6 · 回测/研究模块加固 + 遗留同步）

| # | 事项 | 要求 / 验收标准 | 优先级 |
|---|---|---|---|
| 1 | **Walk-forward 样本外验证** | 回测模块新增滚动 训练/验证 切分（建议 3 年训练 / 1 年验证、步长 1 年），报告 **OOS 衰减比**（OOS 净利 / IS 净利）；**判据：衰减比 ≥50% 维持 V3.4 定稿，深度衰减回退 P0**（`fib_confl:true + add_max_lots:1`） | ★★ |
| 2 | **统计加固包（回测输出规范）** | 每份回测报告默认输出：品种层 **Bootstrap 95%CI 与 p(≤0)**、置换检验 p 值、**多重检验声明**（累计检验次数 + Bonferroni/FDR 或 expected-max 校正）、CVaR(5%)、最长回撤时长 | ★★ |
| 3 | **基准对照行** | 每份回测报告必含 **买入持有（B&H）** 与 **MA 交叉** 两条基准行（同成本口径、同品种池） | ★ |
| 4 | **容量梯度** | `max_pos ∈ {3, 5, 7, 10}` 一键扫描（V2.0 已测 3/5 仓，补齐 V3.4 口径四档） | ★ |
| 5 | **品种池透明度** | 回测报告附品种池清单 + 各品种上市时间；标注**幸存者偏差审计**结论（已退市品种的处理方式） | ★ |
| 6 | **边界单元测试** | pytest 覆盖：加码×止损/吊灯/保本位联动边界（含 V3.4 放开保本新逻辑）、入场当日即触发离场、ATR 极值、跨换月跳空开盘等 | ★ |
| 7 | **VWAP 成交假设对照** | 收盘成交 vs VWAP 成交 双口径对照报告（执行面量化；此前仅有次根开盘 ≈ −0.5% 的估计） | ★ |
| 8 | **换月跳空数据/执行面修复方案** | 策略层守卫已证伪（#42）**不做**；改为出**方案文档**：真实合约拼接 vs 移仓按新旧合约价差结算 的可行性对比，经用户裁定后实施 | ★★ |
| 9 | **同花顺公式 V34 版** | 基于桌面 V32 公式（**SLK:=2.0 已改**）产出 V34：`ADDL0 := NB >= 1 AND CLOSE > HBH AND (CLOSE - EPB) >= 1.0 * EAB;`（`ADDS0` 对称改 `(EPS - CLOSE) >= 1.0 * EAS`），删除结构保本语义（`AVNL/AVNS` 均价仅保留展示）；同步《核对指南》§六；文华 6 版同步 | ★★ |
| 10 | §17.8 原有交付（V3.4 引擎代码 + `add_thr_atr` + 重启容器 + 等价性验证） | 见 §17.8 #1–#6（等价性锚点：16,400 事件 / 3,226 成交 / +69.6万 / DD 16.8万 / 加码 1,583 / 手数均值 1.49） | ★★★ |

> **执行顺序建议**：#10（V3.4 引擎落地）→ #1/#2（加固包，直接决定 V3.4 是否维持定稿）→ #8/#9 → 其余。
> ⚠️ 统计加固提升的是**结论置信度**，不改变行动结论——§17.6「别再加过滤器」依然成立；主线下仍是修漏洞 / 扩样本 / 落地 V3.4。

### 18.2 已执行项（WB，2026-09-21）

| 事项 | 结果 |
|---|---|
| 同花顺公式 `SLK := 2.0` | 桌面 `融合_同花顺_公式2_RHH_小时主图_V32.txt` / `…公式3_RHT_交易系统_V32.txt` 已改并 grep 复核 ✅（对齐引擎 `sl_atr=2.0`，消除"图上已平、引擎仍持仓"） |
| PRD 升版 v2.4 | §13 #6.1–#6.6 更新；§17.5 总账扩至 **42 项**（+E 块）；§17.7 铁律补 #7/#8；§17.9/§17.10 裁定状态更新；本 §18 新增 |

---

## 19. 变更记录 CR-7（2026-09-21）：策略优化方向评审 —— 「可编程可量化」条款的落地裁定

> 提出：WB（2026-09-21）· 用户指令：「读《交易策略优化方向建议.md》，看哪些方向可以落地到 PRD，即可编程可量化的」
> 依据：该文档 6 大方向 21 项条款，**逐条对照 §17.5 总账（42 项）+ §17.7 方法论铁律 + 数据可行性**。
> **裁定原则**：① 未实测的方向级判断**不新增总账编号**，只做"预判定性"（依据 = 与已证伪家族同构）；② 与已证伪项同构的，标注"同族已证伪"并给出解禁条件；③ 真正落地的研究项一律要求**真重跑评估**（铁律 #7：仓位/组合类不能只做配对归因）。

### 19.1 六大方向裁定总表

| 方向（建议文档） | 条款 | 裁定 | 依据 |
|---|---|---|---|
| 三 离场管理 | 3.1/3.4 波动率自适应止盈/止损 | ⚠️ **仅允许"放宽侧"实验** | 收紧侧与已证伪家族同构：#12 三K紧持有 −92.9万、#13 固定止盈 −18.7万、分批止盈 +29.0万 < 纯吊灯（#9 相关）。**铁律：任何收紧出场 = 摧毁"让利润奔跑"**。只可测高波状态 2→2.5/3×ATR 放宽，预期证伪、成本低，列 P3 |
| 三 离场管理 | 3.2 时间衰减出场（持仓上限强平） | ❌ 勿做 | 与"截盈放亏"哲学直接冲突；时间类过滤已证伪无效（#26 族）。解禁条件：无 |
| 三 离场管理 | 3.3 分批离场 Scale-out | ❌ **已实测证伪，勿重复** | 固定止盈 −18.7万；分批止盈 +29.0万 仍不如纯吊灯（总账在案） |
| 三 离场管理 | 3.5 换月跳空专项（减仓/回避/加宽止损带） | ❌ 策略层勿做（**#42 已实测**） | V3.7 实测：跨跳空交易毛 ¥ **+64.5万为正**（顺跳空 +98.7万 > 逆跳空 −34.2万）；G2(3×) 强平 1,020 笔 → −28.9万。其中"**跳空成本建模**"✅ 已在 §18.1 #8（数据/执行面） |
| 四 品种配置 | 4.1 动态品种池（流动性/波动率/趋势强度评分） | ✅ **落地（研究项 #11）** | 与总账 #27 裁定一致："领导股/相对强弱正确落点是品种池构建"；可编程（月度评分取前 30–40）；同时服务 §13 #6.5 扩样本 |
| 四 品种配置 | 4.2 板块轮动 | ⚠️ 降级为 #11 的评分因子 | 独立板块动量轮动 = 时序过滤器家族（多次证伪）；仅作为品种池评分弱权重构成，不独立做 |
| 四 品种配置 | 4.3 相关性管理 / 板块暴露上限 | ✅ **落地（研究项 #12）** | 组合层风险约束，未测过、独立于信号逻辑；**必须真重跑**（铁律 #7：仓位类不能配对归因）；注意容量 5 门控污染 |
| 四 品种配置 | 4.4 品种级参数优化 | ❌ 勿做 | 元发现（§17.6）：参数 ±50万 全在噪声；品种级样本更薄、过拟合放大。解禁条件：扩样本 + walk-forward 通过之后 |
| 五 资金管理 | 5.1/5.3 波动率调整仓位（风险平价）/ 组合风险预算 | ✅ **落地（研究项 #13）** | 未测过的组合层方向；变手数按铁律 #5 记账（R=每手R、手数单独承载）、必须报成交假设（铁律 #6） |
| 五 资金管理 | 5.2 凯利公式动态仓位 | ❌ 预判勿做 | 与 **#41 组合级复利手数同族**（V3.7 刚证伪：参数平台断裂 / 分半反向 / 增量 76% 集中 2026）；本策略利润 91% 集中 Top5 笔、低胜率肥尾，滚动凯利估计极不稳定。解禁条件：#13 波动率平价先测出正结果 |
| 五 资金管理 | 5.4 回撤控制机制（阶梯降仓） | ✅ **落地（并入研究项 #13）** | 可编程、组合层、未测；⚠️ 预期风险须报：本策略大利润在回撤修复段（W1 弱市仅微利），降仓可能砍掉反弹利润 → 必须输出"DD 改善 vs 收益侵蚀"对照 |
| 五 资金管理 | 5.5 加码策略优化（ADX>25 加码 / 动态比例 / 时间窗口 / 总仓上限） | ❌ 勿做 | 几乎全部同族已证伪：3手 #8、地板 #40、复利 #41；"ADX>25 才加码"与 **ADX≥20 起转亏**同族；时间窗口加码 = 收紧类 |
| 六 基本面量价 | 6.1 期限结构过滤 | ⛔ **数据受阻** | 库内仅主连 `continuous`，无多月份合约价 → 先补数据源（列入 §13 #6.5 扩样本配套），否则不可编程 |
| 六 基本面量价 | 6.2 持仓量 OI / 6.3 成交量确认 | ⚠️ **数据待探针 → P3** | 需先确认 `fut_kline` 是否含 volume/oi 字段（`fetch_fdf.py` 入库范围）；若有，可编程做突破放量确认，但属"加过滤器"（预判谨慎，须过铁律 #7/#8） |
| 六 基本面量价 | 6.4 季节性因子 | ❌ 优先级不足 | 50 品种 × 9 年分月统计功效不够；且属过滤器家族 |
| 六 基本面量价 | 6.5 跨品种价差信号 | ⛔ 不做 | 属独立价差策略而非本系统增强 + 数据不足 |
| 七 宏观 regime | 7.1/7.2 状态识别与参数切换 | ❌ 勿做 | ADX 门控已采纳（≥15）且 **≥20 即转亏**（#22 族）→ 更细 regime = 更多过滤器，与元发现冲突；HMM 过拟合风险高 |
| 七 宏观 regime | 7.3 空仓机制 | ❌ 勾回撤过滤族 | 组合层空仓 ≈ 时间/回撤过滤（已证伪无效）；降仓版保留在 #13 |
| 七 宏观 regime | 7.4 跨周期配置 | ⛔ **数据受阻** | `min15` 仅 FG 有数据（1.8 年）；待补全（§13 #6.4）后才可测 |
| 八 执行成本 | 8.1 滑点建模 / 8.3 全成本韧性 | ✅ **落地（并入交付项 #14）** | 成本三档（5bp/13bp/真1跳）已做；补"成本 ×1.5/×2 韧性 + 波动率依赖滑点"作为加固包附加输出 |
| 八 执行成本 | 8.2 成交时机优化 | ✅ 已在 §18.1 #7 | VWAP 对照已列入交付 |
| 九 路线图 | walk-forward 样本外 | ✅ 已在 §18.1 #1（置顶） | 与建议文档"必经之路"结论一致 |

> **汇总**：✅ 落地 5 项（#11 动态品种池、#12 相关性约束、#13 资金管理包、#14 成本韧性、放宽侧离场 P3 可选）｜❌ 勿做 9 项（同族已证伪/预判）｜⛔ 数据受阻 3 项（期限结构 / min15 / 量价字段待探针）｜已在交付清单 3 项（#1/#7/#8）。**本轮不新增总账编号**（均未实测）。

### 19.2 新增 CB 研究项（追加进 §18.1 交付清单）

| # | 事项 | 要求 / 验收标准 | 优先级 |
|---|---|---|---|
| 11 | **动态品种池健康度评分（研究项）** | 月度评分 = 流动性（成交量/持仓量 vs 60 日均值）+ 波动率（ATR 百分位上下限）+ 趋势强度（ADX/斜率），取前 30–40 进入交易池；**对照实验：静态池 vs 动态池（信号逻辑不变）**；与 §13 #6.5 扩样本、总账 #27 裁定衔接 | ★★ |
| 12 | **相关性 / 板块暴露约束（组合层）** | 60 日滚动收益相关矩阵；新开仓与现有持仓相关性 >0.7 → 降仓或跳过；单板块风险敞口上限（如 40%）；**必须真重跑评估**（铁律 #7），报告容量 5 门控重排影响 | ★★ |
| 13 | **资金管理研究包** | ① 波动率平价仓位 vs 等权 vs 夏普加权三档；② 回撤阶梯降仓 + 恢复阶梯（如回撤 5% 减半 / 10% 减至 1/4 / 15% 暂停，恢复按 50% 回撤幅度阶梯复位）；**变手数按铁律 #5 记账、报成交假设（铁律 #6）**；输出"DD 改善 vs 收益侵蚀"对照与分年表 | ★★ |
| 14 | **成本韧性测试（加固包附加输出）** | 成本 ×1.5 / ×2 韧性对照（若高成本下仍盈利则 alpha 质量达标）+ 波动率依赖滑点模型（滑点 ∝ 品种波动率） | ★ |
| 15 | （P3 可选）**离场"放宽侧"唯一实验** | 仅允许：高波动状态（ATR 比率 > 阈值）时初始/吊灯倍数 2→2.5/3×ATR；**禁止任何收紧侧变体**（铁律）；预期证伪、成本一口径跑完即弃 | ⚪ |

> **执行顺序**：仍在 §18.1 建议顺序之后（#10 → #1/#2 → #8/#9 → 其余 → 本节 #11–#14）。**研究项全部在 V3.4 引擎落地 + walk-forward 通过之后执行**——若 OOS 衰减比 <50%，先回退 P0 再谈方向扩展。
> **数据探针先行**：CB 在做 #11–#14 前先出一份 `fut_kline` 字段探针报告（是否含 volume/oi、多月份合约价可行性），决定 6.2/6.3 与期限结构是否解锁。

### 19.3 预判勿做清单（防重复劳动 · 解禁需先过铁律）

时间衰减出场 ｜ 分批离场（已实测证伪）｜ 换月跳空策略层处理（#42 已实测证伪）｜ 品种级参数优化 ｜ 凯利动态仓位 ｜ 加码优化全家族（3手/地板/复利/ADX>25/时间窗口）｜ regime 切换与空仓机制 ｜ 季节性因子 ｜ 跨品种价差 ｜ 收紧侧离场变体（铁律级禁令）。

> ⚠️ 本节"预判勿做"均**未实测**（依据 = 与已证伪家族同构 + 元发现），故**不占用总账编号**；若未来样本扩大（扩品种池 + min15 + walk-forward 通过）后要解禁任一项，须按 §17.7 铁律 #7/#8 重新立项评估。

---

*本 PRD v2.0.0（2026-09-16 统一版）合并自 CB 预测系统 PRD v1.3.5 与 WB 融合/信号/回测补充；预测引擎深度模型细节沿用旧 PRD §5/§16/§17/§18，本版补齐交易策略判定、信号推送、统一数据标准（含小时线逐根增量更新）、回测与研究模块。*

*本 PRD v2.1（2026-09-20）：§6 升级为融合策略 **V3.1**（取消入场「收强」+ 新增 ADX(14)≥15 门控）；新增 §15 变更记录（CR-2026-09-20）；§14.2 回测口径修订为 `continuous`；§13 新增 #4.6 / #6.1。*

*本 PRD v2.2（2026-09-20）：§6 升级为融合策略 **V3.2**（P0 斐波那契·汇流 + P1 利弗莫尔·阶梯加码·结构保本）；
新增 §16 变更记录（CR-3 / CR-4）与 CB 交付清单；§7 推送层补充「加码事件」。*

*本 PRD v2.3（2026-09-21）：§6 升级为融合策略 **V3.4**（CR-5：**放开加码的保本约束**，改由浮盈门槛 `add_thr_atr=1.0` 门控）；
新增 **§17 变更记录**（含「保本」四种实现的实测对比、**两条支路都必须保留**的结构发现、
**端到端 32 项结论总账**、**元发现「加过滤器已到头」**、方法学铁律 6 条、CB 交付清单）；
§13 更新 #6.1~#6.5（V3.4 定稿 / SLK 差异 / 换月跳空守卫 / min15 数据 / 扩样本）；
§1 项目概述同步为 V3.4。**按用户指令本次「只改 PRD」，引擎与同花顺公式仍停在 V3.2（差异仅在加码约束，见 §17.9）。***

*本 PRD v2.4（2026-09-21，CR-6）：**方法学评审采纳项落地**（新增 §18.1 CB 交付清单：walk-forward OOS / 统计加固包 / 基准对照 / 容量梯度 / 品种池透明度 / 边界单元测试 / VWAP 对照 / 换月数据面方案 / 同花顺 V34 公式）；
**SLK 裁定 = 2×ATR 并已改公式**（§13 #6.2 / §17.9 / §18.2）；**换月跳空守卫策略层证伪（#42）→ 改数据/执行面**、**加码强度裁定维持 2 手**（§17.10 三项全部落定）；
§17.5 总账扩至 **42 项**（+E 块 #33–#42）、§17.7 铁律补 #7/#8。同花顺公式已由 WB 改 `SLK:=2.0`，**引擎仍待 CB 按 §17.8/§18.1 落地**。*

*本 PRD v2.5（2026-09-21，CR-7）：**《交易策略优化方向建议》六大方向 21 项条款评审落地**（新增 **§19**）——
✅ 可编程可量化落地 5 项（**#11 动态品种池健康度评分 / #12 相关性与板块暴露约束 / #13 资金管理研究包（波动率平价 + 回撤阶梯降仓）/ #14 成本韧性 / #15 离场放宽侧 P3 可选**，追加进 §18.1）；
❌ 预判勿做 9 项（与总账已证伪家族同构：时间衰减出场 / 分批离场 / 换月跳空策略层 / 品种级参数 / 凯利 / 加码优化全家族 / regime 切换与空仓 / 季节性 / 收紧侧离场——**不占总账编号**，解禁须过铁律 #7/#8）；
⛔ 数据受阻 3 项（期限结构需多合约数据 / min15 仅 FG / 量价字段待探针）。
**执行顺序不变：V3.4 引擎落地 + walk-forward 通过之后，才启动 #11–#14。***
