FSDIR := IF(CLOSE > EMA(CLOSE, 140), 1, 0 - 1);
FSHTF := REF(FSDIR, 1);
FSE20 := EMA(CLOSE, 20);
FSUP := CLOSE > FSE20 AND REF(CLOSE, 1) < REF(FSE20, 1);
FSDN := CLOSE < FSE20 AND REF(CLOSE, 1) > REF(FSE20, 1);
FSTL := LOW <= FSE20;
FSTS := HIGH >= FSE20;
FSKL := CLOSE > REF(HHV(HIGH, 10), 1) AND CLOSE > OPEN;
FSKS := CLOSE < REF(LLV(LOW, 10), 1) AND CLOSE < OPEN;
FSKB := FSKL AND REF(FSKL, 1) = 0;
FSSK := FSKS AND REF(FSKS, 1) = 0;
FSTR := MAX(MAX(HIGH - LOW, ABS(REF(CLOSE, 1) - HIGH)), ABS(REF(CLOSE, 1) - LOW));
FSHD := HIGH - REF(HIGH, 1);
FSLD := REF(LOW, 1) - LOW;
FSPD := IF(FSHD > FSLD AND FSHD > 0, FSHD, 0);
FSMD := IF(FSLD > FSHD AND FSLD > 0, FSLD, 0);
FSAT := EMA(FSTR, 14);
FSPP := EMA(FSPD, 14);
FSMM := EMA(FSMD, 14);
FSDI := IF(FSAT > 0, 100 * FSPP / FSAT, 0);
FSMI := IF(FSAT > 0, 100 * FSMM / FSAT, 0);
FSDX := IF(FSDI + FSMI > 0, 100 * ABS(FSDI - FSMI) / (FSDI + FSMI), 0);
FSAD := EMA(FSDX, 14);
FSOK := REF(FSAD, 1) >= 15.0;
FSA14 := SMA(FSTR, 14, 1);
FSPL := REF(LOW, 1) < REF(LOW, 2) AND REF(LOW, 1) < LOW;
FSPH := REF(HIGH, 1) > REF(HIGH, 2) AND REF(HIGH, 1) > HIGH;
FSNL := BARSLAST(FSPL);
FSNH := BARSLAST(FSPH);
FSOQL := FSNL >= 1 AND FSNL <= 59;
FSOQH := FSNH >= 1 AND FSNH <= 59;
FSSHL := HHV(HIGH, FSNL + 2);
FSBTL := REF(LOW, FSNL + 1);
FSLEGL := FSSHL - FSBTL;
FSSLW := LLV(LOW, FSNH + 2);
FSHTP := REF(HIGH, FSNH + 1);
FSLEGS := FSHTP - FSSLW;
FSFTOL := 0.5 * FSA14;
FSFOKL := FSOQL AND FSLEGL > 0 AND (ABS(LOW - (FSSHL - 0.382 * FSLEGL)) <= FSFTOL OR ABS(LOW - (FSSHL - 0.5 * FSLEGL)) <= FSFTOL OR ABS(LOW - (FSSHL - 0.618 * FSLEGL)) <= FSFTOL);
FSFOKS := FSOQH AND FSLEGS > 0 AND (ABS(HIGH - (FSSLW + 0.382 * FSLEGS)) <= FSFTOL OR ABS(HIGH - (FSSLW + 0.5 * FSLEGS)) <= FSFTOL OR ABS(HIGH - (FSSLW + 0.618 * FSLEGS)) <= FSFTOL);
FSLON := FSHTF >= 0 AND ((FSUP AND FSTL AND FSFOKL) OR FSKB) AND FSOK;
FSSHT := FSHTF <= 0 AND ((FSDN AND FSTS AND FSFOKS) OR FSSK) AND FSOK;
DRAWICON(FSLON, LOW * 0.998, 1);
DRAWICON(FSSHT, HIGH * 1.002, 2);
