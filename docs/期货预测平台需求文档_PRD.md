# 国内期货涨跌预测平台 — 需求文档 (PRD)

> 版本：v1.2（需求增补版）｜ 最近更新：2026-09-07
> 状态：✅ v1.2 全部确认（用户 2026-09-07"1.2 都确认"）——§16 传导预测方案、R1 主连口径（平滑主连）、㉑ 关闭（§16 即第 4 点）
> 变更记录：v1.1 固定工程路径与导入模板（§4.5）；v1.2 追加 §16 传导预测需求并确认
> 工程路径：代码根目录 `E:\Docker\qhyc`；数据模板源 `E:\QH\FG\data`（见 §4.5）
> 用途：提交 WorkBuddy（或 CodeBuddy 等编码代理）进入实现（M1 起）。

---

## 1. 项目概述与目标

构建一个**本地运行**的平台，存储国内期货全品种行情数据，并基于数学 / 统计 / 机器学习模型，预测下一交易日的涨跌方向与幅度。

核心能力：
1. **行情数据仓库**：日线先行，小时线后续；支持自动（定时）+ 手动更新；数据质量可校准。
2. **多模型预测引擎**：傅里叶、小波、ARIMA、GARCH、马尔可夫、HMM、贝叶斯、蒙特卡洛、LSTM、XGBoost 等，集成输出方向与幅度。
3. **可视化看板 + 回测验证**：浏览预测、对比模型、回测评估。

非目标（本期）：实盘下单、多用户账号体系、云端多租户（但架构按云端演进预留，见 §14）。

---

## 2. 总体架构（分层）

```
[数据源] akshare(主) ──┐
                      ├─→ [采集/校准层] 入库 + tqsdk 补缺/比对 + 异常工单
[校准源] tqsdk ────────┘
                          ↓
[数据层] PostgreSQL + TimescaleDB（超表：日线/小时线/日历/元数据/预测/回测/异常/简报信号）
                          ↓
[计算层] 特征工程 → 模型库(集成) → 预测服务 → 回测引擎
                          ↓
[服务层] FastAPI（行情/预测/回测/任务/异常/对接 接口）
                          ↓
[展示层] React 看板（总览/预测详情/热力图/回测/数据质量）
```

---

## 3. 技术栈（已确认）

| 层 | 选型 | 说明 |
|---|---|---|
| 语言 | Python 3.11+ | 量化/ML 生态，复用已有 Zigzag、Qi 指标 |
| 后端 | FastAPI | 异步 API，自动文档 |
| 数据库 | PostgreSQL + TimescaleDB | 时序超表，自动分区/连续聚合/压缩 |
| 前端看板 | React 18 + Vite + TypeScript + Ant Design + ECharts | 已确认直接用 React；K线/热力图用 ECharts |
| 计算 | scipy / statsmodels / scikit-learn / PyTorch / prophet | 模型实现 |
| 调度 | APScheduler | 本地常驻定时任务 |
| 行情源 | akshare（主）+ tqsdk（校准） | 见 §4.1 |
| 部署 | Docker Compose（本地起步） | 资源预估见 §9.1；云端演进见 §14 |

---

## 4. 数据层

### 4.1 数据源与校准（已确认）

- **主源 akshare**：免费，覆盖 CZCE / SHFE / DCE / CFFEX / INE 全品种日线 + 小时线。
- **校准源 tqsdk（天勤）**：行情质量高，用于**补缺**与**二次比对**。
- **tqsdk 账号**：已提供（手机号 + 密码），凭据存项目 `.env`，不写入文档 / git / 日志。
- **入库与校准流程**：
  1. akshare 按品种 + 区间拉取，按主键 `(symbol, trade_date)` upsert 入库。
  2. **缺失检测**：入库结果与交易日历比对，标记 akshare 缺失的交易日/品种。
  3. **tqsdk 补缺**：对缺失部分调用 tqsdk 拉取补齐入同一张表。
  4. **二次比对**：全量入库数据再用 tqsdk 拉取同区间比对（重点：收盘价、结算价、成交量）。
  5. **异常判定**：偏差超阈值（默认：收盘价差异 > 0.5%，或成交量异常缺失/翻倍）→ 生成**异常工单**记录。
  6. **异常处置**：在日志/看板给出提示，**由人工（用户）判断接受或修正，系统不自动覆盖**。异常工单包含：品种、日期、akshare 值、tqsdk 值、偏差、建议。
- **兜底**：支持手动导入用户已有本地历史数据（FG/SA 已有成套文件），导入同样走 tqsdk 比对；实际模板见 §4.5。
- 校准阈值、tqsdk 超时/重试参数放配置文件。
- 主力连续合约映射表（akshare 代码 ↔ tqsdk `KQ.m@EXCHANGE.CODE`）以配置维护，实现时内置常见品种。

### 4.2 更新策略

- **自动（每日三次，已确认）**：由 APScheduler 定时触发，针对**主力连续合约**：
  - 日盘午间 **12:00**（捕获上午盘）
  - 日盘收盘后 **16:00**（含当日完整日盘）
  - 夜盘次日早 **08:00**（捕获前一晚夜盘，夜盘约 02:30 收）
- **预测联动**：每次自动 / 手动更新入库完成后，立即对该批主力连续合约触发一次预测（即**一天三次预测**；三次统一预测**下一交易日**，12:00 为盘中快照版，08:00 为含夜盘修正版，见 ⑨）。
- **手动**：提供 API / 看板按钮，立即更新指定品种或全量，并更新后同样触发预测。
- **增量**：以 `(symbol, date)` 为主键 upsert，断点可续；失败重试 + 告警。
- **预测标的**：仅**主力连续合约**（如 FG888、SA888），不逐具体交割合约。
- **数据仓库范围**（⑧）：全品种**所有历史合约**日线都存（量小），主连由映射表合成，便于回溯换月。

### 4.3 数据模型（TimescaleDB 超表）

- `futures_symbol`（品种/合约元数据）：symbol, name, exchange, unit, multiplier, is_main, main_symbol, active
- `main_contract_map`：trade_date, exchange, product, main_symbol, change_flag（每日主连映射与换月标记）
- `daily_bar`（日线超表，time=trade_date）：symbol, trade_date, open, high, low, close, settle, volume, amount, oi(持仓), ret_close(收盘价涨跌幅%), ret_settle(结算价涨跌幅%), ret5, ret20, src(akshare/tqsdk/csv)
- `main_continuous`（主连表，⑪）：原始主连价 + 平滑主连价（换月比例拼接），模型特征默认取平滑
- `hourly_bar`（小时线超表，time=trade_datetime，二期）：symbol, trade_datetime, open, high, low, close, volume, oi, ret
- `trade_calendar`：exchange, trade_date, is_open, sessions(JSON，含日盘/夜盘分段) —— 用于缺失检测
- `prediction_result`：symbol, target_date, as_of_date, run_id, model_set, direction(涨/跌), direction_prob, ret_point(点估计%), ret_low(P5%), ret_high(P95%), confidence, participated_models(JSON)
- `backtest_result`：model, window, start, end, dir_acc, mae, rmse, quantile_hit, sample_n
- `anomaly_ticket`：symbol, trade_date, field, akshare_val, tqsdk_val, diff, threshold, status(待裁决/已接受/已修正/误报), note
- `briefing_signal`（对接简报引擎预留）：source, symbol, trade_date, direction, score, note, received_at

`daily_bar` / `hourly_bar` 建为 TimescaleDB 超表；`ret5/ret20` 可由连续聚合或写入时计算。

### 4.4 向前兼容设计（小时线 M6 预留，已确认按默认）

本期只落 `daily`，但**表空间与代码逻辑按可平滑扩展小时线设计**，M6 接入时不返工：

- **频率维度参数化**：`freq ∈ {daily, hourly}` 贯穿采集、校准（tqsdk）、特征、模型、预测、回测、API。新增小时线 = 加一张表 + 一个配置，不重写逻辑。
- **表结构同构**：`hourly_bar` 与 `daily_bar` 同构（仅 `time` 粒度不同），共用 `BarRepository` 抽象层（按 `freq` 路由表名）。
- **表空间 / 分块规划（TimescaleDB）**：
  - `daily_bar` chunk_time_interval = 3 个月；`hourly_bar` chunk_time_interval = 1 周（小时数据量大）。
  - 建议独立表空间：日线放主存储，小时线后续可挂大容量 / 压缩表空间（如 `ts_hourly`）。
  - 压缩策略：hourly 写入 7 天后转列存压缩；daily 30 天后压缩。
  - 连续聚合：`hourly_1d_rollup` 由小时线自动聚合出日线，作为日线校验 / 备用来源。
- **特征窗口以"根数"表达**：前 5 / 20 用"前 N 根 K 线"而非硬编码天数；小时线下自然对应前 5 / 20 小时，窗口长度放配置。
- **交易日历含 intraday 时段**：午休 11:30–13:30、夜盘 21:00–02:30 分段，供小时线缺失检测。
- **模型频率无关**：模型均作用于收益率序列，同一套模型库直接服务小时线预测；M6 仅补数据接入与校准（见 §10）。

### 4.5 CSV / JSON 导入模板（⑯ 已定稿，源自用户现有数据）

模板源目录：`E:\QH\FG\data`（FG 为例；SA 等品种同构，按 `E:\QH\{PRODUCT}\data\` 扩展）。
编码：JSON 为 UTF-8（可带 BOM），CSV 为 UTF-8 BOM + CRLF。

| 文件 | 粒度 | 列 / 结构 | 对应库表 |
|---|---|---|---|
| `{P}_daily.json` | 日线·原始主连 | date, open, high, low, close, volume, oi | `main_continuous`（原始价） |
| `{P}_cont_adj.json` | 日线·后复权主连 | 同上 + adj 标记 | `main_continuous`（平滑/复权价） |
| `{P}_cont_adj_adjusted.csv` | **1小时**·后复权主连 | date(含时分秒), open, high, low, close, volume | `hourly_bar`（主连，M6 直接可用） |
| `{P}_rolls.csv` | 换月记录 | date, old, new, delta(价差) | `main_contract_map`（换月+价差） |
| `{P}_contracts.json` | 日线·全部历史合约 | dict[合约代码] → [同 daily 列] | `daily_bar`（⑧ 全合约） |

说明：
- 用户现有数据以**后复权主连 + 换月价差**组织，与 ⑪"平滑主连"设计完全一致，`delta` 即换月拼接价差。
- `cont_adj_adjusted.csv` 表明 FG / SA 已有**小时级**主连数据——M6 可直接由该模板起步，亦可作为 akshare / tqsdk 之外的第三校验源。
- M1 导入器按上表逐类解析入库，`src` 字段标 `csv`；JSON 字段以实际文件为准（已抽样确认）。

---

## 5. 计算 / 预测引擎

### 5.1 特征工程

输入窗口：当日涨跌幅 + 前 5 / 前 20 根 K 线的涨跌幅序列（窗口以"根数"表达，见 §4.4），及衍生特征：
- 波动率（5/20 根标准差）、动量（ret5/ret20）、成交量变化率
- Hurst 指数、样本熵（可预测性）、ATR（波动）
- 你已有：Zigzag 拐点标记、Qi(ATR+MACD) 作为辅助信号/特征
- 涨跌幅口径：模型特征与预测目标统一用**结算价涨跌幅**（ret_settle，⑩）

### 5.2 模型库（按族，职责）

**变换类（与傅里叶同族）**
- 傅里叶变换：提取周期成分，给出周期性方向偏向
- 小波变换：多尺度周期 + 突变点检测，适合非平稳行情

**统计时序类**
- ARIMA / SARIMA：线性外推，输出方向与点估计幅度
- GARCH 族：波动率聚类，输出幅度/风险区间
- 卡尔曼滤波：状态估计 + 去噪，动态预测

**状态 / 概率类（与马尔可夫同族）**
- 马尔可夫链：基于历史状态转移矩阵，输出方向概率
- 隐马尔可夫 HMM：识别隐藏市场状态（牛/熊/震荡）
- 贝叶斯更新：随新数据修正方向后验概率
- 蒙特卡洛模拟：路径分布 → 涨跌概率 + 分位幅度

**机器学习 / 深度学习类**
- LSTM / GRU / Transformer：序列预测方向与幅度
- XGBoost / LightGBM / 随机森林：特征 → 方向分类 + 幅度回归
- 高斯过程回归：预测 + 不确定性区间

**复杂系统 / 信息论类**
- 赫斯特指数 Hurst：判定趋势 vs 均值回复，作为**状态门控**选择启用的模型子集
- 样本熵 / 近似熵：市场可预测性度量（低熵→模型更可信）
- 相空间重构 / RQA：非线性动力学特征

重训频率（⑱）：轻模型（ARIMA / 马尔可夫 / XGB 等）随预测在线更新；LSTM 每周重训一次。

### 5.3 集成机制

- **状态门控**：先用 Hurst 判定趋势 / 均值回复，选择启用模型子集（趋势市偏重 LSTM/ARIMA，震荡市偏重马尔可夫/均值回复类）。
- **方向投票**：各模型输出方向概率，加权投票（权重每月按近 60 日回测准确率更新，⑳）；起步为**涨/跌二分类**（⑫），震荡标签后期加。
- **幅度融合**：各模型幅度预测取分位融合（中位数点估计 + [P5, P95] 区间）。
- **输出规范（JSON，v1 契约，对接简报引擎复用）**：
  ```json
  {
    "schema_version": 1,
    "symbol": "FG888",
    "target_date": "2026-09-07",
    "as_of": "2026-09-06T16:05:00+08:00",
    "run_id": "20260906_1600",
    "direction": "up | down",
    "direction_prob": 0.62,
    "ret_point": 0.8,
    "ret_low": -0.5,
    "ret_high": 2.1,
    "confidence": 0.71,
    "participated_models": ["fourier","arima","markov","lstm"],
    "state": "trend|mean_revert"
  }
  ```

### 5.4 预测服务接口

- `POST /predict`：入参 `{symbol, as_of_date}` → 返回 §5.3 结构
- `POST /predict/batch`：批量预测全品种（用于看板热力图）
- 每次预测生成唯一 `run_id`，同一 `as_of` 多次运行可追溯

---

## 6. 回测引擎

- 输入：历史样本滚动窗口，逐日生成预测并与真实涨跌对比。
- 指标：方向准确率、幅度 MAE / RMSE、分位覆盖命中率、按品种 / 按市场状态（Hurst）分层。
- 用途：模型权重学习、策略评估、看板展示。
- 参数（⑲⑳）：滚动窗口 250 交易日；权重每月按近 60 日准确率更新。

---

## 7. 服务层 API（FastAPI）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /symbols | 品种列表 |
| GET | /bars/{symbol}?freq=daily | 行情查询 |
| POST | /ingest | 手动更新（指定品种/全量） |
| GET | /anomalies | 异常工单列表 |
| POST | /anomalies/{id}/resolve | 人工裁决（接受/修正/误报） |
| POST | /predict | 预测 |
| POST | /predict/batch | 批量预测 |
| POST | /backtest | 回测 |
| GET | /tasks/{id} | 任务状态 |
| GET/POST | /integrations/* | 简报引擎对接（见 §13） |

---

## 8. 展示层（React 看板，已确认）

技术：React 18 + Vite + TypeScript，UI 组件 Ant Design，K线/热力图 ECharts，路由 react-router；数据全部走 FastAPI 接口（开发期 Vite 代理，生产由 nginx 托管构建产物）。涨跌配色遵循国内惯例：**涨红跌绿**。

页面：
- **品种总览**：表格（最新价、N 日涨跌、次日预测方向标签）
- **单品种预测详情**：K线图 + 方向概率、幅度区间、参与模型、特征图、当日三次预测对比
- **全市场次日热力图**：涨跌概率分布
- **回测面板**：准确率曲线、各模型对比
- **数据质量面板**：异常工单列表 + 裁决操作 + 校准日志
- **互证面板（预留）**：本系统 vs 简报引擎 一致率与分歧清单

---

## 9. 部署与运维（本地单机，Docker）

- Docker Compose 三容器：`timescaledb` + `python-api(含调度)` + `web(nginx 托管 React 构建产物)`
- 配置：数据源 token、更新时刻、校准阈值、模型权重 → `config/{local,cloud}.yaml` + `.env`（密钥不进 git）
- 日志：采集 / 校准 / 异常分级记录，轮转保留

### 9.1 Docker 资源预估（已确认采用 Docker）

| 容器 | CPU | 内存 | 说明 |
|---|---|---|---|
| timescaledb | 1–2 核 | 2–4 GB | shared_buffers 1GB 起，数据量小 |
| python-api + scheduler | 1–2 核 | 1–2 GB | 采集/校准/轻模型推理 |
| web（nginx + React 静态） | 0.2–0.5 核 | 128–256 MB | 看板渲染，静态资源极轻 |
| 重型训练峰值（LSTM/XGBoost 重训） | 额外 4–8 核 | 额外 4–8 GB | 调度在空闲时段，可降频 |

- **日常运行推荐：4 核 / 8 GB / 50 GB SSD**
- 若每日全量重训深度模型：8 核 / 16 GB
- 最低可跑：2 核 / 4 GB（关闭深度重训，仅轻模型 + 采集 + 看板）
- 磁盘说明：日线全量极小（49 品种 × 10 年 ≈ 十几万行）；小时线 10 年 ≈ 百万行级，列存压缩后很小——**瓶颈在网络拉取时长与训练 CPU，不在存储**。

---

## 10. 开发里程碑（分期）

- **M1 数据层**：PG+Timescale 建库（工程根目录 `E:\Docker\qhyc`）、akshare 接入、tqsdk 校准（补缺+比对+异常）、自动/手动更新、按 §4.5 模板导入用户现有 FG/SA 数据
- **M2 特征 + 单模型**：特征工程 + 傅里叶/马尔可夫/ARIMA 跑通预测链路
- **M3 全模型库 + 集成**：补齐其余模型、Hurst 门控、加权投票
- **M4 回测引擎**
- **M5 React 看板 + 对接预留**：前端上线；简报引擎对接 API 与 schema 落地（§13）
- **M6 小时线扩展**（架构已预留，见 §4.4）：hourly_bar 接入，对小时级预测复用同一引擎
- **M7 云端部署**（见 §14）：迁移数据 → 云端运行 → 关闭本地常驻（保留本地开发）

---

## 11. 待确认清单

### 11.1 已定结论（原 ①–⑦，冻结）

| # | 项 | 状态 | 结论 |
|---|---|---|---|
| ① | 数据源与校准方案 | ✅ 已定 | akshare 主源 + tqsdk 校准（补缺+二次比对+异常人工裁决） |
| ② | 自动更新时刻 | ✅ 已定 | 每日三次：12:00 / 16:00 / 次日 08:00 |
| ③ | 预测标的 | ✅ 已定 | 仅主力连续合约（如 FG888） |
| ④ | 小时线优先级 | ✅ 已定 | 默认二期 M6；表空间与代码逻辑须向前兼容（见 §4.4） |
| ⑤ | 账号/多用户 | ✅ 已定 | 本期单机单人，无账号体系 |
| ⑥ | 涨跌幅口径 | ✅ 已定 | 百分比（基准价细节见 ⑩） |
| ⑦ | 预测频率 | ✅ 已定 | 每次更新数据后预测，即一天三次 |

### 11.2 全文回溯新增项（v0.4 提出 → v1.0 已收口）

**A. 逻辑 / 口径类**

| # | 项 | 结论 |
|---|---|---|
| ⑧ | 数据仓库范围 | ✅ 全品种所有历史合约日线都存（量小），主连由映射表合成，便于回溯换月 |
| ⑨ | 三次预测目标日 | ✅ 三次统一预测**下一交易日**；12:00 为盘中快照版（标注 as_of），08:00 为含夜盘修正版 |
| ⑩ | 涨跌幅基准 | ✅ 预测目标用**结算价涨跌幅**；ret_close 与 ret_settle 两字段都存 |
| ⑪ | 主连换月跳空 | ✅ 存"平滑主连"+"原始主连"两套价，模型默认用平滑 |
| ⑫ | 震荡判定 | ✅ 起步只做**涨/跌二分类** + 幅度区间；震荡标签后期加 |
| ⑬ | 夜盘归属 | ✅ 按交易所口径：归属**次一交易日** |

**B. 工程 / 数据类**

| # | 项 | 结论 |
|---|---|---|
| ⑭ | tqsdk 账号 | ✅ 账号已提供，凭据存项目 `.env`（不写入文档/git/日志） |
| ⑮ | 历史回补起始 | ✅ 2015-01-01 起 |
| ⑯ | CSV 样例 | ✅ 已定稿：按用户现有数据定义模板（5 类文件，见 §4.5），M1 直接实现导入器 |
| ⑰ | 异常修正边界 | ✅ 只允许"采纳 tqsdk 值"或"标记误报"，不手改数值 |
| ⑱ | 深度模型重训频率 | ✅ 轻模型随预测在线更新；LSTM 每周重训一次 |
| ⑲ | 回测窗口 | ✅ 250 交易日 |
| ⑳ | 权重更新周期 | ✅ 每月，按近 60 日准确率 |
| ㉑ | 用户"第 4 点"背景 | ✅ 已关闭（2026-09-07 用户确认）：§16 跨品种传导即该第 4 点 |
| — | 展示层技术 | ✅ 直接用 React（React 18 + Vite + TS + AntD + ECharts），不做 Streamlit 过渡 |
| ㉒ | 跨品种/跨大类传导 | ✅ 用户需求（2026-09-07），设计与排期见 §16，随 M2/M3/M5 分摊实现 |

---

## 12. 验收标准

- 能自动拉取并存储 ≥49 品种日线，akshare 缺失由 tqsdk 补齐，断点可续。
- 入库数据经 tqsdk 二次比对，异常生成工单并在看板提示，可由人工裁决。
- 给定品种 + 日期，返回结构化预测（方向 + 概率 + 幅度区间 + 置信度），一天三次留痕可追溯。
- 回测可输出方向准确率等指标。
- React 看板可浏览预测、回测与数据质量。
- 简报引擎可通过预留 API 拉取预测 / 推送信号（M5 起可用）。
- Docker 一键起停，资源占用在 §9.1 预估范围内。

---

## 13. 与"期货多空简报引擎"对接（预留，已确认需要）

背景：本系统后续与期货多空简报引擎**相互印证、互为补充**。本期只预留对接能力，不实现简报逻辑。

- **数据契约**：以 §5.3 预测输出 JSON 为 v1 契约（含 `schema_version`），字段变更需升版本号，保证对方平稳升级。
- **新增表 `briefing_signal`**：存对方推来的信号（来源 / 品种 / 日期 / 方向 / 强度分 / 备注），用于互证分析。
- **对接 API**：
  | 方法 | 路径 | 说明 |
  |---|---|---|
  | GET | /integrations/predictions?date=&symbol= | 简报引擎拉取本系统预测（JSON/CSV） |
  | POST | /integrations/signals | 接收简报引擎信号入库 |
  | GET | /integrations/agreement?date= | 双方方向一致率与分歧清单 |
- **鉴权**：静态 API Key（请求头 `X-API-Key`，配置文件管理）；单机单人够用，上云后可换 token / 内网隔离。
- **互证逻辑（后期）**：方向一致 → 综合置信度上调并在看板标注"共振"；分歧 → 看板标记"待人工研判"，不自动取其一。

---

## 14. 本地 → 云端演进方案（已确认：本地调试完成后迁云端）

### 14.1 环境分层
- `docker-compose.yml`（基础）+ `docker-compose.override.yml`（本地开发：代码热挂载、调试端口）
- 配置：`config/local.yaml` / `config/cloud.yaml`，密钥走 `.env`，不进 git
- 同一套镜像，本地与云端只差配置文件——**杜绝"本地能跑云端跑不了"**

### 14.2 数据迁移（一次性初始化）
1. 本地调试完成后：`pg_dump -Fc`（TimescaleDB 扩展版本云端保持一致）
2. 云端 `pg_restore`；或按表 `COPY ... TO/FROM CSV`（按 `(symbol, trade_date)` 排序导出）
3. **迁移校验**：逐表行数比对、各品种 `max(trade_date)` 比对、随机抽 20 条 × tqsdk 复核

### 14.3 增量数据更新（两条路线，推荐 A）
- **A（推荐）**：云端自跑同一套定时采集（12:00 / 16:00 / 次日 08:00 + tqsdk 校准），本地与云端各自独立更新，历史一次性迁移即可，无日常同步负担
- **B（备选）**：本地更新后按日期区间导出增量 CSV → rsync / 对象存储中转 → 云端导入脚本入库；适合"本地为唯一数据入口"的强管控需求
- 两条路线均带增量校验：行数 + 最新日期 + 抽样比对

### 14.4 代码更新（本地调试 → 云端上线）
- 代码进 git：本地 push → 云端 pull（或 CI 自动拉取）
- schema 变更用 **Alembic** 迁移脚本，禁止手改线上表结构
- 发布：打版本 tag → 云端 `docker compose pull && docker compose up -d` → 健康检查 `/health` → 失败则回滚上一 tag
- 上线检查单：config 切 cloud、密钥注入、每日 `pg_dump` 备份（保留 7 份）、日志轮转、容器存活 + 更新失败告警

### 14.5 上线动作清单（M7）
① 本地打 tag → ② pg_dump 全量导出 → ③ 云端建库 restore + 校验 → ④ compose up → ⑤ 手动触发一次 ingest + predict 验证 → ⑥ 开启定时与告警 → ⑦ 本地转开发模式（停常驻，保留调试）

---

*本 PRD v1.2（2026-09-07 全部确认）：v1.1 固定结论不变；§15 实现问题清单 R1–R3 全部已定（R1=平滑主连口径，R2=日历 M2 首项，R3=就绪门控等三细节），§16 跨品种/跨大类传导需求增补已确认。工程根目录 `E:\Docker\qhyc`，CSV/JSON 导入模板见 §4.5。§17 为 2026-09-08 口径裁决（⑩ 由结算价改为收盘价）。可交 CodeBuddy 按 §15/§16/§17 结论继续实现。*

---

## 16. 需求增补（v1.2）：跨品种 / 跨大类传导预测

> 来源：用户需求（2026-09-07）。国内期货品种按大类划分（农产品、化工能源、黑色系、有色/新能源金属、贵金属等），**大类之间走势相对独立，大类内部相关性高**，且存在产业链上下游传导与相互制约——如原油→沥青/燃料油/PTA，铁矿石→螺纹钢/热卷，焦煤→焦炭→钢材，纯碱→玻璃（成本链），大豆→豆粕/豆油，豆粕↔菜粕（替代）。本节把"品种之间涨跌幅的传导"与"大类/品种权重"正式纳入预测因素。

### 16.1 分类与权重体系

- **新表 `sector_map`**：product, sector(大类), chain_role(上游/中游/下游), active。初始分类内置配置，覆盖全部目标品种，支持 CSV 覆盖。示例划分：
  - 黑色系：螺纹钢 RB、热卷 HC、铁矿石 I、焦煤 JM、焦炭 J、硅铁 SF、锰硅 SM、不锈钢 SS
  - 化工能源：原油 SC、燃料油 FU、低硫燃料油 LU、沥青 BU、PTA、乙二醇 EG、甲醇 MA、PP、塑料 L、PVC、苯乙烯 EB、纯碱 SA、玻璃 FG、尿素 UR、橡胶 RU、20号胶 NR、纸浆 SP、PX、烧碱
  - 农产品：豆一 A、豆二 B、豆粕 M、豆油 Y、棕榈油 P、菜粕 RM、菜油 OI、玉米 C、淀粉 CS、白糖 SR、棉花 CF、棉纱 CY、苹果 AP、红枣 CJ、花生 PK、生猪 LH
  - 有色/新能源金属：铜 CU、铝 AL、锌 ZN、铅 PB、镍 NI、锡 SN、氧化铝 AO、工业硅 SI、多晶硅 PS、碳酸锂 LC
  - 贵金属：黄金 AU、白银 AG
  - 金融期货（IF/IH/IC/IM/国债）默认**不纳入**本轮预测范围，配置开关可开
- **大类指数 `sector_index`**：板块内品种涨跌幅按权重合成日度指数；初始**成交额加权**（可配置等权）；成分变动留痕（composition JSON）。
- **品种在板块内权重**：用于大类指数合成与传导强度基准。
- **传导权重矩阵 `transmission_weights`**：src 品种 → dst 品种的方向、时滞档（当日/1日/5日）、权重值；三个来源融合（见 16.2）。

### 16.2 传导建模（三层）

1. **先验链路（配置）**：`transmission_config.yaml` 内置产业链先验，每条含方向、时滞档、强度档(0–1)、成本占比参考：
   - 成本链：原油→石脑油→PX→PTA；原油→燃料油/沥青；焦煤→焦炭→螺纹/热卷；铁矿石+焦炭→钢材；**纯碱→玻璃**；大豆→豆粕/豆油；玉米→淀粉
   - 替代/互补：豆粕↔菜粕；豆油↔棕榈油↔菜油（油脂板块内部）
   - 需求反馈：钢材↔铁矿石（下游需求→上游原料）
2. **数据驱动**：滚动 60/120 日相关系数、Granger 因果检验（滞后阶搜索 1/2/5）、**传递熵**（量化信息流强度与时滞）、VAR 脉冲响应。与先验融合：`w = 0.5×先验 + 0.5×数据`；样本不足时回落纯先验。
3. **层级预测**：先用同一模型库预测**大类指数**的方向/幅度，结果作为品种级特征（仅用 as_of 之前数据，防前视）。

### 16.3 特征工程扩展（全部使用滞后数据，防前视）

| 特征 | 含义 |
|---|---|
| `sector_ret_1d/5d/20d` | 所属大类指数动量 |
| `sector_excess` | 品种涨跌 − 大类涨跌（板块内强弱/背离） |
| `upstream_ret_lag1/lag5` | 权重 top-k（默认 k=3）上游品种的滞后涨跌幅 |
| `cost_gap` | 理论成本传导幅度（上游 ret × 成本占比系数）− 品种实际 ret（背离=信号） |
| `sector_corr_regime` | 板块内 60 日平均两两相关（高相关期传导更可信） |
| `te_topk` | 对本品种信息流最强的 top-3 品种近期方向 |

### 16.4 模型与集成影响

- VAR / Granger / 传递熵正式纳入 §5.2 模型库，新增"跨品种联动"族
- XGBoost / LSTM / 高斯过程等特征集统一加入 16.3 传导特征
- 集成投票机制不变；传导模型输出与单品种模型**同票参与**加权
- 防共线性：每品种仅取"大类指数 + top-k 上游"，不把整个板块塞入特征

### 16.5 数据层新增

- `sector_map`（分类与产业链角色）
- `sector_index`（大类指数，合成数据，无需 tqsdk 校验）
- `transmission_weights`（权重快照：method=prior/corr/granger/te, weight, lag_days, updated_at；**每周重算**，与 LSTM 周训对齐）

### 16.6 看板与对外契约

- 看板：单品种详情新增**"传导来源"卡片**（上游品种当日/5日涨跌 → 预期影响方向与幅度）；新增**大类热力图**与板块强弱排名
- 对外契约：v1 JSON 不变；**v2 预留 `drivers` 字段**（大类动量、top 上游影响、cost_gap 背离标记），保证简报引擎平滑升级

### 16.7 实现排期与注意事项

- **排期**：M2 = sector_map / sector_index / 静态先验传导特征（特征 v1）；M3 = 动态权重（Granger/传递熵/VAR）+ 层级预测 + 特征 v2；M5 = 看板传导卡片与大类热力图
- **注意**：①传导特征一律用滞后数据，严禁当期数据进特征（前视偏差）；②板块内高相关品种不重复入模（top-k + 大类指数）；③新品种/样本不足回落先验权重；④大类指数成分变动留痕；⑤权重每周重算一次，与 LSTM 周训对齐

---

## 17. 口径裁决记录（2026-09-08，用户裁决）

- **⑩ 涨跌幅基准修订**：预测目标由"结算价涨跌幅（settle）"修订为"**收盘价涨跌幅（close）**"。裁决理由：① 收盘价更贴近可成交价格，预测落点是"明天怎么操作"，参考意义强于未必能成交的结算价；② 全链路（回测标签 / 线上预测 / 看板）已自洽，不存在"测与做不一致"；③ 切换 settle 需重跑回测 + 重训 LSTM 快照，成本不小，而当前无模型显著优于随机，收益有限。
- **四层一致性核对（2026-09-08 独立审计）**：
  - **引擎层 ✅ 一致**：`config.label_metric="close"`；回测标签 `_next_ret` 走 close；传导层 `transmission.py`、板块层 `sectors/builder.py`、动态权重层 `sectors/dynamic_weights.py` 均用 `ret_close`；线上预测基于 `csv_smooth` 收盘价主连序列。
  - **库表层 ✅ 一致**：`daily_bar` 同时存 `ret_close` 与 `ret_settle`（settle 仅作对冲 / 对账参考，不参与预测与回测）。
  - **需求文档**：本裁决前 §4.5 / ⑩ 原文写 settle，已就地修订为 close（见 145 行、⑩ 行）。
  - **简报集成（§13）⚠️ 待办**：对接 API（`/integrations/*`）属 M5 里程碑，尚未实现；实现时**必须在 §5.3 预测契约与 `briefing_signal` 表显式声明 `caliber="close"` 字段**，否则与简报引擎（若用 settle）互证时约 26.5% 交易日方向翻转、产生假性分歧。
- **看板要求（M5）**：所有涨跌 / 幅度展示须标注"收盘价口径"，避免用户拿交易所结算价对账时误判"不准"。
- **遗留待 CodeBuddy**：① §13 集成契约补 `caliber` 字段；② `briefing_signal` 表补 `caliber` 列；③ `config.py:112` 注释"待用户裁决切换"可删除（已裁决 close）；④ M5 看板标注收盘价口径。