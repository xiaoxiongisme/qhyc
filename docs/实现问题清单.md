# 实现问题清单（追加式，不改已定结论）

> 依据 PRD v1.1 约定：需求结论冻结，实现过程中的问题与决策记录于此。

## 2026-09-06 ｜ M1 数据层实施

### 环境与依赖

| # | 问题 | 决策 / 修复 |
|---|---|---|
| E1 | Docker Hub（registry-1.docker.io）国内直连超时 | 在 `%USERPROFILE%\.docker\daemon.json` 配置 `registry-mirrors`（daocloud/1ms/rat.dev/dockerproxy），已生效 |
| E2 | 构建期 pip 官方源慢/不稳 | Dockerfile 增加构建参数 `PIP_INDEX_URL`，默认清华源 |
| E3 | `akshare==1.15.81` 在源上已不存在（最低可用 1.16.72） | 改钉 `akshare==1.18.94`（实测新浪接口兼容性最好） |
| E4 | Windows GBK 控制台输出 Unicode 符号报错 | `setup_logging` 将 stdout/stderr 重配置为 UTF-8 |

### 数据库 schema

| # | 问题 | 决策 / 修复 |
|---|---|---|
| D1 | `backtest_result.window` 与 PG 保留字 `WINDOW` 冲突，init SQL 报错 | 列改名 `window_len`（SQL 与 ORM 同步） |
| D2 | `main_contract_map` 主键原为 `(trade_date, product)`，与仓储层 upsert `(trade_date, exchange, product)` 不一致 → ON CONFLICT 失败 | 主键统一为三元组 `(trade_date, exchange, product)` |
| D3 | psycopg3 扩展协议单语句参数上限 65535，大表批量导入（contracts/hourly）超限 | 仓储层增加通用分块（`_chunked`，按列数动态计算块大小） |

### 数据源与口径

| # | 问题 | 决策 / 修复 |
|---|---|---|
| S1 | 新浪主连代码为 `品种+0`（FG0），非库内 `FG888`，直接传 888 返回 null | `AkShareSource.to_sina_symbol()`：888 后缀 → 0；调用前转换 |
| S2 | `futures_main_sina` 结算价列名为 `动态结算价` | 列映射表补充；`ret_settle` 口径不变（⑩） |
| S3 | tqsdk 主连代码为 `KQ.m@交易所.品种`（KQ.m@CZCE.FG），无 888 后缀 | `_to_kq_symbol` 与 YAML 模板同步修正 |
| S4 | tqsdk kline `datetime` 列为纳秒 Unix 时间戳（numpy.float64） | `pd_date` 增加数值分支（ns → date，按容器时区 Asia/Shanghai） |
| S5 | tqsdk 会话未显式关闭，进程退出报 `Event loop is closed` | 单例 `close_tqsdk()`：单品种结束即关，批量流程结束统一关 |

### 数据布局（与 PRD §4.5 假设的差异）

| # | 问题 | 决策 / 修复 |
|---|---|---|
| L1 | 实际数据：FG 与 SA 的模板文件**混放**于 `E:\QH\FG\data\`，且 `E:\QH` 下存在 SX/TS/tqsdk 等无关目录 | 导入器改为按文件名前缀发现品种（`**/data/{P}_daily.json`），不再假设 `{P}/data/` 目录结构；无关目录自然被忽略 |

### 实测结果（2026-09-06）

- FG888：akshare 拉取 2832 行（2015-01-01 起）→ 补缺检测**无缺失** → tqsdk 比对生成 2 条工单
- SA888：本地 CSV 导入 1623 行主连 + 4336 行全合约 + 8260 根小时线；akshare/tqsdk 比对发现 5 条 **5~6% 收盘价偏差工单**（新浪/天勤主力合约选择规则与用户 CSV 换月规则不同所致）→ **待人工裁决**，符合 ⑰ 设计
- M1 验收脚本 5 项全部通过；三容器 healthy；`/health`、`/symbols`、`/bars`、`/anomalies`、`/tasks` 接口实测正常

### 遗留事项

- R1：SA888 的 akshare 主连与用户 CSV/tqsdk 主连存在系统性价差（换月规则差异），M2 前建议在数据质量面板裁决：以哪套为主连口径（PRD 默认模型特征用平滑主连，即用户 CSV 口径）
- R2：`trade_calendar` 目前无写入来源（akshare 日历接口未接入）；缺失检测退化为按已有数据比对，M2 接入
- R3：akshare 全量 17 品种首次回补约 3~5 分钟，由 scheduler boot 任务在后台完成，可通过 `/tasks` 观察进度

---

## 2026-09-06 ｜ M1 收尾（按 PRD §15 R1–R3 决策执行）

### R1 主连口径 → 已按 ⑪ 默认落地

- `config/local.yaml:calibration.canonical_main_source = "csv_smooth"`，
  `canonical_main_version = "v1_csv_smooth_2026-09-06"` —— 模型特征/预测标签的权威口径为
  **用户 CSV 平滑主连**；akshare 主连仅作真实市场价格参照。
- 变更口径须全量重跑历史；后续裁决经数据质量面板确认后在此留痕。

### R2 推断日历 → 已实现（M1 降级方案）

- 新增 `app/ingest/calendar_infer.py`：品种间交叉（当日 ≥80% 主连品种有数据 → 交易日），
  写入 `trade_calendar`，`exchange='INF'` 与官方日历**分列存储**，note 记录阈值与覆盖率。
- API：`GET /calendar/mode`（看板显示"日历=推断模式"）、`POST /calendar/infer`；
  CLI：`scripts/infer_calendar.py`。
- tqsdk 补缺日历三级回退：官方 → 推断(INF) → 无日历且无数据时全量拉取兜底（CFFEX 场景）。
- 实测：推断出 2020-03-30 ~ 2026-09-04 共 **1536 个交易日**；官方日历接入（M2 首项）后全量重跑缺失检测。

### R3 工程细节 → 三项全部落地

| # | 改动 | 实现 |
|---|---|---|
| ① | 数据就绪门控 | `/health` 返回 `readiness`（目标品种数/已就绪品种数/覆盖率/数据滞后天数），未就绪 `status=ingesting`；M2 的 `/predict` 将按同一门控拒绝计算 |
| ② | 回补进度可视化 | `GET /ingest/progress`：目标清单 ×（已入库/行数/最新日期/剩余清单）；品种清单以 config 为唯一事实源，启动时幂等同步 `futures_symbol`（新增品种改 config 即可） |
| ③ | 品种级锁 + 幂等 | `pg_try_advisory_lock(hashtext(symbol))` 会话级锁：boot 与三次定时任务并发时同品种只有一个执行者；upsert 幂等可重复跑 |

### 附带修复（回补过程中暴露）

- 校准失败与主源失败解耦：tqsdk 超时不再把整品种标记为失败（`report.calibration_error` 单独记录），主源数据照常入库
- tqsdk 拉取超时自动重建会话重试（`max_retries` 次），解决批量拉取时 `TqTimeoutError` 卡死

### 全市场回补结果（2026-09-06）

| 交易所 | 品种数 | 主连行数 |
|---|---|---|
| CZCE | 17 | 33,957 |
| SHFE | 17 | 41,446 |
| DCE | 20 | 48,250 |
| CFFEX | 8 | 15,468 |
| INE | 5 | 7,419 |
| **合计** | **67** | **146,540** |

- 数据来源：akshare 141,553 行 + tqsdk 4,987 行（CFFEX 等新浪不覆盖品种，走兜底回退）
- 全合约日线合计 156,187 行（含 FG/SA 全历史合约 CSV 导入）；小时线 17,336 行（FG/SA）
- 就绪门控：**67/67 通过，ratio=1.0，lag=2 天**；异常工单累计 13 条（SA888 口径分歧 6 条 + FG888 比对 2 条等），待数据质量面板裁决
- 数据极少品种为正常新上市品种：LG888 原木 439 行、PR888 瓶片 488 行

### M1 验收状态（对照 PRD §12）

- ✅ 自动拉取并存储 ≥49 品种日线（实际 67）
- ✅ akshare 缺失由 tqsdk 补齐（补缺 + CFFEX 兜底），断点可续（幂等 upsert + 品种锁）
- ✅ 入库数据经 tqsdk 二次比对，异常生成工单（13 条待人工裁决）
- ✅ M2 预测链路（见下节）

---

## 2026-09-07 ｜ M2：特征工程 + 单模型预测链路

### 实现范围（PRD §5.1–5.4，⑨⑩⑫，R1/R3①）

| 模块 | 内容 |
|---|---|
| 特征工程 `app/features/` | ret1/ret5/ret20、波动率 vol5/vol20、ATR14%、成交量变化率、**Hurst 指数**（R/S 分析）、**样本熵**；窗口全部以根数表达（§4.4），由 config.predict.windows 注入 |
| 模型库 `app/predictors/` | 傅里叶（top-k 周期成分解析外推）、马尔可夫（涨跌二态转移矩阵 + 条件经验分位）、ARIMA(1,0,1)（statsmodels）；统一 ModelOutput 契约（direction/prob/ret_point/P5/P95） |
| 集成引擎 `app/engine/` | Hurst 状态门控（trend→arima+fourier；mean_revert→markov+fourier；neutral→全部）；方向加权投票（M2 等权，M3 起按 60 日回测准确率月更⑳）；幅度中位数分位融合；置信度 = 方向强度 × 样本熵折减 |
| API `POST /predict` | 入参 {symbol, as_of_date?} → §5.3 JSON 契约 v1 + 诊断 _meta（source/hurst/sampen/特征集/模型错误）；run_id=`{as_of日}_{HHMMSS}_m2` 同 as_of 多 run 可追溯 |
| API `POST /predict/batch` | 全部/指定品种；async_run=true 后台执行（供热力图） |
| API `GET /predictions` | 预测留痕查询 |
| 调度联动 ⑦ | scheduler 三次 ingest 成功后自动对该批品种预测；`POST /ingest` 同样联动（`predict=true` 默认开） |
| 就绪门控 R3① | `/predict` 系列在数据未就绪时返回 **503** + readiness 详情，不静默计算 |

### 口径与模型输入

- 权威口径 = csv_smooth（R1）；**新增滞后回退**：CSV 为静态快照，滞后 daily_bar > 3 天时自动切 daily_bar 口径并告警
  - FG/SA 实测：CSV 止于 2026-08-12 < daily_bar 09-04 → 回退 `akshare_tqsdk`（有换月跳空，M2.1 解决）
- CFFEX 等无 CSV 品种直接走 daily_bar
- 预测目标日 = 下一交易日（推断日历 INF，兜底跳周末；⑨ 三次预测统一下一交易日）

### M2 实测结果（2026-09-07）

- 单品种 FG888：state=trend（H=0.576）→ arima+fourier → down p=0.525，target=2026-09-07 ✔
- 批量 67 品种：**67/67 成功**；prediction_result 留痕 73 条 / 67 品种 ✔
- 手动 ingest → 联动预测闭环验证通过（task `after-manual-ingest=success`）✔
- 就绪门控：ready=true 时正常计算（全部场景未触发 503，门控代码路径由单测覆盖）

### M2 遗留 → 后续里程碑

- **M2.1 平滑主连自动延伸**：✅ 已实现（见下节）
- **M3**：补齐小波/GARCH/HMM/贝叶斯/蒙特卡洛/LSTM/XGBoost 等；方向投票权重按 60 日回测准确率月更（⑳）
- **M4**：回测引擎（滚动 250 日窗口 ⑲）→ 为权重月更提供准确率数据

---

## 2026-09-07 ｜ M2.1 平滑主连自动延伸（R1 完整落地）

### 逆向验证结论（实现前对用户数据做了两组实证）

1. **用户的平滑算法不可精确复刻**：`diff=adj-raw` 有 48 个变化点（换月仅 20 次），
   2020-07 出现连续 7 天渐变、9 个换月日无变化、`delta` 仅部分匹配
   `new.close-old.close`——用户是混合规则（渐变加权 + 平移），非简单差价/比例复权。
   **决策**：不逐点复刻历史公式；延伸段只需"收益率无换月跳空 + 水平锚定 CSV 末日"。
2. **持仓量法主力判定可用**：close_oi 最大者为主力，与用户 rolls.csv 日期差 2~4 天
   （用户为"确认后延迟移仓"口径）——延伸段与用户历史 rolls 无重叠日期，无一致性冲突。
   实测修正了一个误判：FG 8 月中旬并未来回 1/5/9 循环外的月份，真实换月为
   **2026-08-14 FG609→FG701**（FG701 oi 99 万 > FG609 98.6 万），已用合约数据核实。

### 实现方案（比例平滑法）

| 环节 | 规则 |
|---|---|
| 候选合约 | 用户 contracts.json 已知合约 → 推断主力月码循环（**按出现频次过滤**，剔除移仓偶经月份如 FG010/011/012）→ 生成后续 3 个月码 |
| 换月检测 | tqsdk 拉候选合约日 kline，逐日 close_oi 最大者为主力（平手用 volume）；拉取失败自动重建会话重试 1 次 |
| 平滑收益率 | 换月日 ret = **新合约自身两日收益**（N(d)/N(d-1)-1，消除拼接伪跳空）；其余日 = raw 主连收益 |
| 水平锚定 | adj(t) = adj(t-1) × (1+ret/100)，从 CSV 末日 adj_close 起链式衔接 |
| OHLC | adj_open/high/low = raw × (adj_close/raw_close)；volume 用 raw |
| 落库 | main_continuous 延伸行 `src='csv_smooth_ext'`；新换月入 main_contract_map `src='inferred'` |
| 联动 | scheduler 三次 ingest 与手动 `/ingest` 均在**预测前**自动延伸；CLI `scripts/extend_smooth.py` |

### 实测（2026-09-07）

- FG：延伸 17 行（2026-08-13 ~ 09-04），检出换月 8/14 FG609→FG701；换月日 adj 收益 -0.97%
  （FG701 两日真实收益）而非 raw 拼接伪值，**序列无跳空**
- SA：延伸 15 行（2026-08-17 ~ 09-04），窗口内无换月（SA609 仍为持仓主力）
- **FG888 预测口径已回归 `csv_smooth`**（M2 的滞后回退不再触发），实现 R1 全链路单一口径
- 换月记录 `main_contract_map` 新增 `inferred` 源（FG609→FG701 @2026-08-14）

### 已知限制 / 备注

- tqsdk 3.7.5 `query_quotes` 参数不兼容（`ins_class`），候选合约改由月码循环生成——
  若未来主力月码跳出循环（如新上市月份），需人工在 config/contracts.json 补充
- 尚未上市的远月候选（如 SA709）拉取失败仅为警告，自动跳过
- 用户 rolls.csv 的换月日比 oi 切换日晚 2~4 天；延伸段使用 oi 切换日口径，
  与用户历史口径的日期差异不影响收益率正确性（换月日收益始终取新合约自身收益）

---

## 2026-09-07 ｜ M3：全模型库 + 集成

### 模型库（PRD §5.2 全族，`app/predictors/`，13 个模型）

| 族 | 模型 | 实现 | 备注 |
|---|---|---|---|
| 变换类 | 傅里叶 | top-k 周期解析外推 | M2 |
| 变换类 | **小波** | db4 三层软阈值去噪 + AR(1) 外推 | M3 新增（PyWavelets） |
| 统计时序 | ARIMA | (1,0,1) statsmodels | M2 |
| 统计时序 | **GARCH(1,1)** | arch 包，主要贡献波动率区间（方向弱是预期） | M3 新增 |
| 统计时序 | **卡尔曼滤波** | 局部水平模型，纯 numpy 在线滤波 | M3 新增 |
| 状态/概率 | 马尔可夫 | 涨跌二态转移 + 条件经验分位 | M2 |
| 状态/概率 | **HMM** | hmmlearn GaussianHMM(3 状态)，状态后验混合预测 | M3 新增 |
| 状态/概率 | **贝叶斯更新** | Beta-Binomial 先验 × 近 5 日动量证据 → 后验 P(up) | M3 新增 |
| 状态/概率 | **蒙特卡洛** | 历史收益 bootstrap 10000 路径，非参数分位 | M3 新增 |
| 机器学习 | **随机森林** | 滞后收益 [1,2,3,5,10,20] → 次日收益 | M3 新增（sklearn） |
| 机器学习 | **高斯过程** | RBF+WhiteKernel，窗口 60 控 O(n³) | M3 新增 |
| 机器学习 | **XGBoost** | 同 RF 特征；**n_jobs=2 修复容器线程争用（27s→0.3s）** | M3 新增 |
| 深度学习 | **LSTM** | torch 2.14+cpu（Dockerfile 可选层）；**⑱ 每周重训**（周六 06:00 cron，权重 /app/runtime/lstm/*.pt 共享卷） | M3 新增 |

### 门控与权重（§5.3）

- `config/local.yaml:predict.gate_models`：trend→7 模型（偏时序/ML：arima+fourier+kalman+rf+xgb+lstm+hmm）；
  mean_revert→6 模型（偏均值回复：markov+bayesian+montecarlo+wavelet+garch+fourier）；neutral→8 模型
- 权重 13 个模型配置化（garch 方向弱降权 0.8、lstm 趋势主力 1.2）；⑳ 月更钩子已留，
  M4 回测引擎输出近 60 日准确率后自动生效
- 集成：方向加权投票 + 幅度中位数分位融合 + 样本熵置信度折减（不变）

### 工程改动

- 新依赖：PyWavelets / arch / hmmlearn / xgboost / torch-cpu（Dockerfile 可选层，失败不阻塞构建）
- `config/` 改为容器**只读挂载**（改 yaml 免重建）
- `runtime/` 共享卷（LSTM 权重 api/scheduler 共用）；已入 .gitignore
- 品种级 advisory lock 详见 M1 收尾节；预测任务流水 task_run 新增 `lstm_train` 类型

### M3 实测（2026-09-07）

- 全量预测 boot：**success 67/67 symbols**（含 ingest→延伸→预测完整链路）
- M3 留痕 68 条 / 67 品种；状态分布 trend=45 / neutral=21 / mean_revert=2；
  方向分布 up=44 / down=24；参与模型数 7/8/6 与门控一致 ✔
- 单品种 FG888：trend → 7 模型全参与、无模型错误，端到端 ~5s（XGBoost 修复前 34s）
- LSTM 首次全量训练：67/67 品种，权重落 runtime 卷

### M3 遗留 → M4

- ⑳ 权重月更依赖回测准确率：M4 回测引擎（滚动 250 日窗口 ⑲）落地后，权重表 + 月更任务自动接入
- 预测任务无品种级锁（ingest 有）；M4 评估批量预测与手动预测并发时是否需要
- 全量批量预测 ~7 分钟（67 品种），热力图场景建议缓存当日最新 run（M5 看板处理）

---

## 2026-09-07 ｜ PRD v1.2 §16 M2 排期：跨品种/跨大类传导（特征 v1）

### 数据层（§16.5）

- 新表 3 张（`db/init/03_sector_tables.sql`，现有库在线应用）：
  - `sector_map`：product, sector, chain_role, active（yaml 同步，66 品种覆盖）
  - `sector_index`（超表）：板块日收益/5d/20d 动量/指数水平/**composition 每日留痕**（§16.7 ④）
  - `transmission_weights`：src→dst×method×lag 主键，先验 31 条链已落库（method=prior）

### 配置（`config/transmission_config.yaml`）

- 6 大类：黑色 8 / 化工能源 20 / 农产品 17 / 有色新能源 11 / 贵金属 2 / 金融 8
- 产业链先验 31 条（§16.2）：原油系成本链、煤焦钢链、**纯碱→玻璃**、大豆压榨链、
  玉米→淀粉、替代对（豆粕↔菜粕等）、需求反馈（钢材→铁矿石 lag5）
- 金融期货 `predict_enabled: false`（§16.1 默认排除，开关在 yaml）

### 指数合成（§16.1）

- 权重：**成交量加权**（配置 volume；PRD 原定成交额加权——实测 akshare 主连无成交额列
  `daily_bar.amount` 全空，自动回退 volume，已记录为口径调整；等权可配置切换）
- 实测回补 **13,692 行 / 5 板块**（black 2840 / energy_chem 2839 / agri 2839 /
  nonferrous 2836 / financial 2338）；precious 成员 2 < min_members=3 不成指数（AU/AG 走先验联动）

### 传导特征 v1（§16.3，`app/features/transmission.py`）

9 个特征全部**滞后数据防前视**（§16.7 ①）：
sector_ret_1d/5d/20d、sector_excess、upstream_ret_lag1/lag5（top-3 上游按先验强度加权）、
cost_gap（成本传导预期 − 实际，背离信号）、sector_corr_regime（板块 60 日均两两相关）、
te_topk（M2 用先验 top-3 上游方向，M3 换传递熵）。

### 模型与预测接入（§16.4）

- rf / xgb / gpr 特征集统一加入 9 个传导特征（NaN 填 0）；**LSTM 改多变量输入**
  （基础收益 + 传导特征通道），周更训练器同步升级，全量重训完成
- `predict_symbols` / `POST /predict` 排除金融期货：批量走 `predict_enabled_products`
  （按 product 比较），单品种直接 400 拒绝（实测 IF888 → HTTP 400）
- §16.6 对外契约：v1 不变，预测响应新增 **`drivers`** 字段（大类动量/上游影响/cost_gap/
  corr_regime，简报引擎可平滑升级）
- 品种清单扩至 **73**（新增 HC 热卷、SF 硅铁、SM 锰硅、SI 工业硅、LC 碳酸锂、PS 多晶硅；
  GFEX 三品种新浪不覆盖走 tqsdk 兜底），实测 73/73 采集成功 failed=0

### 实测

- FG888：state=trend → 5 模型；drivers 完整（sector_ret_20d=14.52、sector_excess=-0.46、
  cost_gap=-1.66、corr_regime=0.39）
- 全量预测 65 商品品种成功，M3 留痕 127 条；金融 8 品种被正确排除

### 修正的实施 bug（本轮）

- `predict_enabled_products` 两版口径 bug：旧镜像返回 product 名导致 0/0；
  新版 symbol/product 比较错位导致金融未排除——最终统一按 `product` 比较并在
  api/scheduler 双容器 rebuild 生效（注意：**改 Python 代码必须 rebuild，仅 config 走挂载热更**）
- `transmission_frame` 空结果/列表推导写法导致 KeyError——已修
- scheduler 增加 `RUN_ON_BOOT=0`（数据就绪后避免重启即全量回补；定时任务照常）

### §16 M3 排期（后续）→ ✅ 已实现（见下节）

- 动态权重：滚动 60/120 日相关、Granger（滞后 1/2/5）、传递熵、VAR 脉冲响应，
  与先验融合 `w = 0.5×先验 + 0.5×数据`，每周重算与 LSTM 周训对齐（transmission_weights
  method=corr/granger/te/var/blend）
- 层级预测：先预测大类指数 → 结果作为品种级特征（特征 v2）
- te_topk 切换为真实传递熵 top-3

---

## 2026-09-07 ｜ §16 M3 排期：动态权重 + 层级预测 + 特征 v2

### 第 2 层：数据驱动动态权重（`app/sectors/dynamic_weights.py`）

| 方法 | 实现 | 归一 |
|---|---|---|
| corr | 60/120 日 Pearson 相关绝对值均值 | [0,1] 天然 |
| granger | statsmodels 因果检验，滞后阶 {1,2,5} 搜索 min p | 1 − p |
| te | **传递熵**（中位数二值化离散 TE，lag {1,2,5} 搜索最优，按 Y 条件熵归一） | bits/1.0 |
| var | VAR 正交脉冲响应 5 期峰值 / (2×dst 波动) | [0,1] |
| blend | **w = 0.5×先验 + 0.5×数据均值**（§16.2） | — |

- 样本不足（重叠 <60 日）回落**纯先验**（§16.7 ③）
- **实测：31 链全部计算成功（fallback=0）**，落库 186 行
  （6 method × 31 链：prior/corr/granger/te/var/blend 各 31）
- blend 示例：AU→AG 0.800→0.707（数据侧弱于先验）、B→M 0.600→0.612（略强）
- TE 最优 lag 全部为 1 天（日频下信息流以次日传导为主）

### 第 3 层：层级预测（`engine.service.predict_sector_indices`）

- 大类指数（sector_index.ret_1d，合成序列天然平滑无换月跳空）用**同一模型库 + 同一
  Hurst 门控**预测，落库 `symbol='IDX:{sector}'`（同 as_of 可追溯）
- 品种级预测读取 `latest_sector_prediction(sector)` → `drivers.sector_pred_prob`
  （板块指数模型预测的 P(up)，实测 energy_chem=0.6588 注入 FG888 ✔）
- 防前视：板块预测仅用 as_of 之前指数数据；批量流程先板块后品种

### 特征 v2（`FEATURE_COLS` 10 个）

- `te_topk`：切换为真实传递熵权重（method=te → blend → prior 三级回落）
- `sector_pred_prob`：新增层级预测特征（列填充最新板块 P(up)）
- rf/xgb/gpr/lstm 特征集自动扩展（LSTM 重训后 n_features=1+10）

### 调度（§16.7 ⑤）

- `transmission_weekly_recalc`：周六 06:30（LSTM 周训 06:00 之后），CLI
  `scripts/calc_transmission.py`（含 --skip-hierarchy）
- `predict_symbols` 批量流程自动先跑层级预测

### 实测

- 全量批量 **65/65 成功**（73 品种 − 金融 8）；层级预测 5 板块留痕 IDX:*
- transmission_weights 186 行；单品种 FG888 trend 门控 7 模型无错误

### M4 前遗留

- blend 数据侧为 corr/granger/te/var 等权均值，M4 回测可按各 method 历史命中率再自适应
- precious 板块（AU/AG 仅 2 员）无指数：其层级预测特征 sector_pred_prob 为 NaN（LSTM/树模型已容错填 0）；
  如需覆盖可放宽 min_members=2
- 权重周算的品种级回测准确率评估归 M4

---

## 2026-09-07 ｜ M4 审计修复（响应《M4回测审计报告_20260907》7 项工单）

### [P0] backtest_result 主键缺 symbol → 已修复

- 主键改 `(run_id, symbol, model)`，`backtest_symbols` 批量共用 run_id 且逐品种落库不再互相覆盖
- 旧数据全部清洗（互相覆盖已失真），修复后全量重跑
- **教训沉淀**：PG 保留字 `window` 连续踩坑两次，新表列名一律避开

### [P1-1] LSTM 前视泄漏 → 根治（机制层面）

- 训练快照带 `train_until`（训练数据截止日）：周训同时写 `{S}.pt`（线上）与 `{S}__{YYYYMMDD}.pt`（快照）
- 回测时另生成**"窗口起点前"无泄漏快照**（`train_until ≤ 首评估日`，训练数据不含回测窗口内信息，
  `update_current=False` 不覆盖线上权重）
- 回测逐点只加载 `train_until ≤ 评估日` 的最新快照；无合格快照 → **LSTM 诚实缺席**
- **验收**：FG888 回测快照 train_until=2026-06-11 ≤ 首评估日 ✔；泄漏修复后 LSTM 21 点
  dir_acc=0.75（原 0.85，回落；残余为 20 点小样本噪声 + regime 巧合，机制性泄漏已消除）
- 已知残留：单快照覆盖全窗口期间"训练集不含窗口数据但模型结构与超参已知"，属可接受范畴；
  更严格的 rolling-walk-forward 待 M4.1

### [P1-2] 权重月更护栏 → 已实现

- `min_sample_n ≥ 60`（per symbol per model）+ `min_symbols_in_run ≥ 10`（仅整批 run 够格）
- **跨品种聚合**：每 model 合并 run 内全部品种样本（Σacc×n/Σn）后再更新
- LSTM 观察期冻结 `lstm_weight_freeze=1.2`（config 可调）
- 旧被污染权重已清空（TRUNCATE model_weights），实测单品种 run 被拒：`symbols=1 < 10` ✔

### [P1-3] 分位区间校准 → 部分修复 + 标注

- kalman：改**新息（innovation）经验分位**区间 → 实测 quantile_hit=**0.90** 达标 ✔
- wavelet：AR(1) 残差经验分位 ✔
- rf/xgb：改**时间留出残差**（20% 留出预测残差经验分位）；短窗口（n=20）下校准评估噪声大，
  by_state 增加 `interval_calibrated=false` 语义——M5 看板展示区间需带"未校准"提示，
  可靠验收需 test_days≥60 点以上样本
- montecarlo/garch/markov/bayesian 经验/参数区间保持

### [P1-4] 模型静默失败可见 → 已实现

- `by_state.error_counts` 记录 per-model 失败点数（区分"不适用"与"崩溃"）
- 新表 `backtest_detail`（P2-7）：run_id+symbol+model+eval_date 主键，逐点 pred/prob/point/low/high/actual 全量落库，支撑 M5 下钻

### [P2-1] 标签口径 → 配置开关（待用户裁决）

- `config:backtest.label_metric = close`（现状，内部自洽）｜`settle`（PRD-⑩ 原始口径）
- settle 模式下无结算价的评估点自动跳过；**裁决后改一行配置即可切换，无需改代码**
- 建议裁决输入：审计实测 close/settle 方向不一致率 29.1%

### [P2-2] 回测/线上权重来源 → 已统一

- 回测 ensemble 与线上 predict 均走 `model_weights` 表（fallback config）

### 实测汇总

- 单品种 FG888：21 评估点、7 模型（trend 门控，LSTM 无泄漏快照参与）、kalman 校准 0.90
- 护栏：单品种 run 被拒 `symbols=1 < 10`
- 全量重跑：65 商品品种后台执行中（金融 8 品种按 §16.1 排除），完成后
  `POST /backtest/update-weights` 生成跨品种聚合权重（预计 model 总样本 ≈ 5000+）

### 权重月更首跑结果与后续

- test_days=60 的 run：护栏正确拒绝（n=20 < 60，skipped=675）——护栏按设计工作
- 已按 ⑲ 语义排程 **test_days=250 / step=3**（84 评估点/品种，n≈84 ≥ 60 达标）后台重跑
  （预计 2~3 小时）；完成后手动 `POST /backtest/update-weights`，或等月更任务自动执行
- 长期运行节奏：周六 07:00 周度回测（test_days=60 快照监控用）+ 每月 1 日月更前
  建议先跑一次 test_days=250 深度回测（后续可做成月更任务的前置步骤，M4.1）

---

## 2026-09-07 ｜ M4：回测引擎 + ⑳ 权重月更闭环

### 回测引擎（`app/backtest/engine.py`，PRD §6）

- **逐评估点滚动回测**：最后 ⑲250 交易日内每 3 根一个评估点，每个评估点构造
  "截至当日"特征快照（`features_from_series` 历史切片，防前视①）→ 门控内全模型预测
  → 与**次日真实涨跌**（ret_close，t→t+1）对比
- 指标 per model：dir_acc / MAE / RMSE / 分位覆盖命中 quantile_hit / sample_n；
  **Hurst 状态分层** by_state（§6）；`recent60_dir_acc`（⑳ 权重月更输入）
- **ensemble 同步回测**作为策略基准（同 §5.3 融合规则，抽出 `_ensemble_output` 复用）
- 传导特征整段一次构造后逐日切片（滞后口径，无前视且性能可控）
- 落库 `backtest_result`（run_id+model 幂等覆盖）

### ⑳ 权重月更闭环（`app/backtest/weights.py`）

- 新表 `model_weights`（model, weight, dir_acc, sample_n, source）
- 更新公式：`w = clip((acc60 − 0.5) × 4, 0.1, 2.0)`（随机水平附近近失权）
- **engine 权重来源升级**：`model_weights` 表优先 → config 等权回落
- 月更任务：每月 1 日 06:30；周度回测：周六 07:00；CLI `scripts/run_backtest.py [--update-weights]`

### 实测（FG888，⑳250 日 / 84 评估点）

| model | dir_acc(60点) | 更新后权重 |
|---|---|---|
| lstm | 0.85 | **1.40** |
| kalman | 0.65 | 0.60 |
| hmm | 0.55 | 0.20 |
| markov/montecarlo/bayesian/ensemble | 0.54~0.56 | （mean_revert/neutral 门控，本轮未触发权重更新） |
| rf/fourier/arima | 0.40~0.50 | 0.10（近失权） |
| garch | 0.44 | — |

- 权重生效验证：FG888 预测 p 由 0.6032 → 0.5624（DB 权重参与融合）✔
- 全历史（520 点）预跑：markov 0.559 / montecarlo 0.548 / bayesian 0.538 / lstm 0.526 最高，
  garch 0.44 / arima 0.48 偏低——与模型特性相符

### 已知限制

- LSTM 回测使用周更权重（训练时点晚于评估点，存在轻微 look-ahead 倾向），
  结果解读时对该模型准确率保守看待；M4.1 可改为按评估日动态加载历史权重
- 全量回测（65 品种 × 84 点 × 13 模型）约 1~2 小时，调度在周六 07:00 后台执行；
  手动 API 支持指定品种缩小范围
- 回测窗口默认 250 日（⑲）；test_days/step 可通过 API/CLI 调整

---

## 2026-09-08 ｜ M4.1：LSTM walk-forward 回测（遗留项闭环）

### 实现（`BacktestParams.lstm_retrain_every=60` 根）

- 回测窗口按线上重训节奏**分段**（250 日 ≈ 4~5 段），逐段生成"段起点前"快照：
  训练数据 = `df[df.index < 段首评估日]` 最后 250 根（严格早于段内全部评估日）
- 每段快照 `train_until = 段前最后一日`；段内评估点加载 `train_until ≤ t` 的最新快照
  （复用审计 P1 的 `_select_weight_path` 机制）
- `update_current=False`：回测快照**不覆盖线上权重**，线上仍由周训维护
- 实测 FG888（test_days=60 快跑）：段 1 快照 `train_until=2025-08-25`、段 2 `2026-06-10`，
  两个分段文件正确落盘；`FG888__20260903/04.pt` 为线上周训快照（并行不冲突）

### ⑳ LSTM 权重冻结解除

- 泄漏已机制性根治（walk-forward 快照无前视），`lstm_weight_freeze` 改为
  **null（默认不冻结）**——权重月更按 walk-forward 准确率正常更新 LSTM
- 如需重新冻结（如怀疑新泄漏），config 填数值即可

### 效果与语义变化

- 回测语义从"单快照贯穿全程"（陈旧模型偏差）升级为**walk-forward**：
  忠实模拟线上"每周重训"行为，LSTM 的回测分数即为"定期重训策略"的历史真实表现
- 全量 250 日 walk-forward 回测已排入后台（约 2~3 小时），完成后
  `POST /backtest/update-weights` 更新权重
- 训练成本：每品种 +4~5 次 LSTM 训练（~15s），全量回测时长增至约 2.5~3 小时（可接受）

---

## 2026-09-08 ｜ 响应《M4复验报告》：遗留 5 项处理 + 权重月更端到端验证

### P2-1 口径裁决 → ✅ 已定：close

- 用户裁决：回测与预测标签维持 **close（收盘价）口径**，并已同步 PRD 文档
- `config:backtest.label_metric = close` 维持现状；settle 开关保留（如未来口径变更一键切换）
- 实现问题清单同步：PRD-⑩ 的"结算价涨跌幅"承诺在**回测标签层**调整为 close 口径
  （预测响应仍输出 ret_close/ret_settle 双字段，数据层不变）

### 遗留 5：NaN 污染 → ✅ 已修复

- 根因：hmm/lstm 个别点 point 为 NaN（hmm 状态收敛异常），旧版 metrics 未过滤
- 修复：①采集侧过滤非有限 point/low/high（不入 recs）；②聚合侧再过滤（双保险）；
  ③落库 `_clean()` NaN→None 兜底
- 实测：修复后全量回测 **0 新增 NaN**；历史 10 行 NaN 已清理

### 遗留 4：49 品种 run 缺口 → ✅ 已修复（自适应缩减）

- 根因：test_days=250 固定门槛，数据不足 310 根的新品种（LG/PR/PS/LC 等）被直接跳过
- 修复：**test_days 自适应缩减**——可用数据 ≥ 60+step 即以 `min(250, 可用-60)` 根回测并注明
- 实测：修复后全量回测 **73/73 全覆盖**（含金融 8 品种，符合 §16.1 预测范围排除仅在 predict 侧）

### 遗留 2：权重表重建 → ✅ 端到端完成

- 修复后全量 run `20260908_120937`（73 品种）触发 `update_model_weights`：
  - **updated=10**（10 模型跨品种聚合成功，样本 n=255~6132，全部 ≥ 60 门槛）
  - **skipped=312**（wavelet/garch 等部分品种 n<60 被护栏过滤，符合设计）
  - 聚合结果：全部模型 acc60 ∈ [0.46, 0.51]（与审计"无显著预测力"结论一致）→
    权重贴近 0.1 下限（诚实反映模型当前无增量）
- 旧 NaN 行清理 10 行；权重表 0 NaN

### P1-3 遗留（rf/xgb/wavelet 区间）→ ✅ 已修复（PRD §17 同批）

- **rf/xgb**：留出残差与波动率缩放均无效（树模型点预测误差与近期波动弱相关）→
  改用**原始收益无条件经验分位区间**（P5/P95 + point，分布平稳假设下数学保证 ~90% 覆盖）；
  代价：区间恒宽不含模型信息——树模型的票权贡献保留在方向投票
- **wavelet**：平滑序列残差与真实收益尺度脱节（qhit 0.21）→ 同改无条件分位
- 实测 qhit：CU888 rf=0.95/xgb=0.93；SA888 rf=0.89/xgb=1.00/wavelet=1.00；
  FG888 rf=0.80/xgb=0.85（20 点小样本，二项噪声 ±0.18 内）
- **P1 三项（LSTM 泄漏 / 权重污染 / 区间校准）全部转 FIXED**
- M5 看板：rf/xgb/wavelet 区间仍标"未校准/待重跑积累"（保守展示）；
  短窗口（<60 点）qhit 仅作指示

---

## 2026-09-08 ｜ PRD §17 工单：caliber 全链路口径声明（①②③④）

### 工单① prediction_result + 预测输出契约 ✅

- 表加 `caliber TEXT NOT NULL DEFAULT 'close'`；落库时写入 `label_metric`
- §5.3 JSON 契约新增 `"caliber": "close"` 字段（v1 向后兼容，简报引擎可平滑升级）

### 工单② briefing_signal ✅

- 表加 `caliber TEXT NOT NULL DEFAULT 'close'`；ORM 同步（写入侧 M5 `/integrations/signals` 实现时使用）

### 工单③ config 注释 ✅

- `config.py` label_metric 注释改为"已裁决 close（PRD §15/§17，2026-09-08）"

### 工单④ 看板标注口径（M5 前置）✅

- `/backtest` 响应每行加 `interval_calibrated`（rf/xgb/wavelet=false）与 `caliber`
- `/bars/{symbol}` 响应头 `X-Caliber: close`（看板涨跌展示口径标注）
- `/predictions` 响应逐条加 `caliber`
- 回测落库 `backtest_result` / `backtest_detail` 同步 `caliber` 列

### M5 看板（2026-09-08 上线）

- **架构**：Vite + React 18 多阶段构建（node:20-alpine 构建 → FastAPI 静态托管，
  无新增容器）；API 路由优先注册、静态兜底，同源无 CORS
- **聚合 API**（`app/api/routers/dashboard.py`）：`/dashboard/overview`（品种总览+最新预测）、
  `/dashboard/sectors`（热力图+板块排名）、`/dashboard/backtest-summary`（跨品种聚合+
  Wilson CI+显著性判定）、`/dashboard/backtest/{run_id}/detail`（逐点下钻，P2-7）、
  `/dashboard/quality`（就绪+日历模式+回补进度+工单）
- **展示规范落实**（复验报告）：全部涨跌标"收盘价口径"徽标；dir_acc 带 Wilson 区间与
  样本量；ensemble 未显著时显示"未显著优于随机（与抛硬币无异）"（CI 跨 0.5 自动判定）；
  rf/xgb/wavelet 标"未校准/区间待重跑"
- **附带修复**：`update_model_weights` "最新任意 run" bug（与 v2 审计脚本同根源）——
  改为"最新整批 run"（SQL HAVING 品种门槛过滤），backtest_summary 同步修正；
  backtest-summary 由逐行改为**按模型跨品种聚合**（Σacc×n/Σn，与权重月更同口径）

---

## 2026-09-09 ｜ M5.5（PRD v1.3 §18）：条件反转 + 指标升级 + P7 精简

### §18.2 P1 条件反转模型 + 信号门控 ✅

- 新增 `app/predictors/reversal_model.py`（注册名 `reversal`）：
  - 门控：`|ret_t| <= gate_pct`（config，默认 2.0%，v1.3.5 对齐 research τ*=2.0%）→ 输出 `signaled=False`（"无观点"），
    **不参与集成投票、回测不计入 dir_acc 分母**；ModelOutput 新增
    `signaled` / `gate_reason` 字段（向后兼容，其它模型恒 True）
  - confidence 随 `|ret_t|` 单调上升（`0.5 + 0.25×min(1,|ret_t|/2%)`）；
    Δoi>0 软上调 `oi_boost_prob`（FeatureSnapshot 新增 oi/doi，oi 缺失跳过）
  - 幅度点估计 = `-ret_t × strength`（0.3），区间用近 20 日滚动 σ
- 集成接入：全状态注册（trend/mean_revert/neutral），`weights.reversal=1.2`（≥现有模型）；
  融合层 `_ensemble` / `_ensemble_output` 只统计 `signaled=True` 的模型，
  契约 `_meta` 增加未信号模型留痕
- 回测引擎：`backtest_detail` 新增 `signaled` / `gate_reason` 列
  （`db/init/07_reversal_gate.sql` 已迁移）；未信号行不入命中分母，
  `by_state` 记录 `coverage`（门控覆盖率）与 `signaled_n`

### §18.2 诊断任务（rf/xgb 为何 <0.5）✅ 结论：显式说明

诊断脚本 `scripts/diag_rf_xgb.py`（容器内实测 FG888）：

1. **标签/滞后符号约定无误**：X[t]=[rets[t-lag]…]→y[t]=rets[t]，自回归结构正确
2. **实测学到的是弱反转倾向**：60 根窗口内预测符号与 ret_t 反向 6-7/10（rf 4/6、xgb 3/7）
3. **非"方向学反"**：若学反，翻转后应显著>0.5；实际 rf/xgb CI 上界仅 0.505，
   翻转后同样不显著——属于"回归点估计的方向映射在高方差下无稳定信息"
4. **处置**：按 P7 移出默认门控池（保留在 MODEL_REGISTRY 可手动启用）；
   若未来要挖掘方向信息，应改分类目标（up/down 分类器）而非回归符号——M6c 备选

### §18.3 P2 指标升级 ✅

回测引擎 `_metrics` 新增（全部入库 `backtest_result.by_state` JSON，看板透出）：

| 指标 | 定义 |
|---|---|
| `dir_acc_weighted` | 按 \|实际涨跌\| 加权准确率（大波动权重高，**新主指标**） |
| `ic` / `rank_ic` | 预测幅度 vs 实际收益 Pearson / Spearman 相关 |
| `icir` | 评估点 5 等分段 IC 的均值/标准差 |
| `coverage` / `signaled_n` | 门控覆盖率（发信号占比） |
| `coverage_curve` | 覆盖率-准确率曲线（按 \|point\| 分位 top 10%–100% sweep） |
| `net_pnl_mean/total` | 方向策略净 P&L（扣双边成本，`backtest.cost_pct=0.065` 可配置，§18.11③） |
| `win_rate_pnl` | 净收益为正的笔占比 |

看板"回测面板"改版：主指标列 = 加权准确率 + IC/ICIR + 覆盖率 + 净P&L/笔；
新增"覆盖率-准确率曲线"表；dir_acc 降为参考列。

### P7 模型池精简 ✅

- 移出默认 gate_models：`rf` / `xgb`（回测<0.5 且未校准，见诊断）、
  `wavelet`（覆盖率过低 n=88/6132）、`gpr`（回测期持续失败）
- 保留在 `MODEL_REGISTRY` 可手动启用；默认池 = fourier/markov/arima/garch/kalman/
  hmm/bayesian/montecarlo/lstm/**reversal**（10 个）
- `config/local.yaml` 与 `cloud.yaml` 同构更新

### 附带修复

- `backtest_detail` 批量落库无冲突处理：同 run_id 重跑报 UniqueViolation →
  加 `on_conflict_do_nothing`（幂等，与 backtest_result 对齐）

### §18.11 验收（2026-09-10 数据，gate_pct=2.0% 生产口径）

> 口径说明（2026-09-10 复核补记）：**方向验收以「池化（signaled 信号加权）」口径为准**；
> `sample_n` 加权/宏平均（0.5281）是 ⑳ 权重月更的**输入口径**（基于 `recent60_dir_acc`），
> 不用于验收判定。二者不可混用。

| 门槛 | 要求 | 实测（2026-09-10 全量 run `20260910_143938_bt`，gate_pct=2.0%） | 判定 |
|---|---|---|---|
| 条件反转 **池化** dir_acc（验收口径） | ≥ 0.53 | **0.5492**（n=914，CI [0.5168, 0.5812]，p=3.22e-03） | ✅ |
| 条件反转 sample_n 加权（权重输入口径，非验收） | — | 0.5281（recent60 聚合，落 0.50–0.54 弱边沿区间） | 参考 |
| 覆盖率 | ≥ 15% | **14.91%**（signaled 914 / 总评估点 6132；较研究口径 12.9% 改善） | ⚠️ 仍略低 |
| 含成本净 P&L（**池化**口径） | > 0 | **+0.1326%/笔**（n=914，累计 +121.21%，胜率 pnl>0 = 0.5153）；成本按 §18.3 **双向开平 2×0.065% = 0.13%/笔**。⚠️ 口径提示：生产 `by_state.net_pnl_mean` 存的是**宏平均**（逐品种 mean 再平均）= +0.0279%/笔（66 品种等权，正 38/66），与池化口径差异大；验收以池化口径为准 | ✅ |

- **对照（2026-09-09 研究口径，已有归档）**：`research_m5_validate.py` 未施加平台 3 日 eval 抽样，
  holdout（2024+）`reversal∩|ret|>2.0%` dir_acc=**0.5368**（CI [0.5240,0.5496]，p=2.04e-08）、覆盖率 12.9%。
  该值为**研究方法学口径**，与生产 3 日抽样口径（上表）不可直接比较；两者在阈值对齐（τ\*=2.0% → gate_pct=2.0%）后
  已收敛到同一区间（研究 0.5368 / 生产池化 0.5492）。
- **上轮旧口径已废弃**：2026-09-09 生产 run `20260909_171921_bt` 使用 gate_pct=1.0%（与验收阈值 τ\*=2.0% 错配），
  其 reversal 池化 dir_acc=0.5005（n=2192，不显著）不构成验收依据（见 2026-09-10 复核段「发现 4 阈值错配」）。
- **其余模型**：13 个模型中 reversal 是唯一显著者；其余 12 个池化 dir_acc 0.49–0.52 近随机，ensemble=0.4992（不显著）。
- `gate_pct` 保持 config 可调（**2.0% 默认**，v1.3.5 对齐 research τ\*=2.0%）；18.12 要求阈值年度重估、禁止硬编码历史最优。

### ⚠️ 实证补充：reversal 在近 60 交易日趋势市中失效（18.12 风险应验）

首期整批回测（`20260909_114640_bt`，72 品种 × 近 60 交易日，2026-06~09）：

| 模型 | wacc | IC | 净P&L/笔 |
|---|---|---|---|
| hmm | 54.0% | +0.011 | -0.097% |
| bayesian | 53.1% | -0.146 | -0.124% |
| lstm | 52.2% | +0.086 | -0.099% |
| ensemble | 50.4% | +0.022 | -0.193% |
| **reversal** | **42.2%** | -0.010 | **-0.205%** |

- reversal acc=0.43 **显著低于 0.5**（CI 下界 0.387），与长周期 OOS（2024+ 全面板 0.5368）矛盾
- 原因：近 60 日为单边趋势市（§18.12："反转在单边牛市弱化，可叠加 Hurst 状态过滤"）；
  窄窗口下反转规则系统性失效
- **处置**：
  1. 不改默认参数（18.12 禁止追逐近期最优）；长周期 OOS 验收结论维持
  2. ⑳ 权重月更按"近 60 评估点准确率"自动调权——250 日全量回测完成后 reversal
     将被自动降权，正是月更机制的设计目的
  3. M6c 备选：叠加 Hurst 状态过滤（仅 mean_revert 状态发反转信号）

---

## 2026-09-09 ｜ M6a（PRD v1.3 §18.4+§18.5）：龙虎榜 + 库存接入

### 接口探针（akshare 1.18.94 实测 2026-09-09）

| 接口 | 可用性 | 数据规模 | 备注 |
|---|---|---|---|
| `get_rank_table_czce(date)` | ✅ | 2567 行/日（CZCE 全部 17 品种） | dict{合约: DataFrame}，12 列含 rank/long/short/chg |
| `get_cffex_rank_table(date)` | ✅ | 496 行/日（CFFEX 8 品种） | dict 结构 |
| `get_shfe_rank_table(date, vars_list)` | ❌ | 静默返回空 dict{} | 5 个日期均失败，v1.18.94 数据源坏 |
| `futures_dce_position_rank` | ❌ | BadZipFile | DCE 数据源坏 |
| `futures_inventory_em(symbol)` | ✅ | 72 个交易日/品种 | 列：日期/库存/增减；symbol='a'/'fg' 等 |
| `futures_inventory_99(symbol)` | ❌ | JSONDecodeError | 接口异常 |
| `get_receipt(start,end,vars_list)` | ✅ | 跨所批量 | 备用入口 |
| `futures_shfe_warehouse_receipt` | ✅ | dict | 仓单 |

### Schema 迁移 ✅

- `db/init/08_member_position.sql`：`member_position_rank`（PK: trade_date+exchange+symbol+member+version）
- `db/init/09_inventory.sql`：`inventory`（PK 含 warehouse 字段，em 接口无仓库时记空串 ''）

### 采集器（`app/ingest/member_position.py` + `inventory.py`）✅

- **持仓采集**（已实采验证）：
  - CZCE 2567 行/日（含 FG/SA/MA/TA/SR 等郑商所全部品种）
  - CFFEX 496 行/日（IF/IC/IM/IH/T/TF/TS/TL）
  - SHFE/DCE 按 `EXCHANGE_COVERAGE` 矩阵自动跳过 + 记录到 errors
- **库存采集**：`futures_inventory_em(symbol)` 按品种循环，**42 品种 2010 行/次**（em 接口近 76 报告日全部覆盖；29 品种有数据）
- 防前视：T 日盘后 15:30 调度（持仓）、周五 16:30 周频（库存）

### 因子模块（`app/features/position_factors.py`）✅

- 持仓：net_long_top5（前 5 净持仓）/ cr5_long / cr5_short / net_chg_today / long_chg_top3
- 库存：inv_change_recent / inv_change_4w / inv_zscore_60 / inv_latest

### API ✅

- `GET /positions/latest?exchange=&symbol=&limit=`：最新交易日 top 20 持仓
- `GET /inventory/latest?exchange=&symbol=&limit=`：最新库存（按品种聚合）
- `POST /collect/{date_str}`：手动触发一次采集（CLI 补采/历史回填入口）

### §18.11② 因子 IC 验证脚本（`scripts/research_m6a_factor_ic.py`）✅

- 持仓：按 (date, ex, sym) 聚合净持仓/CR5 因子，与 ret_close 做截面 Spearman IC
- 库存：inv_change_4w 截面 IC vs 次日 ret_t
- 准入门槛：|t|>2 即可入集成权重（M6c 接入）
- 当前数据：持仓 1 个交易日、库存 76 个报告日（em 拉满）；待 30+ 日持仓积累后跑

### 端到端验证 ✅

`scripts/m6a_validate.py`（CI 用）实测 FG 持仓：

```
CZCE FG rank=1 永安期货（代客）       L=  137236 S=  186769 dL=+3836 dS=+729
CZCE FG rank=2 中信期货（代客）       L=  114611 S=  151970 dL=-731  dS=+8330
CZCE FG rank=3 东证期货（代客）       L=  104145 S=  119342 dL=-13779 dS=+3428
...
FG 持仓因子: net_long_top5=-64765, cr5_long=0.16, cr5_short=0.17, net_chg_today=-71091
```

### 已知缺口（akshare 1.18.94）

- **SHFE/DCE 持仓数据源坏**——M6a 覆盖 25/73 品种持仓（DCE 20 + SHFE 17 品种暂缺持仓）
- 待 akshare 修复后通过 `EXCHANGE_COVERAGE` 矩阵一键扩展；目前 DCE/SHFE 品种因子仅靠"无持仓 + 库存"评估
- DCE 品种在 §18.4 P3 范围中权重最大（M/Y/C/I/J/L/V/PP 是大宗之王），降级后 §18.11② 库存路径仍能完整覆盖

### 250 日全量回测进度

后台运行中（`20260909_121305_bt`），约 5 小时完成。完成后自动 `--update-weights`，⑳ 权重月更将纳入 reversal（近 60 日反转弱化将触发自动降权，机制自洽）

---

## 2026-09-09 ｜ PRD v1.3.2 升级：EXCHANGE_COVERAGE 矩阵化 + GFEX + 基差因子

### §18.13 EXCHANGE_COVERAGE 矩阵化（K1–K4 known_issues）✅

- 旧版（v1.3.1）`EXCHANGE_COVERAGE` 是 bool 字典 + 硬编码 try 跳过 SHFE/DCE，违反 v1.3.2 实现红线
- 新版矩阵（`app/ingest/member_position.py`）：每个所含
  `active/func/args/symbols/parser/since/known_issue/fallbacks`
- 采集器 `collect_member_position` 矩阵驱动：active=True 调对应 func，active=False 标 K1/K4 跳过
- **禁止在采集器内对 SHFE/DCE 写硬编码 try 豁免逻辑**（v1.3.2 实现红线）

### §18.13 known_issues 矩阵

| # | 交易所 | active | 状态 | 原因 |
|---|---|---|---|---|
| **K1** | DCE 持仓 | ❌ | akshare 修复后激活 | `futures_dce_position_rank` BadZipFile（v1.18.94） |
| **K1** | SHFE 持仓 | ❌ | 同上 | `get_shfe_rank_table` 静默返回空 |
| **K4** | INE 持仓 | ❌ | **不可获取** | akshare 无 INE 持仓/库存接口；持仓硬上限 68/73 |

### GFEX 接入 ✅

- `futures_gfex_position_rank(date, vars_list)` 实测可用（默认 2023-11-13，结构同构 12 列）
- 覆盖 SI / LC / PS（GFEX 工业硅/碳酸锂/多晶硅，2023-11-10 上市）
- today 入库 **GFEX 180 行**（首次）

### §18.5 基差因子（v1.3.2 新增）✅

- 新表 `spot_basis`（13 字段：spot_price / near_contract / dominant_contract / near_basis / dom_basis / near_basis_rate / dom_basis_rate）
- 采集器 `app/ingest/spot_basis.py`：`futures_spot_price_daily(start_day, end_day, vars_list)`
- 因子 `app/features/position_factors.py::basis_factors()`：
  - `dom_basis_rate_latest`（主力月基差率 %，核心）
  - `basis_change_5d`（5 日基差变化，趋势）
  - `dom_minus_near_basis`（近月 vs 主力月价差）
- 实采今日入库 **54 行**（期货公司现货价对主力月）

### 实采验证

| 指标 | 数值 | 变化 |
|---|---|---|
| 持仓总行数 | **3243 行/日** | +180 (GFEX 新增) |
| 覆盖品种 | **27 品种** | +2 (SI/LC) |
| 库存 | 2010 行 | 持平 |
| 基差 | **54 行** | 新增 |

### 持仓覆盖矩阵（v1.3.2 现状）

- CZCE 17 / CFFEX 8 / GFEX 2 = **27 品种**
- SHFE 17 / DCE 20 = 37 品种待 akshare 修复（K1，仅翻转 active=True 即可）
- INE 5 品种硬上限（K4，永不获取）

### 250 日全量回测

- 首次启动因 API 重启被杀；用 nohup 重启，写入 `logs/backtest_250d_v132.log`
- 完成后自动 `--update-weights`，reversal 因近 60 日 acc=0.43 将被自动降权

---

## 2026-09-09 ｜ M6b（PRD v1.3.2 §18.6）：carry/期限结构因子

### 接口探针与数据源矩阵

| 接口 | 可用性 | 说明 |
|---|---|---|
| `futures_zh_daily_sina(合约)` | ✅ DCE/SHFE/INE/GFEX | 大写代码（M2701/CU2610），8 列含 hold（持仓） |
| 同上 CZCE | ❌ | 大小写均 Length mismatch → **K5 登记**（v1.18.94） |
| `futures_contract_info_*` | ✅ 5/6（DCE 坏） | 到期日/合约清单 |

**CZCE 兜底方案**：走 tqsdk `get_kline_serial("CZCE.FG701", 86400)`（账号已有，M1 验证过）——矩阵 `CARRY_SOURCES`：DCE/SHFE/INE/GFEX=sina，CZCE=tqsdk，CFFEX=暂不采（金融期货 carry 弱）。

**活跃合约清单复用**：直接读 `spot_basis.near_contract / dominant_contract`（M6a 每日维护），无需 contract_info——两表联动。

### Schema + 采集器 ✅

- `db/init/11_contract_daily.sql`：`contract_daily`（PK: symbol+trade_date+version，统一大写代码）
- `app/ingest/contract_bars.py`：sina/tqsdk 双源 + `carry_factors()`（term_slope / roll_yield_monthly / dom_oi）
- 合约代码月解析：CZCE 3 位（701=2027-01）/ 其他所 4 位（2701）——自动兼容

### 首采实测 ✅

- **104 合约 / 6338 行**（sina 4546 + tqsdk CZCE 1792）
- BZ/PL 品种 exchange 未知跳过（futures_symbol 未登记，非阻塞）
- `GET /carry/latest`：44 品种因子

### carry 信号快照（2026-09-09）

| 品种 | 主力 | 次主 | 斜率 | 月化展期 |
|---|---|---|---|---|
| FG 玻璃 | FG701 | FG609 | -8.15% | **-2.04%/月** |
| SA 纯碱 | SA701 | SA609 | -10.77% | **-2.69%/月** |
| RB 螺纹 | RB2701 | RB2609 | -3.82% | -0.95%/月 |
| I 铁矿 | I2701 | I2609 | +2.30% | +0.58%/月 |
| CU 铜 | CU2610 | CU2609 | +0.54% | +0.54%/月 |

交叉验证：SA/FG 深度 Backwardation（负 carry）与库存高企（§18.5）方向一致——工业品产能过剩预期；CU/AU Contango。carry 因子与库存/基差形成三角互证。

### K5 登记（新 known_issue）

- **K5**：`futures_zh_daily_sina` 对 CZCE 合约返回 Length mismatch（v1.18.94）——已用 tqsdk 兜底，无覆盖缺口；akshare 修复后可切回 sina（免 tqsdk 限流）

---

## 2026-09-09 ｜ M6c（PRD v1.3.2 §18.7+§18.8）：波动率产品化 + 截面模块

### §18.7 P8 波动率产品化 ✅（端到端跑通）

**模型层**：
- `ModelOutput` 新增 `vol_forecast / vol_low / vol_high` 三字段（None=不输出）
- GARCH 改造：σ_{t+1} 条件波动率预测成为一等输出；|ret| P5/P95 用**半正态分位**
  （|X|~HalfNormal(σ)：P5=0.0627σ，P95=1.96σ）——`ret_low/high`（带符号）语义不变，
  quantile_hit 统计不受影响
- **GARCH 全状态注册**（trend/mean_revert/neutral）——σ_t 每品种每日可得；
  方向权重仍 0.8（波动率专长，方向贡献弱）

**契约/落库**：
- 迁移 `12_vol_forecast.sql`：`prediction_result` 加 `vol_point/vol_low/vol_high`
- §5.3 契约 v1 向后兼容扩展：预测响应含 vol 三件套
- 融合：取有 vol 输出模型的中位数

**回测指标**（`backtest_result.by_state`）：
- `vol_hit`：|actual| ∈ [vol_low, vol_high] 覆盖率（理论 0.9）
- `vol_rmse`：σ_t vs |actual| 估计误差

**看板**（新"波动率"页签）：
- 各品种 σ_t + |ret| 区间 + 截面 top 20% **异常波动预警**（对止损位/仓位管理提供输入）
- 历史 vol_hit 准度表（≥85% 校准良好；<80% 需 vol_scale 校准，同 P1-3 机制）

实测：FG888 σ=1.48% / SA888 σ=1.53% / CU888 σ=1.07% / AU888 σ=1.50%，
|ret| 90% 区间正确输出；`/dashboard/volatility` 预警 SA888（截面最高 σ）。

### §18.8 截面相对强弱模块 ✅（研究模块，不入 v1 主信号）

- 新增 `app/analytics/cross_section.py`：
  - `build_cross_section()`：日度截面排名（cs_rank 分位 / cs_score / cs_reversal 反转因子）
  - `cross_section_ic()`：截面 Spearman IC + ICIR + t 统计 + 2024 前后分期（§18.11② 准入口径）
  - `top_bottom()`：当日最强/最弱 top-k（看板用）
- 信号语义：多弱空强配对——**执行需双边流动性与做空机制**（§18.12），仅研究跟踪
- 看板"大类热力图"页已含板块强弱榜（M5 交付），截面因子排名后续接入该页

### P6 池化面板建模 ✅（M6c.1，2026-09-10）

- 目标：rf/xgb 训练集从单品种 250 根 → 全品种池化（评估点 84→2000+）；**先改分类目标**
  （§18.2 诊断：回归点估计方向学反 ≈0.49，方向信息弱）
- 实现：
  - 新增 `app/predictors/pooled.py`：
    - `build_pooled_panel(session, symbols, train_end)`：跨全部 *888 构造面板；
      标签 `y=up ⇔ ret_{t+1}>0`（收盘价口径 §17/§18.1）；
      **归一化滞后收益** `zL = ret_{t-L} / vol20_{t-L}` + `log(vol20_t)` 作特征
      —— 高/低波动品种尺度可比，池化不被高波动品种主导；vol 仅用 ≤t-L 数据（防前视）
    - `PooledClassifierModel`（`rf_pool`/`xgb_pool`）：全局分类器预训练 + 落盘
      （`/app/runtime/pooled/*.joblib`，与 LSTM 同机制）；`predict(rets)` 仅用单品种
      截至 as_of 序列构造同分布特征行，加载即用
  - 注册 `MODEL_REGISTRY["rf_pool"/"xgb_pool"]`；`config.local/cloud.yaml` 的 `weights`
    + `gate_models`（trend/mean_revert/neutral）全状态纳入方向投票；新增 `PooledConfig`
  - `scripts/train_pooled.py`：训练落盘（`--train-end` 生成防泄漏快照）；
    `scripts/research_m6c_pooled.py`：OOS 验证（TEST 2024+，评估点 84→2000+，
    对照单品种回归 + 条件反转 ~0.51 弱边沿（v1.3.3：研究方法学口径，生产 3 日抽样 ≈0.50））
- **防泄漏说明（重要）**：集成/回测中池化模型为「全历史训练」全局模型，eval_date 早于
  训练末日的回测点存在轻微前视（方向映射近似平稳）；**诚实 OOS 数字以
  `research_m6c_pooled.py` 的 TRAIN≤2023/TEST≥2024 严格划分为准**。后续可按 LSTM 机制
  增加 walk-forward 快照（按 eval_date 训 ≤t 的池化模型）消除该前视
- 验收（§18.11 口径）：以 research 脚本结论为准
- **实测（2026-09-10，TEST=2024+，73 品种 / 157266 行）**：
  - 评估点 **84 → 45900**（≈546×，P6 目标达成）
  - `rf_pool` TEST dir_acc=**0.5063**，Wilson CI[0.5018,0.5109]，二项 p=0.0067，
    IC=0.0053（|t|≈2.7，边际显著但不及 0.5 太多）
  - `xgb_pool` TEST dir_acc=0.5028，p=0.23（**未过 |t|>2 准入**）
  - 对照：单品种回归 rf 旧口径 TEST 平均 dir_acc=0.5007（印证 §18.2 方向学反）；
    `|ret_t|>1%` 子集池化 dir_acc=0.5090（p=0.026）；**与条件反转生产口径（~0.51 弱边沿）同量级**
- **结论与处置（诚实）**：池化 + 改分类目标**大幅扩展评估点并消除了单品种过拟合/低功率**，
  但暴露出短周期方向本身信息极薄——归一化滞后收益（尤其 z1 昨日收益）仅含≈0.5% 的
  反转信号，弱于显式条件反转模型。故：
  - `rf_pool` 边际显著 → 接入但**低权重 0.3**（config weights），待 ⑳ 月更按近 60 日
    准确率再调（预计趋近 0）
  - `xgb_pool` 未过准入 → **权重 0.0**（仍注册于 MODEL_REGISTRY 供研究/复现，不污染集成）
  - 不在 v1 主信号中高估池化分类贡献；方向主信号不以单一反转锚（生产口径 ≈0.50 弱边沿），交由 ⑳ 月更降权

### 250 日全量回测（续跑机制）

- 首跑 31/73 品种后因 Docker 引擎停止中断
- **新增 `--run-id/--resume` 断点续跑**（同 run+symbol 已有记录秒级跳过）
- 续跑中：41/73 → 预计 2 小时完成 → 自动 ⑳ 权重月更。

---

## 2026-09-08 ｜ M5：React 看板

### 架构

- **Vite + React 18 多阶段构建**：Dockerfile Stage 1 node:20-alpine 构建看板
  （npmmirror 源）→ Stage 2 python API 镜像内静态托管——无新增容器
- FastAPI **API 路由优先注册**，看板静态兜底（`/` → index.html、`/assets` StaticFiles），
  同源部署无 CORS 问题；未构建时 `/` 显示提示 JSON

### 聚合 API（`app/api/routers/dashboard.py`）

| 接口 | 内容 |
|---|---|
| `GET /dashboard/overview` | 品种总览：就绪门控 + 73 品种（板块分组）+ 最新预测（方向/p/幅度/置信度/caliber）+ 数据新鲜度 |
| `GET /dashboard/sectors` | 大类热力图：板块指数动量（1d/5d/20d）+ 板块强弱排名 + 成员当日表现（ret_close 与 ret_settle 参照列） |
| `GET /dashboard/backtest-summary` | 回测面板聚合：**跨品种聚合**（Σacc×n/Σn，与权重月更同口径）+ **Wilson 95% CI** + per-model 显著性判定 |
| `GET /dashboard/backtest/{run_id}/detail` | 回测下钻：逐品种/逐模型/逐评估点明细（backtest_detail，P2-7） |
| `GET /dashboard/quality` | 数据质量：就绪门控 + 日历模式 + 回补进度 + 异常工单 |

### 看板页面（5 页签，复验展示规范全部落实）

| 页签 | 内容 | 规范落实 |
|---|---|---|
| 品种总览 | 板块分组卡片：最新预测方向/p/幅度/目标日/置信度 + 数据新鲜度 | 收盘价口径徽标（§17④） |
| 预测详情 | §5.3 契约字段表 + **§16.6 传导来源卡片**（板块动量/背离/上游滞后/cost_gap/相关度/板块 P(up)）+ 板块背景 + 近 30 日线 + 预测留痕 | caliber 徽标 |
| 大类热力图 | 板块强弱排名（5 日动量条形）+ 热力色（红涨绿跌 A 股习惯）+ 成员当日热力表 | ret_settle 参照列 |
| 回测面板 | per-model 跨品种聚合指标 + **Wilson 95% CI** + 显著性措辞 + Hurst 三状态分层 + 未校准标注 | ensemble 措辞"未显著优于随机"（CI 跨 0.5 时）；rf/xgb/wavelet 标"未校准/区间待重跑" |
| 数据质量 | 就绪门控 + 日历模式 + 回补进度明细（73 品种逐行）+ 异常工单（⑰） | — |

### 附带修复

- **`update_model_weights` 同款"最新任意 run"bug**：默认 run 选取改为"最新整批 run"
  （symbol 数 ≥ min_symbols_in_run，SQL HAVING 过滤）——与 v2 审计脚本 P0 误报同根源，
  一次性修正三处（backtest_summary / update_model_weights / audit script）
- `api.js` 含 JSX 未用 .jsx 后缀导致 vite 构建失败 → 重命名 + 批量修正导入

### 实测（2026-09-08）

- 看板首页/静态资源：HTTP 200，JS bundle 161KB 正常加载
- overview：73 品种全部有预测，readiness.ready=true
- sectors：5 板块（energy_chem 5 日动量 +4.57% 排首位）
- backtest-summary：**13 模型跨品种聚合**（n 88~6132），全部模型 Wilson CI 跨 0.5
  → "未显著优于随机"（诚实结论与复验一致）；rf/xgb/wavelet 未校准标注正确；
  wavelet n=88（多数品种点数据不足被过滤，按护栏设计）
- quality：calendar=inferred、进度 73/73、工单可见

### 当前状态

- 权重月更全链路（回测 → 护栏 → 跨品种聚合 → 落库 → 线上读取）端到端验证通过
- 审计两轮全部工单闭环；M4/M4.1/M4.1+ 收官
- 下一步：M5 看板（回测面板带 Wilson 区间与样本量、rf/xgb/wavelet 区间标注、
  ensemble 措辞"未显著优于随机"）

---

## 2026-09-10 ｜ 复核结论与 §18.11 重版本（小齐 / WorkBuddy 受托）

> 独立复核：读 `实现问题清单.md` + 生产库内 250 日回测 run `20260909_171921_bt`，6 个只读 review 脚本。
> 完整报告见同目录 `复核结论_20260910.md`。

### 完成情况（确认）
M1–M4.1 / §16 / §17 / M5 看板 / M5.5 反转 / M6a 龙虎榜+库存 / M6b carry / M6c 波动率+截面+P6 池化 **全部 ✅**；
250 日全量回测已完成（73 品种 / 11 模型 / 49,320 明细行 / 权重已月更）；K1/K4/K5 known_issues 已登记、K2 已解除、EXCHANGE_COVERAGE 矩阵化已设计。

### 关键复核发现
1. **【高】反转模型 文档验收 vs 生产回测 背离**：§18.11 验收声称 reversal holdout dir_acc=**0.5368**（p=2.04e-08），并以「条件反转 OOS 0.535」为方向主信号锚；但生产 run `20260909_171921_bt` 中 reversal 仅 `signaled dir_acc=0.5000`（n=2192，CI 跨 0.5，未显著），ensemble=0.5050。**⚠️ v1.3.5 更正**：落差主因是**阈值错配**（研究 τ\*=2.0% vs 生产 gate 1.0%），**不是文档高估**——阈值对齐后生产**池化 dir_acc=0.5492（显著）**，与研究 0.5368 收敛（见下节「处置收口」）。
2. **根因（v1.3.5 更正）**：初版归因于「未施加平台 3 日 eval 抽样」**不成立**——阈值对齐后 3 日抽样下仍达 0.5492，说明抽样并不损害该边沿。**真实根因 = `gate_pct` 误用 1.0%**（研究选定 τ\*=2.0%）。下表 1.0% 门控复刻（② `|ret|>1%` n=2206 acc=0.5109 p=0.317）仅证明**该错误阈值区间内无可用边沿**。模型方向逻辑 100% 正确，非逻辑 bug。
3. **【已结】"生产 run 与日线复刻 n/acc 错位"非代码 bug**：用生产自有函数重算 backtest_detail 前 200 行，**pred_dir/signaled/actual 一致率 200/200** → 生产自洽、无缺陷。错位来自 `review_eval_sample.py` 重建评估窗口时**未钉 run 数据快照（2026-09-09，数据截至 2026-09-08）**，且少数品种（TF888/CS888/IC888）run 时数据较旧/已回填导致窗口错位（逐品种 eval 集重叠中位 1.000、该三品种=0.000）。属复核脚本方法学伪影，生产代码无需改动。
4. **【高·新发现】验收阈值 ≠ 上线阈值**：一手报告 `logs/research_m5_validate_20260909_114608.md` 显示 1c 选定阈值 **τ\*=2.0%**（TRAIN 选出；规则「反转 ∩ |ret|>2.0%」，TEST acc=0.5368、覆盖 12.9%），而**生产实际部署 `config/cloud.yaml → predict.reversal.gate_pct=1.0%`**（覆盖 35.75%）。即「通过验收的规则」与「上线的规则」阈值不一致——用生产 1.0% 门控本就不可能复现 2.0% 子集的 0.5368。**（v1.3.5 更正：此阈值错配即 0.5368→0.5000 落差的主因，3 日抽样非主因。）** 须对齐阈值后重跑再作准入判定。

### 处置（已执行）
- ~~**PRD 升级至 v1.3.3**：§18.11 将「条件反转 OOS 0.535/0.5368（≥0.53 准入）」重新界定为「~0.51 弱边沿（研究方法学口径，非生产 3 日抽样口径）」……~~ → **该处置已被 v1.3.5 更正**：0.5368 无需降级（它本就是 τ\*=2.0% 口径的正确效力），正确处置是**把部署阈值对齐到 2.0%**（已完成，见下节）。
- **PRD §18.11/§18.12 补记阈值错配**：研究 τ\*=2.0%（覆盖 12.9%）vs 生产 gate_pct=1.0%（覆盖 35.75%），验收须以生产实际阈值重跑；并更正 PRD 对不存在文件《M5指标验证报告_20260909.md》的引用（实际在 `logs/research_m5_validate_*.md`）。
- `复核结论_20260910.md`：完成情况总览 + 3 项发现 + 回测结论 + 风险清单 + 复核脚本清单（含 review_rootcause*.py 根因脚本）。
- **`scripts/review_eval_sample.py` 修正为 v2 严谨版**：原版从 daily_bar 重建评估窗口会误报"生产 vs 复刻错位"；v2 直接以 `backtest_detail` 的 (symbol, eval_date) 为锚、钉 run 数据快照复算，验证生产 100% 自洽（signaled/pred_dir/actual 均 6132/6132 一致）且复现 dir_acc=0.5005。
- ~~不改动任何生产代码与默认参数（遵守 §18.12 禁止追逐近期最优）。~~ → **该结论已更新（阈值对齐属必要修复，非"追逐近期最优"），见下。**

---

## 2026-09-10 ｜ 处置收口（阈值对齐结案 + 本轮手动修改）

### 一、CodeBuddy 已执行（复核独立验证，非引用声明）
- **`gate_pct` 1.0% → 2.0% 对齐**（`app/core/config.py` 默认 + `config/cloud.yaml`），全量重跑 run `20260910_143938_bt`（73 品种 / 13 模型 / 64,877 明细行）。
- 复核实测：reversal **池化**（signaled 信号加权）dir_acc = **0.5492**（n=914，覆盖率 14.91%，Wilson [0.5168,0.5812]，p=3.22e-03 **显著**）→ §18.11 第 1 条 **≥0.53 在生产口径下达标**；研究口径 0.5368 与生产池化 0.5492 **收敛同区间**。
- 权重落库 `model_weights.reversal` w=0.5244 = clip((0.5281−0.5)×8+0.3)，由新 run 于 2026-09-10 16:06 更新。

### 二、本轮手动修改（WorkBuddy 执行）
| # | 对象 | 改动 | 验证 |
|---|---|---|---|
| 1 | `app/backtest/weights.py` | `on_conflict_do_update.set_` 增补 `updated_at=func.now()`（根因：ORM `onupdate` 对 Core 语句不触发 → 时间戳停在首次插入）；顺带修正顶部过期公式注释（×4 → ×8+0.3） | `py_compile` ✅；容器实测 **12/12** 刷新、权重数值不变（`scripts/verify_weights_updated_at.py`）✅ |
| 2 | 本文档 §18.11 | 验收表改 **2026-09-10 / gate_pct=2.0% 生产口径**；分列池化（0.5492 ✅）vs sample_n 加权（0.5281，权重输入口径）；旧 1.0% run 作废；覆盖率分母修正为总评估点 6132 | ✅ |
| 3 | PRD | **升 v1.3.5**：§18.2 新增「口径纪律」（验收只用池化）；§18.11 阈值对齐结案段；§18.12「验收阈值≠部署阈值」结案；版本头 v1.3.4 未同步问题规范为 v1.3.5 | 工作副本与 `docs/` 副本 `diff` 一致 ✅ |

### 三、遗留（真实未达，不掩饰）
- **覆盖率 14.91% < 15%**：较研究 12.9% 改善但仍差 0.09pp，已记"边际未达"；**建议阈值 sweep（如 gate 1.5%）取平衡点，而非调参迎合门槛**（§18.12 禁止追逐近期最优）。

### 四、部署注意（重要）
- `/app/app` **不在容器 bind mount 列表**（仅 `logs`/`runtime`/`config`/`imports`），故 app 代码改动须 `docker cp` 进容器或**重建镜像**方生效；本轮 `weights.py` 已 `docker cp` 并验证。若重建镜像，宿主源码即基准。
- `config/` 为 bind mount（ro），故配置类改动（如 `gate_pct`）**无需重建即生效**。

### 五、追补（阈值 sweep + 门槛定稿 + 成本口径修正，2026-09-10）

- **阈值 sweep（离线等价扫描，`scripts/review_gate_sweep.py`）**：在 run `20260910_143938_bt` 真实 eval 锚点上扫 gate 0.5→5.0%。自洽校验：未发信号行我方 |ret| vs 生产 `gate_reason` **5218/5218 一致**；发信号行 |ret| 最小值恰 **2.0000%**；gate=2.0% 重算复现库内 0.5492。

  | gate% | 覆盖率 | 池化 acc | 净 P&L/笔* |
  |---|---|---|---|
  | 1.0 | 35.67% | 0.5231 | **−0.0145%** |
  | 1.5 | 22.05% | 0.5296 | +0.0224% |
  | **2.0** | **14.87%** | **0.5493** | **+0.1335%** |
  | 2.5 | 10.60% | 0.5615 | +0.2148% |
  | 3.0 | 7.97% | 0.5644 | +0.3013% |

  \* 净 P&L 按双向开平 **2×0.065% = 0.13%/笔** 扣减。

- **核心结论**：acc 与覆盖率**单调反向**，「覆盖率 ≥15%」与「acc ≥0.53」**可行域不相交**（结构性 trade-off，非"差 0.13pp"运气）；且 **gate=1.0% 净 P&L 转负** → 下调阈值属"两头不达标"。
- **⚠️ 成本口径修正**：净 P&L 此前误按**单边** 0.065% 扣减，已改为**双边 0.13%/笔**（与 `engine.py` 的 `pnl = sign×actual − 2×cost` 一致）；上表 §18.11 验收行的净 P&L 已由「宏平均 +0.028%/笔」修正为「**池化 +0.1326%/笔**」（宏平均 = +0.0279%/笔，两者口径不同，验收以池化为准）。
- **门槛处置定稿（产品裁决：选 C）**：**维持「覆盖率 ≥15%」硬门槛**，同时登记为「结构性不可达」已知偏差（PRD §18.13 **K6**）；`gate_pct` 保持 **2.0%**，禁止调阈值迎合，提覆盖率唯一合规路径 = **扩品种/扩样本**。
- **修正 §18.12 失效场景措辞**：实证显示 2024–2026 窗口 `trend` 状态为正贡献（gate 2.0% acc=0.5395），真正逆效的是 `mean_revert` 状态（acc 0.375–0.444）。