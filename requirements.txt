# =====================================================
# 期货预测平台 - Python 依赖（锁定主版本）
# Python 3.11+
# =====================================================

# --- Web / API ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- 数据库 ---
SQLAlchemy==2.0.36
psycopg[binary,pool]==3.2.3
# M8：决策链路（WB skill 的 _fut_research/_fut_fund_light）直接用 psycopg2 裸连接
# 调 pd.read_sql_query；保持业务代码零改动优先，故并存第二驱动（PRD M8 §5.2①）
psycopg2-binary==2.9.10
asyncpg==0.30.0
alembic==1.14.0

# --- 行情源 ---
# 1.18.94 实测通过（新浪主连接口兼容性最好）
akshare==1.18.94
tqsdk==3.7.5

# --- 数据处理 ---
pandas==2.2.3
numpy==2.1.3
python-dateutil==2.9.0.post0

# --- M2 计算 / 模型 ---
scipy==1.14.1
statsmodels==0.14.4
scikit-learn==1.6.1

# --- M3 模型库 ---
PyWavelets==1.7.0        # 小波变换
arch==7.0.0              # GARCH 族
hmmlearn==0.3.2          # 隐马尔可夫
xgboost==2.1.1           # XGBoost
# torch（LSTM）不在此处安装：镜像层单独用 CPU 源，见 Dockerfile

# --- 调度 / 后台任务 ---
APScheduler==3.11.0
tenacity==9.0.0

# --- 工具 ---
orjson==3.10.12
loguru==0.7.3
httpx==0.28.1
typer==0.15.1

# --- 测试 ---
pytest==8.3.4
pytest-asyncio==0.25.0